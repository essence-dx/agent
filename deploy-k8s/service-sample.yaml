apiVersion: v1
kind: Service
metadata:
  name: dx-agents
  namespace: dx-agents
  labels:
    app.kubernetes.io/name: dx-agents
    app.kubernetes.io/part-of: dx-agents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dx-agents
  ports:
    - name: gateway
      port: 42617
      targetPort: gateway
      protocol: TCP
