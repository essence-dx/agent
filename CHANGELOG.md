# Changelog

## Unreleased

### Changed

- Changed fully flag-driven `dx-agents quickstart` to apply immediately with balanced runtime/risk defaults, SQLite memory, and no extra channels instead of opening the interactive checklist.
- Changed DX Agents Quickstart to read provider API keys from `--api-key-env <ENV_VAR>` instead of accepting secret values directly on the command line.
- Documented the latest DX Agents runtime hardening status for Gemini quota exhaustion, safe Quickstart key seeding, and shell completion branding.

### Fixed

- Made ACP bridge launch DX-first by preferring `DX_AGENTS_ACP_BRIDGE_TOKEN` and `DX_AGENTS_ACP_PAIRING_CODE`, keeping legacy `ZEROCLAW_*` fallback, and updating ACP/gateway recovery commands to `dx-agents`.
- Rebranded ACP initialize metadata to `dx-agents-acp` and `_meta.dxAgents` while preserving `_meta.zeroclaw` for compatible clients.
- Classified and encrypted file upload bundle headers, file download headers, Gmail push webhook secrets, and voice-call auth tokens so config saves and wire surfaces do not expose those credentials.
- Sanitized OpenAI-compatible streaming status error bodies, reflected authorization/header/JSON credential fields, and reliable-provider stream forwarding logs so provider errors cannot echo supplied API keys.
- Encrypted nested model and embedding route `api_key` values on config save/load, masked them in object-array display and whole-config API responses, restored masked PATCH values from the active config, and fixed first-hour policy action counters so rate limits count fresh actions correctly.
- Changed Unix installer onboarding detection to require both a configured model provider and a runnable agent route, preventing provider-only configs from skipping Quickstart, and updated Linux/macOS setup docs to the DX Agents repo, binary, Quickstart, and service command surface.
- Kept Docker release gateway pairing required, switched Docker/Compose/Kubernetes env examples to DX-first names, and made Compose host-port publishing loopback-only by default.
- Updated Quick Start, Windows setup, CLI help, and English help catalog examples away from upstream ZeroClaw install/onboard commands.
- Verified a transient Groq quickstart and `llama-3.3-70b-versatile` agent message returned `dx-agents-provider-ok`, with no raw supplied keys found in the temp config or repo scan.
- Preserved credential surface classification when config fields are exported through schema/wire metadata, keeping secret fields redacted and typed for UI consumers.
- Awaited Windows native process-output tests correctly so async runtime checks compile on Windows.
- Fixed `dx-agents config init` so disabled TLS, local Whisper, and OpenVPN tunnel sections reload after dirty-save omits default empty string fields.
- Made copied DX provider catalog loading observable and fail-soft: callers now receive load diagnostics, malformed copied catalogs log a warning and fall back to native providers, and coverage tracks the current OpenCode Zen free-model rows.
- Updated desktop bridge/provider smoke commands and operator runbooks to use the current `dx-agents agent -a dx ... --message` surface instead of removed `self-test --provider` flags.
- Removed the unreachable-code warning from the Tauri desktop build script by splitting Windows manifest setup from the non-Windows build path.
- Hardened release governance by forcing Docker publication to wait for the GitHub release, allowlisting website redeploys, passing announcement secrets explicitly, making package-manager workflows dry-run by default, and making Homebrew formula work audit-only until a DX-owned formula path exists.
- Rebranded Kubernetes/OpenShift samples and checked-in AUR/Scoop package templates to the DX Agents public binary/image contract without storing provider API keys in sample ConfigMaps.
- Pointed self-update checks at the DX Agents release repository by default, switched update requests to the `dx-agents` user agent, preferred `dx-agents-*` release artifacts while keeping legacy archive compatibility, and added Windows zip extraction for update assets.
- Rebranded stable release artifacts, CI Docker release images, Docker Compose, and the Kubernetes sample to use the `dx-agents` binary/image contract, and gated package-manager dry-runs, website redeploys, and release announcements behind explicit DX release variables.
- Rebranded the shell installer, Windows installer, and Raspberry Pi deploy service to install and verify `dx-agents` binaries from the DX Agents release repository while preserving the current `.zeroclaw` runtime config path for compatibility.
- Kept the update asset-selection helper warning-free in production builds after the DX asset migration.
- Re-synced the web API client with the gateway's current config, quickstart, skills, browse, workspace, cron, cost, memory, session, and TUI routes so the dashboard agent surfaces typecheck again.
- Required cron edit requests from the dashboard to include the owning agent alias, matching the gateway's policy-validation contract.
- Redacted secret-bearing URL query parameters from provider error text, covering Gemini API-key URLs before errors reach UI or log surfaces.
- Removed WebSocket bearer-token fallback from URL query parameters for chat, ACP, and node endpoints; browser clients now use the bearer subprotocol without appending auth tokens to the URL.
- Bound the web chat WebSocket client to the `/agent/:alias` route, restoring per-agent session scoping and supervised approval responses.
- Honored `GEMINI_API_KEY` and `GOOGLE_API_KEY` environment variables in the Gemini provider before falling back to OAuth, matching the documented direct API-key path.
- Treated Google/Gemini quota-exhausted 429 responses as non-retryable in the reliable provider path, avoiding wasted retry loops when an account has no available quota.
- Verified the live auth boundary with newly supplied transient credentials: Gemini reached the API and failed closed on quota exhaustion, while Groq quickstart plus `dx-agents agent -a dxllama3370bversatile -p groq --model llama-3.3-70b-versatile --message ...` returned the expected smoke token without writing raw secrets to temp files.
- Fixed Quickstart flag prefill to submit provider API keys under the runtime's canonical `api_key` field.
- Added Groq and Google API-key prefixes to provider error sanitization so rejected live-provider responses cannot echo those credentials.
- Restored Zed-safe provider/model discovery JSON for `dx-agents providers list --json` and `dx-agents models list --json`.
- Fixed Bash, Fish, and Zsh dynamic config completion wrappers to call `dx-agents config complete` instead of the upstream `zeroclaw` binary.

### Added

- Added DX onboarding branding to the desktop onboarding page with a static rainbow DX wordmark, lightweight ASCII train rail motion, and guarded train-exit completion animation.
- Renamed the active workspace folder to `G:\Dx\agent` while preserving the internal `dx-agents` Cargo package, binary, library, and receipt command contracts for launch compatibility.
- Moved the root `Cli` and `Commands` clap definitions into `src/cli_commands.rs`, keeping top-level command shape separate from execution and parity dispatch.
- Moved gateway start/restart/status/pair-code command execution into `src/cli_gateway_handler.rs`, keeping gateway dispatch and startup helpers out of the main CLI orchestrator.
- Moved auth login, paste-redirect, paste-token, refresh, logout, profile selection, list, and status execution into `src/cli_auth_handler.rs`, keeping provider auth flows out of the main CLI orchestrator.
- Moved desktop companion install guidance, binary discovery, and launch execution into `src/cli_desktop_handler.rs`, keeping desktop-app concerns out of the main CLI orchestrator.
- Moved provider list, provider catalog regeneration, and the human-readable supported-provider table into `src/cli_provider_handler.rs`, keeping provider command execution out of the main CLI orchestrator.
- Moved doctor models, traces, and default diagnostic execution into `src/cli_doctor_handler.rs`, keeping diagnostic command execution out of the main CLI orchestrator.
- Moved tool safety-drill, execution receipts, receipt history, and receipt alert execution into `src/cli_tool_handler.rs`, keeping tool safety/receipt commands out of the main CLI orchestrator.
- Moved self-test quick/full diagnostics, selected-check validation, provider smoke tests, result printing, and failure exit semantics into `src/cli_self_test_handler.rs`, keeping diagnostic self-test execution out of the main CLI orchestrator.
- Moved service lifecycle init-system parsing and install/start/stop/restart/status/log dispatch into `src/cli_service_handler.rs`, keeping OS service command execution out of the main CLI orchestrator.
- Moved channel start/doctor async-runtime routing and normal channel subcommand dispatch into `src/cli_channel_handler.rs`, keeping channel command execution out of the main CLI orchestrator.
- Moved update check/install branching and user-facing update status printing into `src/cli_update_handler.rs`, keeping self-update command execution out of the main CLI orchestrator.
- Moved dashboard compatibility report, retained history, and alert command dispatch into `src/cli_dashboard_compatibility_handler.rs`, keeping dashboard readiness command execution out of the main CLI orchestrator.
- Moved memory list/get/stats/learning/receipt/clear/reindex execution routing into `src/cli_memory_handler.rs`, keeping memory command execution out of the main CLI orchestrator.
- Moved continuation status, record, and journal-path execution routing into `src/cli_continuation_handler.rs`, keeping continuation command execution out of the main CLI orchestrator.
- Moved model refresh/list/set/health/failover/status execution routing into `src/cli_model_handler.rs`, keeping model command execution out of the main CLI orchestrator.
- Moved cron list/preview/history/delivery/add/update/remove/pause/resume execution routing into `src/cli_cron_handler.rs`, keeping scheduled-task command execution out of the main CLI orchestrator.
- Moved hardware discovery/introspection/info and peripheral list/add/flash/setup execution routing into `src/cli_device_handler.rs`, keeping device command execution out of the main CLI orchestrator.
- Moved SOP, migration, and session execution routing into `src/cli_workflow_handler.rs`, keeping operator workflow command execution out of the main CLI orchestrator.
- Moved integration info and skill list/audit/install/remove/test execution routing into `src/cli_extension_handler.rs`, keeping extension command execution out of the main CLI orchestrator.
- Moved compact dashboard routing, exit-code health probing, full human status rendering, cost usage display, channel summary, and peripheral summary into `src/cli_status_handler.rs`, keeping status command execution out of the main CLI orchestrator.
- Moved the large parity subcommand dispatch table into `src/cli_parity_handler.rs`, keeping parity execution routing out of the main CLI orchestrator while preserving the existing command contract.
- Moved shared Zed bridge `agents` command execution into `src/cli_agents_handler.rs`, removing duplicate kernel-only and full-runtime dispatch while preserving the JSON bridge command contract.
- Moved config schema/list/get/set/init/migrate/patch/docs/complete execution into `src/cli_config_handler.rs`, keeping config mutation and JSON Patch behavior out of the main CLI orchestrator.
- Moved daemon startup checks, channel/peripheral registration, shared canvas-store setup, and reload-loop subsystem wiring into `src/cli_daemon_handler.rs`, keeping long-running runtime orchestration out of the main CLI dispatcher.
- Moved full-runtime `agent` and `acp` command execution into `src/cli_agent_handler.rs`, keeping CLI channel registration, final temperature resolution, and ACP server setup out of the main CLI dispatcher.
- Moved optional `plugins-wasm` list/install/remove/info execution into `src/cli_plugin_handler.rs`, keeping plugin host operations out of the main CLI dispatcher.
- Made the root `plugins-wasm` feature explicitly enable `agent-runtime`, so plugin-enabled binary builds compile against the runtime command surface they expose.
- Moved onboarding backup handling, quick/CLI/TUI selection, deprecated flag warnings, and config save into `src/cli_onboard_handler.rs`, keeping early onboarding execution out of the main CLI bootstrap.
- Moved kernel-only agent fallback execution into `src/cli_kernel_handler.rs`, keeping no-runtime provider fallback mutation, simple chat execution, stdin interaction, and shared agents dispatch out of the main CLI bootstrap.
- Tightened no-runtime feature boundaries by gating runtime-only command handlers and returning an explicit unavailable cron preflight gate when `agent-runtime` is disabled.
- Moved Zed bridge `agents` CLI command definitions into `src/cli_agents_commands.rs`, keeping contract/status/social/automation/run/receipt clap wiring separate from bridge execution.
- Moved the large parity CLI command enum into `src/cli_parity_commands.rs`, keeping parity schema ownership out of the root binary bootstrap.
- Moved root CLI parser tests into `src/cli_parser_tests.rs`, keeping command-surface coverage out of the binary bootstrap file.
- Moved parity CLI parser tests into `src/cli_parity_parser_tests.rs`, separating parity command coverage from core CLI parser coverage.
- Split runtime receipt parity parser tests into `src/cli_parity_runtime_receipt_parser_tests.rs`, reducing the largest parser-test module without changing CLI behavior.
- Split release-gate parity parser tests into `src/cli_parity_release_gate_parser_tests.rs`, keeping release-gate command-surface coverage separate from general parity parser coverage.
- Split legacy-alias parity parser tests into `src/cli_parity_legacy_alias_parser_tests.rs`, keeping alias-removal command coverage out of the shared parity parser-test module.
- Split runtime-budget parity parser tests into `src/cli_parity_runtime_budget_parser_tests.rs`, leaving the shared parity parser-test module focused on core parity commands.
- Replaced the parity command handler's crate-wide wildcard import with explicit parity/readiness module imports.
- Moved provider list/catalog CLI command definitions into `src/cli_provider_commands.rs`, keeping provider/model catalog clap wiring separate from command execution.
- Moved optional `plugins-wasm` CLI command definitions into `src/cli_plugin_commands.rs`, preserving the feature gate while keeping plugin clap wiring separate from the main dispatcher.
- Moved the deprecated `props` compatibility parser into `src/cli_deprecated_props_commands.rs`, keeping legacy clap scaffolding separate from the main dispatcher.
- Moved continuation CLI command definitions into `src/cli_continuation_commands.rs`, keeping status, record, and journal-path clap wiring separate from continuation command execution.
- Moved memory CLI command definitions into `src/cli_memory_commands.rs`, keeping memory list/get/stats/learning/receipt/clear/reindex clap wiring separate from memory command execution.
- Moved doctor CLI command definitions into `src/cli_doctor_commands.rs`, keeping model-catalog and trace diagnostic clap wiring separate from doctor command execution.
- Moved model CLI command definitions and provider health/failover value enums into `src/cli_model_commands.rs`, keeping model/provider clap wiring separate from command execution.
- Moved auth CLI command definitions into `src/cli_auth_commands.rs`, keeping login/paste-token/refresh/logout/use/list/status clap wiring separate from auth command execution.
- Moved config CLI command definitions into `src/cli_config_commands.rs`, keeping schema/list/get/set/init/migrate/patch/docs clap wiring separate from the command dispatcher.
- Moved onboarding section parsing, legacy `--*-only` target resolution, and resolver unit tests into `src/cli_onboard.rs`, keeping onboarding routing separate from the main command dispatcher.
- Moved auth input reading, OpenAI Codex auth import parsing, OpenAI account-id extraction, and token-expiry formatting into `src/cli_auth_helpers.rs`, keeping auth support routines separate from the command dispatcher.
- Moved pending OAuth login state persistence into `src/cli_auth_pending.rs`, isolating encrypted verifier storage, load/save/clear behavior, and owner-only permission handling from the main CLI dispatcher.
- Moved gateway admin URL construction, pair-code fetch, health fetch, and health printing helpers into `src/cli_gateway.rs`, keeping network-facing gateway helper logic out of the main dispatcher.
- Moved CLI help localization decoration into `src/cli_i18n.rs`, keeping recursive clap command translation separate from the main dispatcher.
- Moved emergency-stop CLI parsing and engage/resume/status helpers into `src/cli_estop.rs`, keeping the safety-sensitive estop flow isolated from the main command dispatcher.
- Moved shell completion parsing and script generation into `src/cli_completion.rs`, preserving dynamic config-path completion while keeping completion-specific clap code out of `src/main.rs`.
- Moved top-level CLI helper routines from `src/main.rs` into `src/cli_helpers.rs`, keeping config comment writing, JSON set-prop coercion, temperature parsing, no-command help, and embedded terminal canary output out of the command dispatcher.
- Split the Zed bridge contract model into focused type, handoff, and provider-catalog modules so `src/zed_agent_bridge_contract.rs` keeps receipt/surface contract assembly instead of owning every DTO and metadata builder.
- Moved the Zed bridge surface map into `src/zed_agent_bridge_contract_surfaces.rs`, reducing the contract assembler below 900 lines while preserving the exported contract shape.
- Extracted Zed run-receipt types, task-state helpers, active-run counting, and run-receipt writing into `src/zed_agent_run_receipts.rs`, keeping `src/zed_agent_bridge.rs` focused on bridge command/report orchestration while preserving the existing public re-exports.
- Extracted Zed bridge report DTOs, schema constants, stable command maps, public command translation, and common redaction metadata into `src/zed_agent_bridge_reports.rs`, further shrinking `src/zed_agent_bridge.rs` without changing its public API.
- Fixed the provider/model catalog compile contract by enabling `rkyv` `size_32` with default features disabled, preserving the intended `rkyv`/`memmap2` fast catalog path for Zed without falling back to broad JSON parsing.
- Fixed the binary-side model list JSON path so `dx-agents models list --json` calls the shared provider-catalog module from the library boundary.
- Validated the Zed bridge source lane with `cargo test -q --test component zed_agent_bridge::` after the contract-audit receipt classification and provider/model catalog fixes.
- Hardened the Zed run-receipt contract for `dx agents run --json`: receipts now include task lifecycle state, provider/model readiness, tool-call counts, social/account status, automation status, duration state, explicit retry/cancel support flags, and safe redacted config metadata while keeping prompts, transcripts, account targets, provider credentials, and tool payloads out of JSON.
- Promoted safe social/auth and automation counters from run receipts into the runtime-owned `dx-agents agents receipts list --json` row contract, so Zed can render background-task account and automation state without opening receipt bodies.
- Added a `needs_auth` alias to connected account summaries while preserving `needs_connection`, giving Zed a launch-native social auth counter without breaking existing receipt readers.
- Hardened receipt task-state handling so unknown non-redacted `task_state` values become unsafe review rows and make the receipt index warn, while active-task counting falls back to queued/running receipt status for concurrent or future-state rows.
- Classified `contract-audit-latest.json` as a first-class runtime receipt-index row so Zed can see bridge contract-audit evidence instead of silently losing it from `dx-agents agents receipts list --json`.
- Mirrored task-payload, transcript, and provider-credential export booleans inside the per-run `safe_config` block so every run receipt carries the same machine-readable no-export posture as the shared bridge redaction contract.
- Extended the common Zed bridge redaction object with explicit `exports_task_payloads`, `exports_transcripts`, and `exports_provider_credentials` booleans, and included those fields in contract-audit secret-exposure checks.
- Hardened the receipt rail summary so `dx-agents agents receipts list --json` exposes redacted `task_state` alongside provider/model readiness, duration, and retry/cancel posture for Zed background-task rows, with scalar redaction now covering bearer headers, authorization labels, provider-key labels, private-token strings, and OAuth token names.
- Made `dx-agents agents receipts list --json` report an absent pre-existing receipt root as `missing_config` with `receipt_root_present=false`, matching the root DX CLI cached inbox missing-folder state before the command writes its own receipt.
- Added first-class `qr_connect_panel` and `automations_entrypoint` entries to the Zed bridge `zed_surfaces` contract so GPUI can bind QR/connect and automation controls to exact JSON receipts and public `dx agents ... --json` commands without inferring them from prose.
- Source-reviewed the public root-command helper fallback so runtime-owned `dx-agents ...` labels and already-public labels both resolve to owned command strings before governed Cargo validation.
- Added public root-command metadata to Zed handoff entries, GPUI surface contracts, and social/automation row actions so the contract can advertise both runtime-owned `dx-agents ...` receipt producers and Zed-executable `dx agents ...` bridge commands at every render/action level.
- Added a `public_commands` map to the Zed bridge contract so GPUI can discover the root `dx agents ... --json` commands directly while the existing `commands` map continues to describe runtime-owned `dx-agents` receipt producers.
- Documented the public root DX CLI contract bridge aliases for Zed: `dx agents contract --json` and `dx agents contract-audit --json`, while keeping the runtime-owned contract reports as the receipt producers.
- Documented the public root DX CLI provider/model bridge aliases for Zed: `dx agents providers list --json`, `dx agents models list --json`, and `dx agents providers catalog regenerate --json`, while keeping the runtime-owned `dx-agents providers ...` / `dx-agents models ...` commands as the receipt producers.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-implementation-readiness-manifest --json` as a metadata-only Batch 170 control manifest for future live-handoff ownership, command allowlists, rollback owners, audit evidence, live-gate preconditions, disabled-by-default policy, and closed redaction without enabling release automation, live handoff execution, archive writes, or change writes.
- Documented the Zed GPUI row-action handoff completion: Zed now consumes structured social and automation `actions[]` descriptors while keeping execution on fixed safe bridge command vectors.
- Added structured Zed row action descriptors for social accounts and automations, including enablement, receipt filenames, refresh commands, user-action flags, and explicit `secrets_exposed=false` guarantees.
- Added row-level social connect/disconnect/refresh commands and automation run/refresh commands to Zed bridge receipts so GPUI can render real account and automation actions without reconstructing CLI strings or exposing secrets.
- Added `dx-agents agents contract-audit --json` and `contract-audit-latest.json` so Zed/Friday can verify GPUI surface coverage, declared receipt schemas, stable command discoverability, unique receipt metadata, binary-cache catalog posture, and secret-exposure flags without running heavy builds or importing `dx-agents` crates into Zed.
- Added a machine-readable `zed_surfaces` GPUI surface map to the Zed bridge contract so Zed can bind status, social connection cards/actions, automation rows, background task receipts, providers, models, and catalog recovery to exact commands and receipt files without inferring UI surfaces from prose.
- Added an explicit provider/model catalog safety story to the Zed bridge contract, including normal binary-cache/receipt use, JSON import-only policy, stale-cache fallback, safety guards, render fields, and redacted provider-secret fields.
- Added machine-readable `item_render_fields` to the Zed bridge contract so GPUI can render social account, automation, receipt, provider, and model rows without inferring nested JSON shapes from prose.
- Added `dx-agents agents snapshot --json` and `snapshot-latest.json` as a one-call Zed first-paint bridge over agent status, social account readiness, automations, and receipt rows without exporting task payloads, transcripts, automation bodies, account targets, or secrets.
- Added `dx-agents agents receipts list --json` and the `receipts-list-latest.json` receipt so Zed can render a safe background task and receipt rail from known bridge receipts without reading task payloads, transcripts, automation bodies, or secrets.
- Added receipt-rail scalar metadata redaction and stricter dynamic run-receipt filename matching so poisoned receipt rows cannot echo token-like `command`, `task_id`, `last_error`, or `next_action` values.
- Added `dx-agents agents contract --json` and the `contract-latest.json` receipt so Zed GPUI can discover DX Agents bridge commands, receipt schemas, render fields, provider-catalog fallback guidance, and redaction rules without importing `dx-agents` crates.
- Added common render counters and `last_error` to `contract-latest.json` so Zed can render contract health without a separate status probe.
- Added a `zed_handoff` block to the DX Agents bridge contract with exact Zed startup commands, explicit user-action commands, provider/model catalog commands, latest receipt paths, the dynamic `agent-run-<timestamp>-<pid>.json` receipt pattern, safe render fields, and never-render secret/payload fields.
- Added platform-specific social connect/disconnect entries to the shared bridge `commands` object so Zed can discover every callable social action from status and contract JSON.
- Added sanitized platform-specific `command` values to social connect/disconnect action receipts so Zed can audit the exact action it invoked without exposing account targets or credentials.
- Added unmatched social platform redaction so accidental token-like `--platform` values are not echoed in receipt commands or recovery hints.
- Added machine-readable Zed bridge redaction fields for requested and unmatched social platform values so GPUI treats accidental token-like input as never-renderable.
- Added Zed bridge JSON contracts for `dx-agents agents social connect --json`, `dx-agents agents social connect --platform <platform> --json`, `dx-agents agents social disconnect --json`, `dx-agents agents social disconnect --platform <platform> --json`, `dx-agents providers list --json`, `dx-agents providers catalog regenerate --json`, and `dx-agents models list --json`, with redacted receipts under `G:\Dx\.dx\receipts\agents`.
- Added a generated provider/model catalog cache path using `rkyv` plus `memmap2`, including schema version, generated timestamp, source hash, provider/model counts, stale-cache fallback metadata, and a safe regeneration command for Zed.
- Added provider/model readiness summaries with local, remote, OpenAI-compatible, OpenRouter-compatible, LiteLLM-compatible, and models.dev-shaped metadata labels without exporting provider keys, base URLs, environment values, prompt payloads, cookies, or tokens.
- Documented the DX CLI release-gate and import-summary action-map bridge coverage so Zed can consume recovery-control readiness through stable CLI JSON without importing `dx-agents` crates.
- Added CLI-first Zed bridge commands for `dx-agents agents status --json`, `dx-agents agents social list --json`, `dx-agents agents automate list --json`, and `dx-agents agents run --json`, with receipts under `G:\Dx\.dx\receipts\agents`.
- Added secret-safe Zed bridge report types for status, social account summary, automation summary, and metadata-only background task receipts without exporting provider keys, channel tokens, account targets, automation bodies, tool payloads, or task payloads.
- Added `DX.md` launch worker guidance for the DX Agents to Zed GPUI bridge.
- Added May 22 bridge TODO items for JSON status, social accounts, automations, and receipts.
- Added the Batch 169 future live-handoff gate design runbook with explicit non-execution, no archive-write, no change-write, approval-blocker, rollback-blocker, retained-history, alert-review, inventory/verifier, strict CI review, and future implementation-batch requirements.
- Added parity inventory and verifier coverage for the future live-handoff gate design packet, retained gate-design history, and gate-design history alerts, including schema checks, section/action/surface/alert ids, required inventory registration, bounded source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history-alerts --json` for metadata-only future live-handoff gate design history alerts covering empty/single/stale evidence, design regressions, prep/source regressions, inventory/verifier regressions, strict-command drift, approval-blocker drift, rollback-readiness drift, unsafe execution/archive-write drift, operator-action drift, section regressions, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history --archive --json` for retained metadata-only future live-handoff gate design snapshots covering design-packet state, prep/source states, inventory/verifier posture, approval blockers, rollback blockers, strict CI guidance, non-execution/archive-write posture, operator action safety, section stability, command guidance, freshness, and payload-policy drift.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet --json` as a metadata-only future live-handoff gate design packet over the Batch 168 prep packet, with explicit operator approval blockers, rollback blockers, strict CI signoff guidance, disabled release/live execution, disabled archive writes, disabled change writes, non-executing operator actions, runbook linkage, and closed redaction.
- Added the Batch 168 future live-handoff prep runbook covering release-boundary quorum refresh, retained soak evidence, quorum-history alert review, prep-packet review, inventory/verifier checks, explicit strict CI opt-in, disabled live execution/archive-write boundaries, and payload/secret export prohibitions; Batch 169 now opens future live-handoff gate design and safety quorum work.
- Opened Batch 169 for a non-executing future live-handoff gate design packet, retained gate-design history, gate-design history alerts, parity inventory/verifier coverage, and an operator runbook that keeps live execution disabled until an explicit implementation batch exists.
- Added parity inventory and verifier coverage for retained release-boundary quorum history, release-boundary quorum history alerts, and the future live-handoff prep packet, including schema checks, retained-history surface ids, alert ids, prep-packet section/action ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-prep-packet --json` as a metadata-only future live-handoff prep packet consuming release-boundary quorum evidence, retained quorum history, quorum-history alerts, parity inventory/verifier posture, runbook coverage, strict CI signoff guidance, explicit future live-handoff blockers, disabled execution/archive-write posture, and closed redaction without enabling live handoff execution.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history-alerts --json` for metadata-only release-boundary quorum history alerts covering empty/single/stale history, quorum regressions, final promotion packet source regressions, retained packet history regressions, packet-history alert regressions, inventory/verifier regressions, strict-command drift, future live-handoff blocker drift, signal/section drift, unsafe execution/write posture, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history --archive --json` for retained metadata-only release-boundary quorum snapshots covering quorum state, final promotion packet/history/alert source states, inventory/verifier posture, strict-command availability, future live-handoff blocker stability, disabled execution/archive-write safety, signal/section stability, freshness, command guidance, and payload-policy drift before quorum-history alerts and future live-handoff prep.
- Added the Batch 167 release-boundary runbook covering retained final promotion packet evidence, quorum review, explicit strict CI opt-in, future live-handoff blocker posture, non-executing live handoff boundaries, and payload/secret export prohibitions; Batch 168 now opens release-boundary soak and future live-handoff prep.
- Added parity inventory and verifier coverage for retained final promotion operator packet history, packet-history alerts, and release-boundary evidence quorum, including schema checks, retained-history surface ids, alert ids, quorum signal/section ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum --json` for metadata-only release-boundary quorum review over the final promotion operator packet, retained packet history, packet-history alerts, parity inventory/verifier posture, runbook coverage, explicit strict promotion signoff CI command availability, disabled execution/archive-write posture, future live-handoff blocker visibility, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history-alerts --json` for metadata-only final promotion operator packet history alerts covering empty/single/stale evidence, packet-state regressions, promotion signoff source regressions, inventory/verifier regressions, section/action/runbook drift, strict-command drift, future live-handoff blocker drift, unsafe execution/archive-write drift, command-guidance drift, payload-policy regressions, and stable-history posture before release-boundary quorum.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history --archive --json` for retained metadata-only final promotion operator packet snapshots covering packet state, promotion signoff-gate source state, retained signoff evidence state, alert source state, inventory/verifier posture, section/action/runbook stability, strict command guidance, future live-handoff blocker posture, execution/archive-write safety, freshness, and payload-policy drift before release-boundary alerts and quorum.
- Added the Batch 166 final promotion operator packet runbook covering retained strict promotion signoff evidence, final packet review, explicit strict CI opt-in, future live handoff gate boundaries, disabled execution/archive writes, and payload/secret export prohibitions; Batch 167 now opens retained final promotion-packet evidence and release-boundary readiness work.
- Added parity inventory and verifier coverage for retained readiness handoff promotion signoff-gate history, signoff-gate history alerts, and the final promotion operator packet, including schema checks, retained-history surface ids, alert ids, final packet section/action ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet --json` as the final metadata-only readiness handoff promotion operator packet combining the strict promotion signoff gate, retained signoff-gate history, signoff-gate alerts, parity inventory/verifier posture, runbook coverage, explicit strict CI opt-in guidance, future live-handoff blockers, disabled live execution/archive writes, recommendation-only exit posture, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history-alerts --json` for metadata-only strict promotion signoff-gate history alerts covering empty/single/stale evidence, gate-state regressions, promotion source regressions, exit-code regressions, strict-mode drift, unsafe execution/archive-write drift, future live-handoff blocker drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history --archive --json` for retained metadata-only strict readiness handoff promotion signoff-gate snapshots covering gate state, promotion-pack source state, promotion-pack history alert state/counts, recommended/effective exit codes, strict-mode posture, live-execution/archive-write safety, future handoff blocker drift, gate stability, command guidance, freshness, and closed payload-policy drift without exporting protected values.
- Added the Batch 165 operator promotion signoff runbook covering the safe promotion pack, retained evidence archive, history alerts, strict promotion signoff gate, explicit `--fail-on-non-clear` CI opt-in, recovery loop, future live-handoff blockers, and payload/secret export prohibitions; Batch 166 now opens retained strict promotion signoff evidence and final operator-promotion packet work.
- Added parity inventory and verifier coverage for retained readiness handoff promotion-pack history, promotion-pack history alerts, and the strict promotion signoff gate, including schema checks, retained-history surface ids, alert ids, signoff gate ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --json` with explicit `--fail-on-non-clear` strict mode, default non-failing exit behavior, promotion-pack and promotion-pack-history alert gating, disabled live execution/archive-write posture, future live-handoff blocker enforcement, and metadata-only redaction coverage.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history-alerts --json` for metadata-only readiness handoff promotion-pack history alerts covering empty/single/stale evidence, promotion-pack regressions, dry-run source regressions, inventory/verifier regressions, future live-handoff blocker drift, unsafe execution/write posture, action/section drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history --archive --json` for retained metadata-only readiness handoff promotion-pack snapshots covering promotion-pack state, dry-run/history/alert source states, inventory/verifier posture, future live-handoff blockers, disabled execution/write posture, action and section stability, command guidance, freshness, and closed payload-policy drift without exporting protected values.
- Completed Batch 164 readiness handoff promotion pack and operator dry-run work, including the promotion dry-run, retained promotion dry-run history, promotion dry-run history alerts, promotion pack, focused promotion verification record, parity inventory/verifier coverage, focused source-contract tests, docs, no-secret scanning, and opened Batch 165 for retained promotion-pack evidence plus strict signoff prep.
- Added parity inventory and verifier coverage for the readiness handoff promotion dry-run, retained promotion dry-run history, promotion dry-run history alerts, promotion pack, and focused promotion verification record, including schema checks, section/action/future-blocker ids, retained-history surface ids, alert ids, verification-record check/command ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-verification-record --json` as a focused metadata-only readiness handoff promotion verification record covering promotion-pack behavior, retained promotion dry-run history, promotion dry-run history alerts, required inventory surfaces, declared verifier check ids, CLI parser coverage, non-execution guarantees, redaction posture, and lightweight operator commands before final parity inventory/verifier integration.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack --json` for a metadata-only readiness handoff operator promotion pack over the current promotion dry-run, retained promotion dry-run history, promotion history alerts, parity inventory/verifier posture, runbook coverage, strict handoff signoff-gate guidance, explicit future live-handoff blockers, closed redaction, and safe non-executing operator actions while keeping release automation execution and writes disabled.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run-history --archive --json` and `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run-history-alerts --json` for retained metadata-only readiness handoff promotion dry-run snapshots and alerts covering promotion dry-run state regressions, quorum/final-packet source regressions, inventory/verifier regressions, strict-command drift, future live-handoff blocker drift, unsafe execution/write posture, action/section drift, command-guidance drift, freshness, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run --json` for a metadata-only readiness handoff promotion dry-run over the final readiness handoff evidence quorum, retained final packet history, packet-history alerts, inventory/verifier posture, runbooks, strict handoff signoff-gate command guidance, disabled execution/write posture, and closed redaction while keeping `effective_exit_code=0`.
- Completed Batch 163 readiness handoff final packet retention and evidence quorum work, including retained final readiness handoff packet history, packet-history alerts, readiness handoff evidence quorum, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 164 for a non-executing readiness handoff promotion pack.
- Added parity inventory and verifier coverage for retained final readiness operator handoff packet history, final packet history alerts, and readiness handoff evidence quorum, including schema checks, retained-history surface ids, alert ids, evidence-quorum signal/section ids, required inventory registration, source-contract coverage, collection coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-evidence-quorum --json` for a metadata-only readiness handoff evidence quorum requiring a clear final handoff packet, stable retained final packet history, clear packet-history alerts, inventory/verifier readiness, runbook coverage, explicit strict handoff signoff-gate command availability, disabled execution/write posture, and closed redaction before future readiness handoff automation promotion.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet-history-alerts --json` for metadata-only final readiness operator handoff packet history alerts covering empty/single/stale evidence, packet regressions, strict signoff-gate source regressions, inventory/verifier regressions, exit-code regressions, section/action/runbook drift, command-guidance drift, unsafe execution/write posture, payload-policy regressions, and stable-history posture without exporting protected values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet-history --archive --json` for retained metadata-only final readiness operator handoff packet snapshots covering packet state, strict signoff-gate source states, inventory/verifier posture, exit-code posture, section/action/runbook stability, strict-command guidance, non-execution/write posture, freshness, and payload-policy drift without exporting protected values.
- Added parity inventory and verifier coverage for readiness operator handoff signoff-gate history, gate-history alerts, and the final readiness operator handoff packet, including schema checks, retained-history surface ids, alert ids, final packet section/action ids, required inventory registration, source-contract coverage, collection coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet --json` as the final metadata-only readiness operator handoff packet over the strict signoff gate, retained strict gate history, gate-history alerts, parity inventory/verifier posture, runbooks, safe operator actions, explicit strict opt-in guidance, disabled live execution, disabled writes, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate-history-alerts --json` for metadata-only strict readiness operator handoff signoff-gate history alerts covering empty/single/stale evidence, gate-state regressions, recommended/effective exit-code regressions, strict-mode drift, live-execution/archive-write regressions, command-guidance drift, payload-policy regressions, and stable-history posture without exporting protected values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate-history --archive --json` for retained metadata-only strict readiness operator handoff signoff-gate snapshots covering gate state, signoff/source states, recommended/effective exit-code posture, explicit strict-mode posture, live-execution/archive-write posture, gate stability, command guidance, freshness, and payload-policy drift without exporting protected values.
- Added parity inventory and verifier coverage for readiness operator handoff signoff, retained signoff history, signoff-history alerts, and the strict signoff gate, including schema checks, signoff item/action ids, retained-history surface ids, alert ids, strict gate ids, required inventory registration, source-contract coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate --json` with explicit `--fail-on-non-clear` strict mode for a metadata-only readiness operator handoff signoff gate over the signoff packet and retained signoff-history alerts; the gate recommends exit codes, keeps `effective_exit_code=0` by default, disables release automation execution and archive writes, and keeps payload export closed.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-history --archive --json` and `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-history-alerts --json` for retained metadata-only readiness operator handoff signoff snapshots and alerts covering signoff state, focused verification record state, packet/history/alert source states, inventory/verifier state, non-execution posture, operator-action stability, signoff-item stability, command-guidance drift, freshness, and payload-policy regressions.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff --json` as a metadata-only readiness operator handoff signoff packet requiring a clear handoff packet, stable retained packet history, clear packet-history alerts, focused verification-record coverage, inventory/verifier readiness, parity runbooks, safe operator actions, disabled release automation execution, disabled writes, zero effective exit code, and closed redaction.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation readiness handoff dry-run history, handoff-history alerts, readiness operator handoff packet, retained operator handoff packet history, packet-history alerts, and the focused handoff verification record, including schema checks, retained-history surface ids, alert ids, packet section/action/future-blocker ids, verification-record check/command ids, source-contract coverage, required inventory registration, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-verification-record --json` as a focused metadata-only verification record covering readiness operator handoff packet behavior, retained packet evidence, packet-history alerts, expected inventory surfaces, declared verifier check ids, CLI parser coverage, non-execution guarantees, and payload-free redaction before parity inventory/verifier integration.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet-history --archive --json` and `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet-history-alerts --json` for retained metadata-only readiness operator handoff packet snapshots and alerts covering packet state, readiness dry-run source states, inventory/verifier posture, strict-command posture, non-execution policy, dry-run snapshot/alert counts, explicit future live-handoff blocker stability, action/runbook stability, section stability, command guidance, payload-policy drift, freshness, and stable-history posture before the focused verification record.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet --json` for a metadata-only readiness automation operator handoff packet over the current readiness handoff dry-run, retained handoff dry-run history, handoff history alerts, parity inventory/verifier posture, runbooks, safe operator actions, explicit future live-handoff blockers, disabled execution/write posture, strict dry-run readiness gate command guidance, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run-history-alerts --json` for metadata-only readiness handoff dry-run history alerts covering empty/single/stale evidence, handoff regressions, readiness source-state regressions, inventory/verifier regressions, strict-command drift, explicit future live-handoff blocker drift, unsafe execution/write drift, section/action drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run-history --archive --json` for retained metadata-only readiness handoff dry-run snapshots covering handoff state, readiness evidence quorum/history/alert source states, inventory/verifier posture, strict-command guidance, explicit future live-handoff blocker stability, disabled execution/write posture, section/action stability, freshness, and payload-policy drift without exporting protected values.
- Completed Batch 159 runtime budget CI promotion release automation readiness quorum retention and handoff dry-run work, including retained readiness quorum history, quorum-history alerts, the non-executing readiness handoff dry-run packet, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 160 for retained readiness handoff evidence plus an operator handoff packet.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation readiness evidence quorum history, quorum-history alerts, and readiness handoff dry-run, including schema checks, required retained-history surface ids, alert ids, handoff section/action/future-blocker ids, inventory surface registration, source-contract tests, collection coverage, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run --json` for a metadata-only readiness handoff dry-run packet over readiness evidence quorum, retained quorum history, quorum alerts, parity inventory/verifier posture, runbooks, explicit future live-handoff blockers, disabled release automation execution/write posture, strict dry-run readiness gate guidance, safe operator actions, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum-history-alerts --json` for metadata-only readiness evidence quorum history alerts covering empty/single/stale evidence, quorum regressions, readiness packet/history/alert regressions, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, signal/section drift, command-guidance drift, payload-policy regressions, and stable-history posture before the non-executing handoff dry-run packet.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum-history --archive --json` for retained metadata-only readiness evidence quorum snapshots covering quorum state, readiness packet/history/alert source states, inventory/verifier posture, strict-command guidance, disabled execution/write posture, signal/section stability, freshness, and payload-policy drift without exporting protected values.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation readiness operator packet history, packet-history alerts, and readiness evidence quorum, including schema checks, required retained-history surface ids, alert ids, quorum signal/section ids, source-contract tests, inventory collection coverage, and closed runtime-budget redaction checks.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum --json` for a metadata-only readiness evidence quorum requiring a clear readiness operator packet, stable retained packet history, clear packet-history alerts, inventory/verifier posture, runbook coverage, disabled release automation execution/writes, explicit strict dry-run readiness gate guidance, and closed redaction before future handoff automation.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet-history-alerts --json` for metadata-only readiness operator packet history alerts covering empty/single/stale evidence, packet regressions, strict readiness gate source regressions, inventory/verifier regressions, exit-code regressions, section/action/runbook drift, command-guidance drift, unsafe execution/write posture, payload-policy regressions, and stable-history posture before readiness evidence quorum.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet-history --archive --json` for retained metadata-only readiness operator packet snapshots covering packet state, strict readiness gate/history/alert source states, inventory/verifier posture, runbook coverage, action/section stability, strict-command guidance, disabled execution/write posture, freshness, and payload-policy drift without exporting protected values.
- Added parity inventory and verifier coverage for strict runtime budget CI promotion release automation dry-run readiness gate history, gate-history alerts, and the readiness operator packet, including retained-history surface ids, alert ids, packet section/action ids, schema checks, bounded source-contract coverage, inventory collection coverage, required verifier surface registration, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet --json` for a metadata-only release automation readiness operator packet combining the strict dry-run readiness gate, retained strict gate history, gate-history alerts, parity inventory/verifier posture, runbooks, safe operator actions, required archive guidance, explicit strict opt-in command guidance, non-failing default exit behavior, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate-history-alerts --json` for metadata-only strict dry-run readiness gate history alerts covering empty/single/stale evidence, gate-state regressions, readiness source-state regressions, recommended/effective exit-code regressions, strict-mode drift, unsafe execution/archive-write drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate-history --archive --json` for retained metadata-only strict dry-run readiness gate snapshots covering gate state, readiness source state, retained alert state, recommended/effective exit-code posture, strict-mode posture, non-execution/write policy, gate-count stability, command guidance, freshness, and payload-policy drift without exporting protected values.
- Completed Batch 156 runtime budget CI promotion release automation dry-run readiness retention and strict-prep work, including retained readiness history, readiness-history alerts, the strict non-failing-by-default readiness gate, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 157 for retained strict readiness gate history plus a readiness operator packet.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation dry-run readiness history, readiness-history alerts, and the strict dry-run readiness gate, including schema checks, retained-history surface ids, alert ids, strict gate ids, non-execution posture, exit-code evidence, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate --json` with explicit `--fail-on-non-clear` strict mode for a metadata-only dry-run readiness gate over the readiness packet and retained readiness-history alerts; the gate recommends exit codes, keeps `effective_exit_code=0` by default, disables release automation execution and archive writes, and keeps runtime-budget redaction closed.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-history-alerts --json` for metadata-only dry-run readiness history alerts covering empty/single/stale evidence, readiness regressions, evidence quorum/history/alert regressions, retained quorum-history instability, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, action/section drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-history --archive --json` for retained metadata-only dry-run readiness snapshots covering readiness state, evidence quorum/history/alert source states, inventory/verifier posture, stable retained quorum history, strict operator signoff command availability, non-execution/write posture, action/section stability, command guidance, freshness, and payload-policy drift without exporting protected values.
- Completed Batch 155 runtime budget CI promotion release automation evidence quorum retention and dry-run prep, including retained quorum history, quorum-history alerts, the non-executing dry-run readiness packet, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 156 for retained readiness history plus strict prep.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation evidence quorum history, quorum-history alerts, and dry-run readiness, including schema checks, retained-history surface ids, alert ids, dry-run section/action ids, source-contract tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness --json` for a metadata-only release automation dry-run readiness packet requiring a clear release automation evidence quorum, stable retained quorum history, clear quorum-history alerts, inventory/verifier posture, runbook coverage, strict operator signoff gate command availability, disabled execution/write posture, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum-history-alerts --json` for metadata-only release automation evidence quorum history alerts covering empty/single/stale evidence, quorum regressions, final packet/history/alert source regressions, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, signal/section drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum-history --archive --json` for retained metadata-only release automation evidence quorum snapshots covering quorum state, final operator packet/history/alert source states, inventory/verifier posture, stable retained packet evidence, strict-command availability, non-execution/write posture, signal/section stability, command guidance, freshness, and payload-policy drift without exporting protected values.
- Added parity inventory and verifier coverage for final runtime budget CI promotion release automation operator packet history, packet-history alerts, and release automation evidence quorum, including schema checks, retained-history surface ids, alert ids, quorum signal/section ids, source-contract coverage tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum --json` for a metadata-only release automation evidence quorum requiring a clear final operator packet, stable retained packet history, clear packet-history alerts, inventory/verifier readiness, runbook coverage, explicit strict operator signoff gate command availability, disabled execution/write posture, and closed redaction before any future release automation execution handoff.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet-history-alerts --json` for metadata-only final operator packet history alerts covering empty/single/stale evidence, packet-state drift, strict gate source drift, inventory/verifier drift, exit-code regressions, section/action/runbook drift, command-guidance drift, top routing drift, unsafe execution/write posture, payload-policy regressions, and stable-history posture before release automation evidence quorum.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet-history --archive --json` for retained metadata-only final release automation operator packet snapshots covering packet state, strict signoff gate source states, inventory/verifier posture, exit-code stability, section/action/runbook stability, strict-command guidance, non-execution policy, freshness, and payload-policy drift without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, provider values, database paths, workspace paths, or secrets.
- Completed Batch 153 runtime budget CI promotion release automation operator signoff gate retention and operator packet work, including retained strict gate history, gate-history alerts, the final metadata-only release automation operator packet, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 154 for retained final packet evidence plus quorum.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation operator signoff gate history, signoff gate history alerts, and the final release automation operator packet, including schema checks, retained-history surface ids, alert ids, packet section/action ids, source-contract coverage tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet --json` as the final metadata-only release automation operator packet combining the strict operator signoff gate, retained gate history, gate alerts, parity inventory/verifier posture, runbooks, safe operator actions, required archive guidance, strict opt-in command guidance, non-failing default exit behavior, and closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate-history-alerts --json` for metadata-only strict operator signoff gate history alerts covering empty/single/stale evidence, gate-state regressions, recommended/effective exit-code regressions, strict-mode drift, unsafe-execution/write drift, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate-history --archive --json` for retained metadata-only strict operator signoff gate snapshots covering gate state, signoff source state, retained alert state, recommended/effective exit-code posture, strict-mode posture, non-execution/write policy, gate-count stability, command guidance, freshness, and payload-policy drift without executing release automation or exporting payload values.
- Completed Batch 152 runtime budget CI promotion release automation operator signoff retention and strict-gate work, including retained operator signoff history, signoff-history alerts, a non-failing-by-default strict operator signoff gate, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 153 for strict gate retention plus a final release automation operator packet.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation operator signoff history, signoff-history alerts, and the strict operator signoff gate, including schema checks, retained-history surface ids, alert ids, strict gate ids, non-execution posture, recommended/effective exit-code evidence, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate --json` with explicit `--fail-on-non-clear` strict mode for a metadata-only runtime budget CI promotion release automation operator signoff gate over the operator signoff packet and retained signoff-history alerts; the gate recommends exit codes, keeps `effective_exit_code=0` by default, disables release automation execution and writes, and keeps runtime-budget redaction closed.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-history-alerts --json` for metadata-only runtime budget CI promotion release automation operator signoff history alerts covering empty/single/stale evidence, signoff regressions, verification-record regressions, packet/history/alert regressions, inventory/verifier regressions, unsafe-execution drift, operator-action drift, signoff-item regressions, command-guidance drift, payload-policy regressions, and stable-history posture without executing release automation or exporting payload values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-history --archive --json` for retained metadata-only runtime budget CI promotion release automation operator signoff snapshots covering signoff state, verification-record state, packet/history/alert states, inventory/verifier state, non-execution posture, action/item stability, command guidance, freshness, and payload-policy drift without executing release automation or exporting payload values.
- Completed Batch 151 runtime budget CI promotion release automation operator handoff and signoff work, including retained dry-run history, dry-run alerts, operator handoff packet, retained packet history, packet alerts, focused verification record, operator signoff, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 152 for retained operator signoff history plus a strict signoff gate.
- Added parity inventory and verifier coverage for runtime budget CI promotion release automation handoff dry-run history, dry-run history alerts, operator handoff packet, retained packet history, packet-history alerts, focused verification record, and operator signoff, including schema checks, required surface/action/alert/check/item coverage, safe-command coverage, and closed runtime-budget redaction checks.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff --json` for a metadata-only runtime budget CI promotion release automation operator signoff packet requiring a clear operator handoff packet, stable retained packet history, clear packet alerts, focused verification-record coverage, inventory/verifier readiness, parity runbooks, non-execution posture, safe operator actions, and closed redaction while keeping release automation execution disabled, writes disabled, and `effective_exit_code=0`.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-verification-record --json` for a focused metadata-only runtime budget CI promotion release automation operator handoff verification record covering dry-run behavior, retained packet evidence, packet-history alerts, inventory coverage, declared verifier coverage, CLI parser coverage, non-execution guarantees, safe lightweight commands, and payload-free redaction without executing release automation or exporting payload values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet-history --archive --json` and `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet-history-alerts --json` for retained metadata-only release automation operator handoff packet snapshots and warning/blocker alerts covering packet state, dry-run source states, inventory/verifier posture, strict-command posture, non-execution policy, operator action stability, section stability, command guidance, payload-policy drift, freshness, and stable-history posture without executing release automation or exporting payload values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet --json` for a metadata-only runtime budget CI promotion release automation operator handoff packet combining the current dry-run, retained dry-run history, dry-run alerts, parity inventory/verifier state, strict command posture, archive guidance, runbook coverage, safe operator actions, non-execution policy, and closed redaction while keeping release automation disabled, writes disabled, and `effective_exit_code=0`.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run-history-alerts --json` for metadata-only runtime budget CI promotion release automation handoff dry-run history alerts covering empty/single/stale evidence, handoff regressions, quorum/source regressions, strict-command drift, unsafe-execution drift, operator-action drift, section regressions, command-guidance drift, payload-policy regressions, and stable-history posture without executing release automation or exporting payload values.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run-history --archive --json` for retained metadata-only runtime budget CI promotion release automation handoff dry-run snapshots covering handoff state, quorum source states, strict-command posture, non-execution policy, operator action stability, section stability, command guidance, payload-policy drift, freshness, and closed redaction without executing release automation or exporting payload values, provider values, database paths, workspace paths, memory bodies, query text, command payloads, tool payloads, transcripts, or secrets.
- Completed Batch 150 runtime budget CI promotion release operator packet and evidence quorum work, including release operator packet coverage, retained packet history, packet alerts, release evidence quorum, quorum history, quorum alerts, non-executing handoff dry-run, inventory/verifier coverage, focused CLI/parser/module tests, docs, no-secret scanning, and opened Batch 151 for release automation operator handoff and signoff work.
- Added parity inventory and verifier coverage for the runtime budget CI promotion release operator packet, retained operator packet history, operator packet history alerts, release evidence quorum, retained quorum history, quorum history alerts, and release automation handoff dry-run, including schema checks, required section/action/surface/signal/alert ids, bounded source-contract tests, constructor-level inventory coverage, and closed redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-operator-packet --json` for a metadata-only runtime budget CI promotion release operator packet combining the release gate, retained release-gate history, release-gate alerts, parity inventory, parity verifier, runbooks, safe operator actions, required archive guidance, strict opt-in command guidance, non-failing default exit behavior, and closed runtime-budget redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-operator-packet-history --archive --json` for retained metadata-only runtime budget CI promotion release operator packet snapshots covering packet state, recommended/effective exit-code posture, release-gate source states, inventory/verifier posture, section/action/runbook stability, command guidance, strict-command drift, top blocker/alert routing, payload-policy drift, freshness, and closed runtime-budget redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-operator-packet-history-alerts --json` for metadata-only runtime budget CI promotion release operator packet history alerts covering empty/single/stale evidence, packet-state regressions, recommended/effective exit-code regressions, release-gate source-state regressions, inventory/verifier regressions, section/action/runbook drift, command-guidance drift, top-alert drift, non-execution regressions, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum --json` for a metadata-only runtime budget CI promotion release evidence quorum requiring a clear release operator packet, repeated clear retained release-gate evidence, clear gate alerts, inventory/verifier readiness, strict command availability, disabled writes, and closed runtime-budget redaction before future handoff automation.
- Added `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum-history --archive --json` and `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum-history-alerts --json` for retained metadata-only runtime budget CI promotion release evidence quorum snapshots and warning/blocker alerts covering quorum/source-state drift, repeated-clear evidence, strict-command availability, signal/section stability, command guidance, write-policy regressions, payload-policy regressions, freshness, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run --json` as a metadata-only runtime budget CI promotion release automation handoff dry-run packet that consumes quorum evidence, retained quorum history, quorum alerts, parity coverage stubs, verifier stubs, and strict-command posture while keeping release automation disabled, writes disabled, and `effective_exit_code=0`.
- Completed Batch 149 runtime budget CI promotion release signoff retention and release-gate promotion, including retained signoff history, signoff-history alerts, non-failing-by-default release gate, retained release-gate history, release-gate history alerts, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 150 for release operator packet and evidence quorum work.
- Added parity inventory and verifier coverage for runtime budget CI promotion release signoff history, signoff history alerts, release gate, retained release-gate history, and release-gate history alerts, including schema checks, required surface/gate/alert coverage, bounded source-contract tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-release-gate-history-alerts --json` for metadata-only runtime budget CI promotion release gate history alerts covering empty/single/stale evidence, release-gate regressions, recommended/effective exit-code regressions, strict-mode drift, live-execution regressions, archive-write regressions, command-guidance drift, payload-policy regressions, and stable-history posture.
- Added `dx-agents parity runtime-budget-ci-promotion-release-gate-history --archive --json` for retained metadata-only runtime budget CI promotion release gate snapshots covering release-gate state, recommended/effective exit-code posture, strict-mode policy, non-failing default policy, live-execution and archive-write guardrails, gate-count drift, command-guidance drift, payload-policy drift, freshness, and closed runtime-budget redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-release-gate --json` with explicit `--fail-on-non-clear` strict mode for a metadata-only runtime budget CI promotion release gate over the release signoff packet and signoff-history alerts; the gate recommends exit codes, keeps `effective_exit_code=0` by default, disables live execution and archive writes, and keeps runtime-budget redaction closed.
- Added `dx-agents parity runtime-budget-ci-promotion-release-signoff-history-alerts --json` for metadata-only runtime budget CI promotion release signoff history alerts covering empty/single/stale evidence, signoff-state regressions, recommended/effective exit-code regressions, non-failing default regressions, source-state regressions, parity coverage regressions, item/action/runbook drift, command-guidance drift, payload-policy regressions, and stable-history posture without exporting payload values, memory content, skill file content, browser storage values, workspace paths, or secrets.
- Added `dx-agents parity runtime-budget-ci-promotion-release-signoff-history --archive --json` for retained metadata-only runtime budget CI promotion release signoff snapshots covering signoff state, recommended/effective exit-code posture, non-failing default policy, source-state posture, item/action/runbook stability, command guidance, payload-policy drift, freshness, file-name-only archive evidence, and closed redaction without exporting payload values, memory content, skill file content, browser storage values, workspace paths, or secrets.
- Completed Batch 148 runtime budget CI promotion operator packet retention and signoff, including retained operator packet history, packet-history alerts, release signoff, inventory/verifier coverage, focused tests, docs, no-secret scanning, and opened Batch 149 for release signoff retention and release-gate promotion.
- Added parity inventory and verifier coverage for runtime budget CI promotion operator packet history, operator packet history alerts, and release signoff, including retained-history surface ids, alert ids, signoff item/action ids, required inventory surfaces, and closed runtime-budget redaction checks.
- Added `dx-agents parity runtime-budget-ci-promotion-release-signoff --json` as a metadata-only runtime budget CI promotion release signoff packet requiring a clear operator packet, retained stable operator packet history, clear packet-history alerts, inventory and verifier coverage, runbook coverage, safe operator actions, non-failing default policy, explicit strict failure command, and closed runtime-budget redaction before release signoff.
- Added `dx-agents parity runtime-budget-ci-promotion-operator-packet-history-alerts --json` for metadata-only runtime budget CI promotion operator packet history alerts covering empty/single/stale evidence, packet-state regressions, recommended exit-code regressions, source-state regressions, section/action/runbook drift, command-guidance drift, top-alert regressions, non-execution regressions, payload-policy regressions, and stable-history posture with closed redaction.
- Added `dx-agents parity runtime-budget-ci-promotion-operator-packet-history --archive --json` for retained metadata-only runtime budget CI promotion operator packet snapshots covering packet state, source states, recommended exit-code posture, section/action/runbook stability, command guidance, top blocker/alert routing, payload-policy drift, freshness, file-name-only archive evidence, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added parity inventory and verifier coverage for retained runtime budget CI promotion handoff history, promotion handoff history alerts, and the final promotion operator packet, including schema checks, required history surface ids, alert ids, operator section/action ids, source-contract coverage tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion-operator-packet --json` as the final metadata-only runtime budget CI promotion operator packet combining current promotion handoff state, retained handoff history, handoff alerts, parity inventory, parity verifier, runbook coverage, safe operator actions, required archive guidance, latest safe command, recommended exit-code posture, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-ci-promotion-alerts --json` for metadata-only runtime budget CI promotion handoff history alerts covering empty/single/stale evidence, handoff-state regressions, recommended/effective exit-code regressions, dependency regressions, top blocker/alert drift, archive-command drift, command-guidance drift, non-execution regressions, payload-policy regressions, and stable promotion-history posture without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-ci-promotion-history --archive --json` for retained metadata-only runtime budget CI promotion handoff snapshots covering dependency posture, top blocker/alert routing, archive command guidance, recommended/effective exit-code posture, non-execution policy, payload-policy drift, freshness, file-name-only archive evidence, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added parity inventory and verifier coverage for runtime budget CI gate history, runtime budget CI gate alerts, and the runtime budget CI promotion handoff, including schema checks, required retained-history surface ids, alert ids, promotion dependency ids, bounded source-contract tests, and closed runtime-budget redaction flags.
- Added `dx-agents parity runtime-budget-ci-promotion --json` as a metadata-only runtime budget CI promotion handoff over the safe-default CI gate, retained CI gate history, CI history alerts, remediation packet, retained remediation history, remediation alerts, parity inventory, and parity verifier with dependency statuses, top blocker/alert routing, safe archive guidance, non-executing defaults, verification-safe inventory/verifier stubs, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-ci-alerts --json` for metadata-only runtime budget CI gate history alerts covering empty/single/stale evidence, recommended/effective exit-code regressions, strict-mode drift, non-failing default regressions, live-execution regressions, archive-write regressions, gate-count drift, command-guidance drift, payload-policy regressions, stable-history posture, top alert routing, and safe archive guidance without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-ci-history --archive --json` for retained metadata-only runtime budget CI gate snapshots covering recommended/effective exit-code posture, strict opt-in state, non-failing default policy, live-execution policy, archive-write policy, gate-count drift, payload-policy drift, freshness, archive file names, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-ci --json` with explicit `--fail-on-non-clear` strict mode for a metadata-only runtime budget CI gate over the remediation packet, retained remediation history, and remediation history alerts; the gate recommends exit codes, keeps `effective_exit_code=0` by default, disables live tool execution and archive writes, exports closed redaction flags, and is now covered by parity inventory and verifier surfaces.
- Added `dx-agents parity runtime-budget-remediation-history --archive --json` and `dx-agents parity runtime-budget-remediation-history-alerts --json` for retained metadata-only runtime budget remediation packet snapshots and warning/blocker alerts covering stale evidence, unresolved alert drift, unsafe command drift, missing archive cadence, non-execution regressions, operator-action drift, section regressions, command-guidance drift, and payload-policy regressions without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-remediation --json` as a metadata-only operator remediation packet over retained runtime budget history and alerts, including current slow/skipped/missing-isolation posture, top budget alert routing, safe focused commands, archive/runbook guidance, disabled live execution and writes, non-failing `effective_exit_code=0`, focused CLI/parser tests, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Completed Batch 144 parity runtime budget history and alerts with retained metadata-only snapshots, alert coverage, inventory surfaces, verifier checks, focused tests, docs, no-secret scanning, and opened Batch 145 for runtime budget remediation packets and a strict CI gate.
- Added parity inventory and verifier coverage for `parity_runtime_budget_history` and `parity_runtime_budget_history_alerts`, including schema checks, required retained-history surfaces, required alert ids, closed redaction flags, bounded source-contract tests, and metadata-only inventory surfaces.
- Added `dx-agents parity runtime-budget-history-alerts --json` for metadata-only retained parity runtime budget alerts covering empty/single/stale history, slow-surface regressions, skipped-surface regressions, duration bucket regressions, missing isolation diagnostics, payload-policy regressions, current slow/skipped posture, top alert routing, safe archive guidance, and closed redaction without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Added `dx-agents parity runtime-budget-history --archive --json` for retained metadata-only parity inventory runtime budget snapshots covering timing bucket posture, slow-surface counts, skipped-surface counts, missing isolation diagnostics, stale evidence, drift, archive file names, and closed redaction flags without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.
- Completed Batch 143 parity collector runtime budgets with metadata-only per-surface timing, slow-surface warning classification, skipped-surface timeout diagnostics, verifier budget-policy coverage, docs, focused tests, and opened Batch 144 for retained runtime budget history and alerts.
- Added skipped parity inventory surface diagnostics and distinct verifier reporting for `skipped_surface_count` versus `slow_surface_count` so timeout-safe skipped collectors remain metadata-only and operator-visible.
- Added a bounded parity inventory runtime-budget policy that turns over-budget surfaces into warning surfaces with metadata-only isolation evidence and verifier coverage through `inventory_runtime_budget_policy`.
- Added metadata-only per-surface parity inventory timing evidence with collection duration buckets, budget state, and budget threshold metadata so slow collector surfaces can be diagnosed without exporting paths, payloads, provider values, or secrets.
- Completed Batch 142 bounded full-surface parity collector verification with collection-depth guards, bounded source-contract tests, ignored manual full-smoke diagnostics, an operator-visible bounded collector diagnostics inventory surface, verifier coverage, and opened Batch 143 for runtime budgets and slow-surface isolation.
- Added `bounded_parity_collector_diagnostics` to parity inventory and verifier coverage so operators can see the enabled inventory/verifier depth guards, ignored-manual full-smoke posture, bounded contract-test names, and closed workspace-path/secret export evidence.
- Split default parity full-surface smoke coverage into bounded source-contract tests for inventory surface wiring and verifier check wiring, while keeping the recursive full collector smoke tests available as ignored manual diagnostics.
- Added central parity inventory and verifier collection-depth guards that return bounded metadata-only nested guard reports with closed redaction flags when parity surfaces call inventory or verifier transitively, preventing recursive full-surface collection from hanging broad verification paths.
- Completed Batch 141 runtime receipt CI release automation operator signoff coverage with focused module, retention, alert, CLI parser, inventory, verifier, redaction, non-execution, format, metadata, and compiler checks, then opened Batch 142 for bounded full-surface parity collector verification.
- Added parity inventory and verifier coverage for the runtime receipt CI release automation operator signoff packet, retained signoff history, and signoff history alerts, including schema checks, signoff items, safe operator actions, retained history surfaces, alert ids, redaction flags, and verification-safe handoff verification-record collection.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff-history-alerts --json` for metadata-only release automation operator signoff history alerts covering empty/single/stale evidence, signoff regressions, focused verification-record regressions, packet/history/alert regressions, inventory/verifier regressions, unsafe-execution drift, operator-action drift, signoff-item regressions, command-guidance drift, and payload-policy regressions without executing release automation or exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff-history --archive --json` snapshots for release automation operator signoff state, verification-record state, packet/history/alert states, inventory/verifier states, non-execution posture, action/item drift, command guidance, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff --json` as a metadata-only release automation operator signoff packet requiring a clear operator handoff packet, retained packet history, clear packet alerts, focused verification-record coverage, inventory/verifier readiness, parity runbooks, non-execution posture, and closed payload redaction while keeping release automation execution disabled, writes disabled, and `effective_exit_code=0`.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-verification-record --json` for a focused metadata-only verification record covering release automation operator handoff packet behavior, retained packet history, packet history alerts, inventory coverage, verifier coverage contracts, CLI parser coverage, non-execution guarantees, and payload-free redaction while listing safe lightweight validation commands.
- Added parity inventory and verifier coverage for the runtime receipt CI release automation operator handoff packet, retained operator handoff packet history, and packet history alerts, including schema checks, operator handoff sections, safe operator actions, retained history surfaces, alert ids, and closed redaction flags through verification-safe collectors that avoid release automation execution.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet-history-alerts --json` for metadata-only release automation operator handoff packet history alerts covering empty/single/stale evidence, packet-state regressions, dry-run source regressions, inventory/verifier regressions, strict-command drift, unsafe-execution drift, operator action drift, section drift, command-guidance drift, payload-policy regressions, and stable retained evidence without executing release automation or exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet-history --archive --json` snapshots for release automation operator handoff packet state drift, dry-run source-state drift, strict-command drift, non-execution drift, suggested operator-action drift, section drift, command-guidance drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet --json` as a metadata-only release automation operator handoff packet that consumes the handoff dry-run, retained dry-run history, dry-run history alerts, parity inventory, parity verifier, strict-command posture, and required archive guidance while keeping release automation disabled, writes disabled, and `effective_exit_code=0`.
- Completed Batch 139 runtime receipt CI release automation handoff dry-run coverage with focused module, retention, alert, CLI parser, inventory, verifier, redaction, and non-execution checks, then opened Batch 140 for a metadata-only release automation operator handoff packet.
- Added parity inventory and verifier coverage for the runtime receipt CI release automation handoff dry-run, retained dry-run history, and dry-run history alerts, including schema checks, dry-run sections, safe operator actions, retained history surfaces, alert ids, and closed redaction flags through verification-safe collectors that avoid release automation execution.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-handoff-dry-run-history-alerts --json` for metadata-only release automation handoff dry-run history alerts covering empty/single/stale evidence, handoff-state regressions, quorum source regressions, inventory/verifier regressions, strict-command drift, unsafe-execution drift, suggested action drift, section regressions, command-guidance drift, payload-policy regressions, and stable retained evidence without executing release automation or exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-handoff-dry-run-history --archive --json` snapshots for release automation handoff dry-run state drift, source-state drift, strict-command drift, non-execution drift, suggested operator-action drift, section drift, command-guidance drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-automation-handoff-dry-run` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-handoff-dry-run --json` as a metadata-only release automation handoff dry-run packet that consumes the release evidence quorum, retained quorum history, quorum history alerts, parity inventory, parity verifier, and strict-command posture while keeping release automation disabled, writes disabled, and `effective_exit_code=0`; the report exports states, counts, scores, safe DX command names, dry-run-only operator actions, runbook paths, redaction booleans, and recovery hints without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added the next Batch 139 release automation handoff dry-run backlog after completing Batch 138 focused validation for release evidence quorum behavior, retained history, alerts, redaction, CLI parsing, inventory coverage, verifier coverage, and lightweight compiler checks.
- Added parity inventory and verifier coverage for the runtime receipt CI release evidence quorum, retained evidence quorum history, and evidence quorum history alerts, including schema checks, required quorum signals, quorum sections, retained history surfaces, alert ids, and closed redaction flags through verification-safe collectors that avoid recursive inventory/verifier calls.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum-history-alerts --json` for metadata-only runtime receipt CI release evidence quorum history alerts covering empty/single/stale evidence, quorum-state regressions, release packet regressions, release gate history regressions, release gate alert regressions, inventory/verifier regressions, strict-command drift, signal/section regressions, command-guidance drift, payload-policy regressions, and stable retained evidence without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum-history --archive --json` snapshots for runtime receipt CI release evidence quorum state drift, release packet state drift, release gate history drift, release gate alert drift, inventory/verifier drift, strict-command drift, signal/section drift, command guidance drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-evidence-quorum` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum --json` for a metadata-only runtime receipt CI release evidence quorum that requires a clear release operator packet, repeated clear retained release gate evidence, clear release gate alerts, passing inventory, passing verifier, explicit strict release-gate command availability, and closed payload redaction before future release automation handoff; the report exports only states, counts, scores, safe DX command names, runbook paths, strict opt-in metadata, redaction booleans, and recovery hints.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-operator-packet --json` as the final metadata-only runtime receipt CI release operator packet, combining the strict release gate, retained release gate history, release gate alerts, parity inventory, parity verifier, runbook coverage, safe operator actions, required archive guidance, and explicit strict CI command while keeping `effective_exit_code=0` by default and avoiding prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, and secrets; parity inventory and verifier coverage now include the release gate history, release gate alerts, and release operator packet surfaces.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate-history-alerts --json` for metadata-only strict runtime receipt CI release gate history alerts covering empty/single/stale evidence, release-gate state regressions, recommended/effective exit-code regressions, strict failure-mode drift, non-failing default regressions, live-execution/archive-write regressions, command-guidance drift, payload-policy regressions, and stable repeated evidence without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate-history --archive --json` snapshots for strict runtime receipt CI release gate state drift, recommended/effective exit-code drift, strict-mode drift, non-failing default regressions, live-execution/archive-write regressions, gate-count drift, command-guidance drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-gate` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate --json` as a strict runtime receipt CI release gate over the release signoff packet and release signoff history alerts, keeping `effective_exit_code=0` by default, exiting nonzero only when `--fail-on-non-clear` is explicitly set, disabling live execution/archive writes, blocking payload-policy regressions, and adding parity inventory/verifier coverage for the release signoff history, release signoff alerts, and release gate surfaces.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff-history-alerts --json` for metadata-only runtime receipt CI release signoff history alerts covering empty/single/stale evidence, signoff-state regressions, recommended/effective exit-code regressions, non-failing default policy regressions, source-state regressions, parity coverage regressions, item/action/runbook regressions, command-guidance drift, payload-policy regressions, and stable repeated evidence without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff-history --archive --json` snapshots for runtime receipt CI release signoff state drift, recommended/effective exit-code drift, non-failing default policy drift, source-state drift, item/action/runbook drift, command guidance drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-release-signoff` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff --json` as a metadata-only runtime receipt CI release signoff packet requiring a clean current operator packet, retained operator packet history, clear operator packet history alerts, parity inventory coverage, verifier coverage, runbook coverage, closed redaction flags, safe operator actions, and a non-failing default policy with `effective_exit_code=0`; parity inventory and verifier coverage now include the release signoff surface.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet-history-alerts --json` for metadata-only runtime receipt CI operator packet history alerts covering empty/single/stale evidence, packet-state regressions, recommended-exit regressions, source-state regressions, section/action/runbook regressions, command-guidance drift, top-alert regressions, payload-policy regressions, stable repeated evidence, and parity inventory/verifier coverage without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet-history --archive --json` snapshots for runtime receipt CI operator packet state drift, recommended exit-code drift, source-state drift, section/action/runbook drift, command guidance drift, top blocker/alert drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion-operator-packet` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added parity inventory and verifier coverage for retained runtime receipt CI promotion handoff history, promotion history alerts, and the final promotion operator packet, including schema, required history surface, required alert id, operator section, operator action, and runtime receipt redaction checks.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet --json` as a final metadata-only runtime receipt CI promotion operator packet combining current handoff state, retained handoff history, handoff history alerts, parity inventory, parity verifier, runbook coverage, safe operator actions, required archive command, latest safe command, recommended exit-code metadata, and closed redaction flags without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion-alerts --json` for metadata-only runtime receipt CI promotion handoff history alerts covering empty/single/stale evidence, handoff-state regressions, recommended-exit regressions, dependency posture regressions, top-alert regressions, payload-policy regressions, and stable history evidence without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-promotion-history --archive --json` snapshots for runtime receipt CI promotion handoff state drift, recommended exit-code drift, dependency warning/blocker drift, top blocker drift, top alert drift, archive/safe-command drift, freshness, and payload-policy regressions under `target/runtime-receipt-readiness-ci-promotion` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added parity inventory and verifier coverage for runtime receipt CI gate history, CI gate history alerts, and the runtime receipt CI promotion handoff, including schema, required surface/alert/dependency, and redaction checks through verification-safe collectors that avoid recursive inventory/verifier calls.
- Added `dx-agents parity runtime-receipt-readiness-ci-promotion --json` for a metadata-only runtime receipt CI promotion handoff across readiness, retained readiness history, readiness alerts, CI gate, retained CI gate history, CI gate history alerts, parity inventory, and parity verifier state, with promotion-specific blockers, recommended exit-code metadata, safe archive/remediation commands, runbook links, and closed redaction for prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, and secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci-alerts --json` for metadata-only runtime receipt CI gate history alerts covering empty/single/stale evidence, recommended/effective exit-code regressions, strict failure-mode drift, live-execution regressions, payload-policy regressions, and gate-state drift without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added retained `dx-agents parity runtime-receipt-readiness-ci-history --archive --json` snapshots for runtime receipt CI gate exit-code drift, strict-mode posture, live-execution regressions, evidence freshness, blocked/warning gate drift, top-gate changes, and payload-policy regressions under `target/runtime-receipt-readiness-ci-gate` without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness-ci --json` for a strict CI-ready metadata-only runtime receipt gate that recommends exit codes, stays non-failing by default, requires explicit `--fail-on-non-clear` before exiting nonzero, keeps live tool execution disabled, avoids archive writes, and blocks payload-policy regressions without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added parity inventory and verifier coverage for the runtime receipt readiness bundle, retained readiness history, readiness alerts, and readiness CI gate, including schema, required signal/surface/alert/gate, and redaction checks through verification-safe collectors that avoid recursive inventory/verifier calls.
- Added retained `dx-agents parity runtime-receipt-readiness-history --archive --json` snapshots and `dx-agents parity runtime-receipt-readiness-alerts --json` for stale evidence, receipt, replay-readiness, memory-learning, verifier, and payload-policy regressions without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added `dx-agents parity runtime-receipt-readiness --json` for a compact metadata-only runtime receipt operator bundle across tool receipts, retained receipt history, receipt alerts, session replay readiness, memory learning receipts, parity inventory, and parity verifier state without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets.
- Added parity inventory and verifier coverage for tool execution receipts, retained tool receipt history, receipt alerts, session replay readiness, and memory learning receipts, including schema, required receipt/surface/alert, and redaction checks.
- Documented the runtime execution receipt and learning reliability gate in the OpenClaw/Hermes core parity matrix and gap review, including the operator commands for retained receipt history, replay readiness, and learning-loop receipt evidence.
- Added `dx-agents memory learning-receipts --mode <mock|dry-run> --json` for metadata-only learning-loop receipts that show accepted or safely skipped memory write/search/reindex/skill-hook decisions, injection-scan states, rollback metadata, and payload-free policy without exporting memory bodies, query text, memory keys, skill file bodies, secrets, database paths, or rollback payloads.
- Added `dx-agents sessions replay-readiness --mode <mock|dry-run> --json` for metadata-only session replay readiness across tool routing, interruption semantics, stuck-session recovery, child-session lineage, session counts, and receipt evidence without exporting transcript text, prompts, query text, tool payloads, provider secrets, database paths, workspace paths, or payload fingerprints.
- Added `dx-agents tools receipt-history --mode <mock|dry-run> --archive --json` and `dx-agents tools receipt-alerts --mode <mock|dry-run> --json` for retained metadata-only tool receipt snapshots, stale evidence warnings, failed/slow result drift, approval drift, retry-loop drift, payload-policy regressions, and stable execution evidence without exporting prompts, commands, payloads, provider values, paths, or secrets.
- Added `dx-agents tools receipts --mode <mock|dry-run> --json` for metadata-only tool execution receipt summaries covering tool id, category, approval state, result state, retry posture, duration bucket, and redaction policy without invoking tools or exporting prompts, commands, payloads, provider values, paths, or secrets.
- Added parity inventory, verifier, migration-runbook, and OpenClaw/Hermes gap-review coverage for the alias-removal evidence quorum, retained quorum history, quorum alerts, disabled no-op plan, and maintainer signoff guard.
- Added a metadata-only legacy alias removal maintainer signoff packet that requires two fresh clear quorum snapshots, clear quorum alerts, clear dry-run, clear rollback rehearsal, clear release-packet history alerts, clear disabled no-op plan posture, and passing verifier coverage while keeping execution disabled.
- Added a metadata-only disabled no-op alias removal execution plan that exports affected alias ids, placeholder command ids, rollback manifest ids, dependency blockers, and redaction flags without writing files, removing aliases, or exporting alias values.
- Added retained metadata-only alias removal evidence quorum snapshots and quorum history alerts for empty/single/stale evidence, archive cadence, warning/blocker regressions, payload-policy regressions, and accidental write-mode drift before any signoff or no-op removal planning.
- Added a metadata-only alias removal evidence quorum report that combines dashboard telemetry history, telemetry alerts, exact-alias dry-run planning, rollback rehearsal, retained release-packet history, release-packet alerts, parity inventory coverage, and parity verifier checks without enabling alias removal.
- Added retained metadata-only cutover release-packet history snapshots and alerts, plus parity inventory/verifier coverage, so release-note and rollback evidence can be compared across runs before exact-alias removal.
- Added parity inventory and verifier coverage for dashboard compatibility telemetry history, telemetry alerts, exact-alias removal dry-run planning, rollback rehearsal, cutover release-packet history, and release-packet history alerts.
- Documented the exact-alias dry-run, rollback rehearsal, retained release-packet history, and parity verifier gates in the dashboard compatibility migration runbook and OpenClaw/Hermes gap review while keeping compatibility alias removal deferred.
- Added a metadata-only rollback rehearsal command that validates exact-alias rollback action ids, safe recovery paths, affected repo-relative files, and payload-free policy without writing changes.
- Added a metadata-only exact-alias removal dry-run planner that reports removable alias surfaces, affected repo-relative files/modules, prerequisites, rollback actions, blocked gates, and payload-free policy without writing changes.
- Added retained metadata-only dashboard compatibility telemetry history snapshots and alerts so zero legacy dashboard usage must stay stable across repeated runs before any compatibility alias removal.
- Documented the retained exact-alias cutover-plan evidence gate in the dashboard compatibility migration runbook and OpenClaw/Hermes gap review while keeping compatibility alias removal deferred.
- Added parity inventory and verifier coverage for exact-alias cutover-plan history, cutover-plan history alerts, and the cutover release packet, including schema, required surface, alert, section, exact-alias, rollback-action, and redaction checks.
- Added a metadata-only exact-alias cutover release-note and rollback packet that summarizes cutover plan state, retained history, alerts, safe commands, rollback actions, exact alias names, blocked reasons, and payload-free policy without exporting environment values or dashboard storage values.
- Added metadata-only exact-alias cutover-plan history alerts for empty/single/stale evidence, candidate drift, prerequisite regressions, rollback regressions, accidental change-application, cutover allowance regressions, blocked-reason regressions, payload-policy regressions, and stable retained evidence.
- Added retained metadata-only exact-alias cutover-plan snapshots behind explicit archive mode plus latest-vs-previous drift for candidate aliases, prerequisites, rollback actions, blocked reasons, cutover safety, evidence freshness, and payload-free policy.
- Added a metadata-only legacy alias removal evidence soak report that combines alias readiness, retained alias history, alias alerts, release checklist state, maintainer handoff, final operator packet, retained packet history, packet alerts, dashboard telemetry, parity coverage, verifier coverage, and migration runbook state while keeping legacy aliases active.
- Added retained metadata-only legacy alias removal evidence soak snapshots behind explicit archive mode plus latest-vs-previous drift for cutover readiness, alias usage, dependency states, evidence freshness, and payload-free policy.
- Added metadata-only legacy alias removal evidence soak history alerts for empty/single/stale history, telemetry regressions, final-packet regressions, alias-usage regressions, parity/verifier regressions, migration-runbook regressions, payload-policy regressions, and stable retained evidence.
- Added a metadata-only exact-alias legacy alias removal cutover plan that names environment/dashboard alias candidates, prerequisites, rollback actions, blocked reasons, and payload-free operator actions without changing code paths or deactivating compatibility aliases.
- Added parity inventory and verifier coverage for the legacy alias removal evidence soak, retained soak history, soak history alerts, and exact-alias cutover plan, including schema, required-section/surface/alert/candidate/prerequisite/rollback, and redaction checks.
- Documented the legacy alias evidence-soak and exact-alias cutover-plan gate in the OpenClaw/Hermes gap review and dashboard compatibility migration runbook, naming the future-removal aliases while keeping compatibility aliases active.
- Added a metadata-only legacy alias removal final operator packet that consumes maintainer handoff, retained handoff history, handoff alerts, release checklist, dashboard telemetry, parity coverage, verifier coverage, and migration runbook state while keeping legacy aliases active until final signoff gates are clear.
- Added retained metadata-only legacy alias removal final operator packet history snapshots behind explicit archive mode plus latest-vs-previous drift for packet state, final removal allowance, dependency states, staleness, and payload-free policy.
- Added metadata-only legacy alias removal final operator packet history alerts for empty/single/stale evidence, packet regressions, handoff regressions, release-checklist regressions, telemetry regressions, coverage regressions, migration-runbook regressions, payload-policy regressions, and stable retained evidence.
- Added parity inventory and verifier coverage for legacy alias removal final operator packet, retained packet history, and packet history alerts, including schema, required-section/surface/alert, and redaction checks.
- Documented the legacy alias removal final-operator-packet gate in the OpenClaw/Hermes gap review and dashboard compatibility migration runbook, keeping legacy aliases active until retained final packet evidence and dashboard telemetry are clear.
- Added a strict CI workflow template command that consumes rollout pack, retained pack history, and pack alerts, then exports safe default workflow commands, explicit failure-mode commands, required preflight commands, archive cadence, runbook paths, and payload-free redaction metadata.
- Added retained redacted strict CI workflow template history snapshots behind an explicit archive command plus latest-vs-previous drift for workflow state, command counts, rollout dependency states, explicit failure-mode protection, and staleness.
- Added a strict CI workflow dry-run evaluator that checks safe-default, explicit-failing, preflight, and custom command sequences for accidental `--fail-on-non-clear` usage without echoing raw proposed command values.
- Added retained redacted strict CI workflow dry-run history snapshots plus dry-run history alerts for command-count drift, failure-mode drift, template-state drift, accidental failure mode, and stale evidence.
- Added a metadata-only strict CI workflow release-note handoff that summarizes workflow template state, dry-run posture, retained evidence, alerts, runbook paths, and next operator action without exporting raw command values or workspace paths.
- Added a metadata-only strict CI workflow promotion bundle that combines release notes, retained template history, retained dry-run history, dry-run alerts, and verifier metadata into one payload-free promotion packet.
- Added a metadata-only strict CI workflow config draft that emits reviewable GitHub Actions skeleton lines with `dx-command-id:*` placeholders instead of raw command payloads.
- Added retained redacted strict CI workflow promotion bundle history snapshots plus promotion-bundle history alerts for bundle drift, config-state drift, alert regressions, stale evidence, accidental failure-mode enablement, and command-payload export regressions.
- Added a metadata-only hosted CI promotion handoff that combines the config draft, promotion bundle, retained promotion-bundle history, history alerts, verifier metadata, runbook paths, placeholder command ids, and sanitized next actions into one operator packet.
- Added retained redacted hosted CI handoff history snapshots plus hosted handoff alerts for handoff-state drift, config-draft drift, promotion-history regressions, stale evidence, accidental strict-failure enablement, and command-payload export regressions.
- Added a placeholder-only GitHub Actions workflow exporter that writes repo-relative hosted CI skeletons with `dx-command-id:*` comments by default and requires an explicit unsafe flag before raw command values can be emitted.
- Added retained redacted GitHub Actions export history snapshots plus export alerts for skeleton drift, raw-command regressions, stale evidence, output-target drift, strict-failure leakage, and payload-free promotion evidence.
- Added a metadata-only GitHub Actions artifact review command that compares committed workflow skeletons against the generated placeholder-only export, reports placeholder and step drift, blocks raw command leakage, and keeps workflow bodies out of reports.
- Added a metadata-only raw-command promotion checklist that only suggests unsafe GitHub Actions export mode after clean export history, export alerts, artifact review, hosted handoff alerts, safe-default dry-run evidence, retained dry-run history, dry-run alerts, placeholder-only export posture, and payload-free redaction checks.
- Added a lightweight GitHub Actions export collector for artifact review, inventory, and verifier metadata so those checks avoid recursively pulling the full hosted handoff/verifier stack during normal iteration.
- Added a metadata-only raw-command promotion explain command that summarizes blocker ids, statuses, action ids, and safe recovery actions without echoing command values, workflow bodies, workspace paths, or artifact contents.
- Added retained redacted raw-command promotion checklist history snapshots plus checklist history alerts for promotion-state drift, evidence staleness, artifact-review regressions, safe-default dry-run regressions, generated export regressions, blocked item regressions, and accidental unsafe-mode suggestions.
- Added a metadata-only raw-command promotion maintainer handoff that bundles checklist state, retained checklist history, checklist alerts, artifact review posture, GitHub Actions export history and alerts, hosted handoff alerts, safe-default dry-run evidence, operator action ids, and payload-free policy without exporting workflow bodies or raw command values.
- Added retained redacted raw-command promotion handoff history snapshots for handoff-state drift, alert-state regressions, evidence staleness, payload-policy regressions, and accidental raw-command enablement without exporting workflow bodies, raw commands, or workspace paths.
- Added metadata-only raw-command promotion handoff history alerts for empty/single/stale history, handoff-state regressions, checklist/export/hosted alert regressions, raw-command enablement regressions, payload-policy regressions, and stable retained handoff evidence.
- Added a compact metadata-only raw-command promotion release-audit digest that combines the maintainer handoff, retained handoff history, handoff alerts, checklist alerts, verifier metadata, safe archive guidance, runbooks, and payload-free redaction guarantees.
- Added retained redacted raw-command promotion release-audit digest history snapshots behind explicit archive mode plus latest-vs-previous drift for release-audit state, handoff state, handoff-history state, alert states, verifier metadata, command guidance, staleness, and payload-free policy.
- Added metadata-only raw-command promotion release-audit digest history alerts for empty/single/stale history, release-audit regressions, handoff and handoff-history regressions, alert regressions, verifier metadata regressions, command-guidance drift, payload-policy regressions, and stable retained audit evidence.
- Added a final metadata-only raw-command promotion operator packet that combines the release-audit digest, retained digest history, digest alerts, runbooks, and explicit manual unsafe-mode policy with inventory and verifier coverage.
- Added retained redacted raw-command promotion operator packet history snapshots behind explicit archive mode plus latest-vs-previous drift for packet state, digest states, manual unsafe-mode policy, command guidance, staleness, and payload-free policy.
- Added metadata-only raw-command promotion operator packet history alerts for empty/single/stale history, packet-state regressions, manual-policy regressions, digest-state regressions, section regressions, command-guidance drift, payload-policy regressions, and stable retained handoff evidence.
- Added a metadata-only raw-command operator packet release checklist that gives maintainers placeholder-only defaults, explicit unsafe-mode review steps, retained packet-history gates, packet-history alerts, runbook links, and payload-free inventory/verifier coverage.
- Added retained redacted raw-command operator packet release-checklist history snapshots behind explicit archive mode plus latest-vs-previous drift for checklist state, packet/history/alert states, placeholder defaults, unsafe-review allowance, command guidance, staleness, and payload-free policy.
- Added metadata-only raw-command operator packet release-checklist history alerts for empty/single/stale history, checklist-state regressions, placeholder-default regressions, unsafe-review regressions, dependency regressions, item-count regressions, command-guidance drift, payload-policy regressions, and stable retained signoff evidence.
- Added parity inventory and verifier coverage for raw-command operator packet release-checklist history plus release-checklist alerts, including schema, required-surface, required-alert, and redaction checks.
- Added a metadata-only legacy alias decommission readiness report for `ZEROCLAW_*` environment aliases, legacy storage fallback, dashboard browser aliases, migration readers, archived Agent reference, and internal `zeroclaw-*` namespace compatibility without exporting secret values, config contents, browser storage values, or workspace paths.
- Added retained metadata-only legacy alias decommission snapshots behind explicit archive mode plus latest-vs-previous drift for alias state, active legacy env names, active surface counts, decommission readiness, staleness, and redaction policy.
- Added metadata-only legacy alias decommission alerts for empty/single/stale evidence, active alias usage, alias regressions, missing migration readers, decommission gate regressions, payload-free regressions, and unsafe removal attempts.
- Added parity inventory and verifier coverage for legacy alias decommission readiness, retained history, and alert metadata, including schema, required-surface, required-alert, and redaction checks.
- Documented the legacy alias decommission decision in the OpenClaw/Hermes gap review and dashboard compatibility migration gate, keeping aliases active until retained evidence and dashboard telemetry are clear.
- Added a metadata-only legacy alias removal release checklist that gates future compatibility removal on legacy alias inventory, retained history, alerts, dashboard compatibility telemetry, parity inventory coverage, verifier coverage, runbook policy, and payload-free redaction.
- Added retained metadata-only legacy alias removal checklist snapshots behind explicit archive mode plus latest-vs-previous drift for checklist state, removal allowance, blocked and warning item counts, dependency states, staleness, and redaction policy.
- Added metadata-only legacy alias removal checklist history alerts for empty/single/stale evidence, checklist regressions, dashboard telemetry regressions, legacy alias alert regressions, coverage regressions, payload-policy regressions, and stable retained evidence.
- Added parity inventory and verifier coverage for legacy alias removal checklist, retained checklist history, and checklist history alerts, including schema, required-item, required-surface, required-alert, and redaction checks.
- Documented the legacy alias removal release-checklist gate in the OpenClaw/Hermes gap review and dashboard compatibility migration runbook, keeping legacy aliases active until retained evidence and dashboard telemetry are clear.
- Added a metadata-only legacy alias removal maintainer handoff that consumes release checklist state, retained checklist history, checklist alerts, dashboard telemetry, parity coverage, and migration runbook state without exporting storage values, environment values, command payloads, secrets, or workspace paths.
- Added retained metadata-only legacy alias removal maintainer handoff snapshots behind explicit archive mode plus latest-vs-previous drift for signoff state, checklist dependencies, dashboard telemetry, migration runbook state, parity coverage, staleness, and payload-free redaction policy.
- Added metadata-only legacy alias removal maintainer handoff history alerts for empty/single/stale evidence, handoff regressions, checklist regressions, dashboard telemetry regressions, parity coverage regressions, migration-runbook regressions, payload-policy regressions, and stable retained evidence.
- Added parity inventory and verifier coverage for legacy alias removal maintainer handoff, retained handoff history, and handoff history alerts, including schema, required-section, required-surface, required-alert, and redaction checks.
- Documented the legacy alias removal maintainer-handoff gate in the OpenClaw/Hermes gap review and dashboard compatibility migration runbook, keeping legacy aliases active until retained handoff evidence and dashboard telemetry are clear.
- Added a strict CI rollout audit digest that combines opt-in policy evidence, retained opt-in history, opt-in alerts, release-candidate evidence, verifier metadata, exit-code posture, safe commands, runbooks, and next remediation action into one metadata-only operator handoff.
- Added retained redacted strict CI rollout audit digest history snapshots behind an explicit archive command plus latest-vs-previous drift for rollout readiness, opt-in policy state, release-candidate state, alert state, exit codes, command guidance, and staleness.
- Added strict CI rollout audit digest history alerts for empty history, single snapshots, stale evidence, rollout-readiness regressions, opt-in policy regressions, release-candidate regressions, alert-state regressions, exit-code drift, command drift, and stable handoff history.
- Added a strict CI rollout pack command that exposes one safe non-failing default command, one explicit `--fail-on-non-clear` command, rollout readiness state, evidence freshness, archive guidance, runbooks, and payload-free redaction metadata for CI template handoff.
- Added retained redacted strict CI rollout pack history snapshots with drift across rollout state, opt-in readiness, evidence freshness, dependency states, exit codes, command guidance, and staleness.
- Added strict CI rollout pack history alerts for empty history, single snapshots, stale evidence, rollout-state regressions, opt-in readiness regressions, evidence regressions, dependency-state regressions, exit-code drift, accidental failure-mode enablement, command drift, and stable pack history.
- Established DX Agents as the active Rust product forked from latest ZeroClaw.
- Added provider/model CLI management, session recovery commands, focused self-test slices, channel allowlist management, and OpenClaw/Hermes/Agent/ZeroClaw migration readers.
- Added `docs/parity/openclaw-hermes-core.md` to track implemented, preserved, and deferred parity work.
- Added DX Agents desktop package branding, bundle identity, onboarding copy, tray labels, and compatible dashboard token storage.
- Added a grouped DX CLI host menu command for the desktop bridge so future UI work can render command groups directly.
- Added `dx-agents continue` with `workloop` alias, a durable JSONL continuation journal, TODO/CHANGELOG status parsing, and next-check suggestions for automation runs.
- Added `dx-agents models health` with mock, dry-run, live, and JSON modes for fast provider profile verification.
- Added OpenClaw/Hermes migration dry-run summaries with target comparison, skip/rename counts, category totals, and sample actions before memory writes.
- Added `dx-agents status --compact` and `--json` as a read-only operator dashboard across provider, gateway, sessions, memory, cron, and channel allowlist health.
- Added desktop bridge surfaces for provider health and continuation journal history alongside grouped DX CLI actions.
- Added a desktop bridge self-test IPC command that reports declared command availability plus repo, CLI, and host-contract diagnostics without launching the full app.
- Added desktop provider-health controls for mock, dry-run, and live checks plus optional profile selection.
- Added dashboard actions for opening TODO.md, CHANGELOG.md, and the continuation journal from known safe targets.
- Added compact status snapshots to the desktop bridge so operators can compare provider, gateway, memory, cron, and warning health across refreshes.
- Added persisted desktop bridge settings with host-contract defaults and constraint normalization.
- Added a desktop dashboard surface for bridge self-test diagnostics.
- Added a secret-safe desktop live provider smoke action that reuses selected provider/profile settings and redacts common auth tokens before display.
- Added gateway pairing and channel allowlist details to compact status snapshots in the desktop bridge.
- Added `dx-agents cron preview --json` and a desktop cron dry-run preview for scheduled jobs and heartbeat-style continuations.
- Added a desktop media-viewer route planner for choosing inspect, terminal, or preview actions from the selected media input.
- Added a DX CLI Bridge embedded PTY readiness panel that shows missing input, resize, lifecycle, renderer, and media-session gates from the generated host contract.
- Added DX CLI Bridge embedded terminal session diagnostics for open, resize, input, interrupt, and close operations from the generated host contract.
- Added DX CLI Bridge embedded terminal renderer diagnostics for ANSI, alternate screen, cursor state, scrollback, and terminal-video frame readiness from the generated host contract.
- Added DX CLI Bridge embedded terminal input diagnostics for keyboard, paste, focus, mouse, and control-sequence forwarding from the generated host contract.
- Added DX CLI Bridge embedded terminal resize diagnostics for initial size, viewport resize, debounce, renderer reflow, and PTY resize readiness.
- Added DX CLI Bridge embedded terminal media-session diagnostics for terminal-video, audio, image-preview, backpressure, and frame-budget readiness.
- Added DX CLI Bridge embedded terminal production-readiness diagnostics for PTY, lifecycle, renderer, input, resize, and media-session rollup blockers.
- Added DX CLI Bridge embedded terminal evidence export diagnostics for redacted blocker evidence, artifact reference, and freshness policy.
- Added provider smoke history JSONL snapshots and a desktop history panel for comparing live model confidence over time.
- Added a desktop gateway pairing-code remediation action that appears when pairing is required but no token is cached.
- Added `dx-agents cron history --json` plus a desktop cron run-history panel for recent failures and slow runs.
- Added a redacted desktop bridge status export action under `target/host-telemetry`.
- Added operator-visible desktop recovery hints for failed provider, cron, gateway, and bridge export actions.
- Added a desktop release-readiness report for repo paths, debug/release binaries, host contracts, status docs, parity docs, and migration command availability.
- Added safe desktop review of redacted bridge status exports, including latest-export opening restricted to `bridge-status-export-*.json` files under host telemetry.
- Added an operations runbook for the desktop bridge covering live provider smoke, gateway pairing recovery, cron preview/history, redacted status exports, embedded terminal readiness, and focused verification.
- Expanded the desktop release-readiness report with Tauri bundle config, version metadata, installer icons, bundle targets, and expected distribution outputs without running a release build.
- Added an operator QA smoke checklist for provider, gateway pairing, cron delivery, memory, migration dry runs, and desktop bridge release-readiness flows.
- Added operator QA evidence paths for continuation journal, redacted bridge exports, release-readiness output, and docs-backed QA artifacts.
- Added validated raw host-contract pass-through so embedded terminal input and resize readiness fields from DX CLI survive the desktop bridge boundary.
- Added no-payload embedded terminal input and resize fixtures for keyboard, paste, focus, mouse, control-sequence, viewport resize, debounce, renderer reflow, and PTY resize metadata.
- Added a redacted embedded-terminal readiness export that combines host-contract production gates with no-payload fixture evidence.
- Added an embedded terminal pilot plan that preserves Windows Terminal as the production fallback through evidence-only, synthetic, echo-process, TUI-canary, and media-canary phases.
- Added a synthetic embedded terminal session timeline export that uses no-payload fixtures and production-gate evidence without spawning a process.
- Added DX CLI Bridge diagnostics for generated `embedded_terminal_session_timeline` contract summaries, synthetic-only safety, and next canary phase.
- Added a controlled embedded terminal echo-process pilot using the fixed `dx-agents echo-pilot --json` payload instead of arbitrary shell input.
- Added desktop bridge diagnostics for the synthetic embedded terminal session timeline and controlled echo-process lifecycle evidence.
- Added embedded terminal canary promotion and rollback documentation from synthetic session to echo process to TUI and media canaries.
- Added a DX CLI Bridge fallback renderer for generated `embedded_terminal_session_timeline` contract diagnostics.
- Added a developer-only embedded TUI canary gate that defaults off, preserves normal terminal actions, and only reports explicit local opt-in state.
- Added bounded embedded TUI canary lifecycle evidence for open, resize, interrupt, close, and cleanup guardrails.
- Added DX CLI Bridge controls and diagnostics for TUI canary enable guidance, disable guidance, rollback state, and lifecycle evidence.
- Added operator QA notes for repeated TUI canary diagnostics, process cleanup evidence, renderer stability, and external media fallback.
- Added DX CLI Bridge diagnostics for the generated `embedded_terminal_echo_pilot` contract, including fixed argv readiness, output redaction, failure recovery, and arbitrary shell-input blocking.
- Added a hidden fixed `dx-agents tui-canary --json` developer runner plus desktop IPC execution evidence gated by `DX_AGENTS_TUI_CANARY`.
- Added a DX CLI Bridge TUI canary run control and output diagnostics for gated process status, byte caps, stdout/stderr previews, and rollback reset state.
- Added redacted TUI canary renderer evidence snapshots for alternate screen, cursor, scrollback, resize, interrupt, close, and cleanup state.
- Added source-owned DX CLI Bridge diagnostics for TUI renderer evidence snapshots and rollback safety.
- Added guarded source-owned TUI runner contract reading for fixed argv, output caps, duration caps, skip reasons, and drift diagnostics.
- Added redacted media canary evidence for terminal-video, audio, image preview, backpressure, frame budget, close, and cleanup.
- Added media canary evidence to desktop bridge diagnostics and redacted operator status exports.
- Added TUI runner contract support/schema drift diagnostics plus fixed-command and result-state bridge export fields.
- Added DX CLI Bridge diagnostics for the promoted `embedded_terminal_tui_canary_runner` contract, including source-owned argv, output caps, result states, skip reasons, and contract-drift status.
- Added source-owned TUI renderer evidence contract reading with snapshot redaction, rollback-state, and drift diagnostics.
- Added a dashboard branding and DX env alias compatibility batch to the continuation backlog.
- Added dashboard DX Agents compatibility aliases for auth token, session id, chat history, compact-chat preferences, tool-activity preferences, theme, locale, sidebar collapse state, and live log storage.
- Added dashboard compatibility diagnostics to the DX CLI Bridge for brand, env, global, storage, and unauthorized-event aliases without displaying stored values.
- Added DX Agents WebSocket subprotocol offers for chat and canvas while preserving `zeroclaw.v1` fallback negotiation.
- Added source-owned `dashboard_compatibility` metadata to the DX CLI host contract for dashboard brand, env, global, storage, event, and WebSocket protocol aliases.
- Added a capture-friendly dashboard compatibility status export with schema, source, alias counts, legacy policy, and source-token drift checks.
- Added dashboard compatibility status to redacted bridge status exports.
- Added `docs/dashboard-compatibility-migration.md` as the versioned cleanup gate for legacy dashboard aliases.
- Added dashboard compatibility migration-gate status to the compatibility export and release-readiness artifact checks.
- Added `dx-agents dashboard-compatibility --json` for redacted operator capture of dashboard alias metadata, policy, drift checks, and migration-gate state.
- Added `dx-agents parity inventory` for a redacted OpenClaw/Hermes-inspired runtime parity report across providers, sessions, tools, memory, skills, gateway, cron, migrations, and desktop bridge exports.
- Added `dx-agents parity preflight` for a single redacted runtime release-readiness gate across provider failover, session tool routing, memory learning, gateway pairing, cron delivery, and tool safety.
- Added `dx-agents parity skills` for metadata-only skill-pack discovery and execution-readiness reporting without exporting skill bodies, script bodies, absolute paths, or registry URL values.
- Added `docs/parity/openclaw-hermes-gap-review.md` to tie cloned OpenClaw/Hermes evidence to implemented, blocked, deferred, and next-highest-value DX Agents runtime parity work.
- Added `dx-agents parity verify` for a focused lightweight verification gate over parity inventory schemas, required runtime surfaces, preflight gates, source commands, skill metadata, and redaction flags.
- Added `dx-agents parity skill-audit` for metadata-only skill trust verdicts, structural risk counts, credential-pattern counts, persistence-pattern counts, and recovery hints without exporting skill bodies, script bodies, absolute paths, or matched sensitive text.
- Added `dx-agents sessions search-summary` for redacted session FTS summaries with dry-run and deterministic mock modes, child-session hiding, role counts, and match counts without exporting transcript text, tool payloads, provider secrets, query text, or database paths.
- Added `dx-agents parity agents` for metadata-only multi-agent routing readiness across delegate agents, swarms, parallel fanout, router handoff, subagent loop guardrails, child-session lineage, background handoff, and gateway control-plane recovery without exporting delegate names, prompts, provider keys, tool payloads, session content, task ids, result paths, or workspace paths.
- Added `dx-agents parity delivery-queue` plus runtime preflight coverage for metadata-only unattended delivery queue health across queue storage, retry posture, drain status, retention, gateway prerequisites, and cron delivery targets without exporting message payloads, prompts, commands, recipients, channel identities, secrets, or workspace paths.
- Added a Rust durable outbound delivery queue store for cron/manual/heartbeat delivery metadata with pending, retrying, failed, delivered, and dead-letter states, bounded retry attempts, next-at timestamps, retention cleanup, and metadata-only drain summaries.
- Added `dx-agents parity remote-skills` for metadata-only remote/node skill readiness across node endpoints, pairing/auth, device capability metadata, remote exec metadata, and trust gates without exporting node names, URLs, IPs, tokens, shared secrets, peer allowlist values, raw capability names, workspace paths, or skill bodies.
- Added remote/node skill readiness to `dx-agents parity skills`, `dx-agents parity inventory`, and `dx-agents parity verify` so OpenClaw-inspired remote skill metadata is visible from the existing parity gates.
- Added `dx-agents parity managed-tools` for metadata-only managed tool gateway readiness across managed surfaces, vendor gateway resolution, auth posture, provider route metadata, and approval gates without exporting user tokens, API keys, gateway URLs, vendor names, provider base URLs, MCP server values, entity IDs, or tool payloads.
- Added managed tool gateway readiness to `dx-agents parity inventory` and `dx-agents parity verify` so Hermes-inspired managed gateway metadata is visible from the existing parity gates.
- Added `dx-agents parity channels` for metadata-only channel/app capability readiness across configured/active channel counts, core vs long-tail coverage, credential gates, allowlist posture, webhook/gateway posture, and session delivery policy without exporting bot tokens, app secrets, access tokens, webhook URLs, usernames, room IDs, phone numbers, email addresses, channel IDs, or message payloads.
- Added channel/app readiness to `dx-agents parity inventory` and `dx-agents parity verify` so OpenClaw/Hermes long-tail integration metadata is visible from the existing parity gates.
- Added `dx-agents parity promotion` for metadata-only remote execution promotion guardrails across remote skill nodes, managed tool gateways, channel/app readiness, delivery queue posture, tool safety, trust/auth, approval, rollback, and canary-required policy without exporting secrets, node identities, gateway URLs, workspace paths, prompts, commands, delivery targets, or tool payloads.
- Added `dx-agents parity canary` for dry-run remote execution canary evidence across synthetic node, managed gateway, channel delivery, approval, and rollback rows without invoking live remote tools, sending messages, storing payloads, or exporting identities, prompts, commands, gateway URLs, delivery targets, workspace paths, or tool payloads.
- Added `dx-agents parity canary-history` with an explicit `--archive` mode for retained redacted canary snapshots under `target/remote-execution-canary` plus latest-vs-previous drift summaries for canary state, blocked surfaces, payload-free guarantees, live invocation regression, rollback readiness, and staleness without exporting absolute paths or payload values.
- Added `dx-agents parity canary-alerts` for metadata-only remote execution canary drift alerts across empty, single, stable, improving, changed, worsening, payload/live regression, rollback regression, and stale history states, plus a remediation runbook.
- Added `dx-agents parity release-gate` for a metadata-only remote execution go/no-go decision across promotion guardrails, dry-run canary evidence, retained canary history, drift alerts, approval, rollback, and payload-free policy while keeping live execution disabled.
- Added `dx-agents parity release-gate-history` with an explicit `--archive` mode for retained redacted release gate audit snapshots under `target/remote-execution-release-gate` plus latest-vs-previous drift for gate state, blocked dependencies, payload-free policy, live-execution enablement, approval, rollback, and staleness.
- Added `dx-agents parity release-gate-alerts` for metadata-only release gate audit alerts across empty, single, stable, improving, changed, worsening, payload-free regression, live-execution enablement regression, approval regression, rollback regression, and stale history states, plus a remediation runbook.
- Added `dx-agents parity release-gate-digest` for a compact metadata-only operator summary across release gate status, release gate history, release gate alerts, canary alerts, and promotion guardrails with a single state, top blocker, latest safe command, and next remediation action.
- Added `dx-agents parity release-gate-digest-history` with an explicit `--archive` mode for retained redacted operator digest snapshots under `target/remote-execution-release-gate-digest` plus latest-vs-previous drift for operator state, top blocker, blocked signals, warning signals, latest safe command, and staleness.
- Added `dx-agents parity release-gate-digest-alerts` for metadata-only operator digest alerts across empty history, single snapshots, stale snapshots, blocked signal regressions, warning signal regressions, top blocker changes, safe command changes, and operator-state regressions, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness` for one metadata-only remote execution operator readiness bundle across release gate digest, digest history, digest alerts, release gate alerts, canary alerts, and promotion guardrails with top blocker, top alert, latest safe command, evidence freshness, and remediation metadata.
- Added `dx-agents parity release-gate-readiness-history` with an explicit `--archive` mode for retained redacted remote execution readiness snapshots under `target/remote-execution-readiness` plus latest-vs-previous drift for readiness state, top blocker, top alert, stale source count, blocked components, warning components, latest safe command, and staleness.
- Added `dx-agents parity release-gate-readiness-alerts` for metadata-only readiness history alerts across empty history, single snapshots, stale snapshots, blocked component regressions, warning component regressions, stale source regressions, top signal changes, safe command changes, and readiness-state regressions, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci` for one metadata-only CI promotion packet across current readiness, retained readiness history, readiness alerts, strict-mode exit-code metadata, payload-free policy, top blocker, top alert, latest safe command, and remediation guidance without exiting nonzero by default.
- Added `dx-agents parity release-gate-readiness-ci-history` with an explicit `--archive` mode for retained redacted readiness CI gate snapshots under `target/remote-execution-readiness-ci` plus latest-vs-previous drift for CI state, recommended exit code, readiness state, alert state, history state, top blocker, top alert, latest safe command, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-alerts` for metadata-only readiness CI gate history alerts across empty history, single snapshots, stale snapshots, CI state regressions, recommended exit-code regressions, readiness signal state changes, top signal changes, and safe command changes, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-digest` for one compact metadata-only readiness CI operator payload across the current CI gate, retained CI gate history, CI history alerts, exit-code policy, and payload-free policy with a digest state, score, top blocker, top alert, latest safe command, recommended exit code, runbook paths, and remediation guidance.
- Added `dx-agents parity release-gate-readiness-ci-digest-history` with an explicit `--archive` mode for retained redacted readiness CI digest snapshots under `target/remote-execution-readiness-ci-digest` plus latest-vs-previous drift for digest state, recommended exit code, CI/history/alert states, top blocker, top alert, latest safe command, warning signals, blocked signals, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-digest-alerts` for metadata-only readiness CI digest history alerts across empty history, single snapshots, stale snapshots, digest-state regressions, recommended exit-code regressions, blocked signal regressions, warning signal regressions, top signal changes, and safe command changes, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion` for one metadata-only CI promotion packet across readiness CI gate, digest, retained digest history, digest alerts, strict exit-code policy, and payload-free policy with a single promotion state, recommended exit code, top blocker, top alert, required archive command, latest safe command, runbooks, and remediation guidance.
- Added `dx-agents parity release-gate-readiness-ci-promotion-history` with an explicit `--archive` mode for retained redacted readiness CI promotion snapshots under `target/remote-execution-readiness-ci-promotion` plus latest-vs-previous drift for promotion state, recommended exit code, CI/digest/history/alert states, top blocker, top alert, required archive command, latest safe command, warning dependencies, blocked dependencies, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-history-alerts` for metadata-only readiness CI promotion history alerts across empty history, single snapshots, stale snapshots, promotion-state regressions, recommended exit-code regressions, blocked dependency regressions, warning dependency regressions, top signal changes, archive command changes, and safe command changes, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion-alert-digest` for one compact metadata-only automation payload across the current readiness CI promotion packet, retained promotion history, promotion history alerts, strict exit-code policy, and payload-free policy with digest state, score, recommended exit code, top blocker, top alert, required archive command, latest safe command, runbooks, redaction metadata, and remediation guidance.
- Added `dx-agents parity release-gate-readiness-ci-promotion-alert-digest-history` with an explicit `--archive` mode for retained redacted readiness CI promotion alert digest snapshots under `target/remote-execution-readiness-ci-promotion-alert-digest` plus latest-vs-previous drift for digest state, recommended exit code, promotion/history/alert states, top blocker, top alert, required archive command, latest safe command, warning signals, blocked signals, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-alert-digest-history-alerts` for metadata-only readiness CI promotion alert digest history alerts across empty history, single snapshots, stale snapshots, digest-state regressions, recommended exit-code regressions, promotion/history/alert state changes, blocked signal regressions, warning signal regressions, top signal changes, archive command changes, and safe command changes, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion-enforcement` for the final metadata-only CI go/no-go packet across the current promotion alert digest, retained digest history, digest history alerts, strict exit-code policy, payload-free policy, top blocker, top alert, required archive command, latest safe command, runbook paths, and remediation guidance.
- Added `dx-agents parity release-gate-readiness-ci-promotion-enforcement-history` with an explicit `--archive` mode for retained redacted enforcement snapshots under `target/remote-execution-readiness-ci-promotion-enforcement` plus latest-vs-previous drift for enforcement state, recommended exit code, top blocker, top alert, required archive command, latest safe command, warning gates, blocked gates, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-enforcement-alerts` for metadata-only enforcement history alerts across empty history, single snapshots, stale snapshots, enforcement-state regressions, recommended exit-code regressions, blocked gate regressions, warning gate regressions, top signal changes, archive command changes, and safe command changes, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-policy` for explicit strict CI dry-run metadata across the enforcement packet, retained enforcement history, and enforcement-history alerts, including would-exit code, effective opt-in exit code, blocking reason, safe commands, runbooks, and payload-free redaction guarantees.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-policy-history` with an explicit `--archive` mode for retained redacted strict-policy snapshots under `target/remote-execution-readiness-ci-promotion-strict-policy` plus latest-vs-previous drift for strict-policy state, would-exit code, effective exit code, blocking reason, required archive command, latest safe command, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-policy-alerts` for metadata-only strict-policy history alerts across empty history, single snapshots, stale snapshots, strict-policy state regressions, would-exit regressions, effective-exit regressions, blocking reason changes, archive command drift, safe command drift, and stable history, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-checklist` as the final metadata-only strict CI promotion checklist across enforcement, retained enforcement history, enforcement alerts, strict policy, retained strict-policy history, strict-policy alerts, and payload-free redaction.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-checklist-history` with an explicit `--archive` mode for retained redacted strict-checklist snapshots under `target/remote-execution-readiness-ci-promotion-strict-checklist` plus latest-vs-previous drift for checklist state, promotion_allowed, top blocker, top alert, required archive command, latest safe command, warning items, blocked items, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-strict-checklist-alerts` for metadata-only strict-checklist history alerts across empty history, single snapshots, stale snapshots, checklist-state regressions, promotion_allowed regressions, blocked/warning item regressions, top signal drift, archive command drift, safe command drift, and stable history, plus a remediation runbook.
- Added `dx-agents parity release-gate-readiness-ci-promotion-release-candidate` as the strict CI opt-in evidence bundle across strict checklist, retained checklist history, checklist alerts, strict-policy alerts, enforcement alerts, verifier metadata, and payload-free redaction.
- Added `dx-agents parity release-gate-readiness-ci-promotion-opt-in-policy` as the explicit strict CI opt-in policy layer above release-candidate evidence with default non-failing dry-run behavior, `--fail-on-non-clear` as the clearly named opt-in failure mode, would-exit/effective-exit metadata, safe command guidance, and payload-free redaction.
- Added `dx-agents parity release-gate-readiness-ci-promotion-opt-in-policy-history` with an explicit `--archive` mode for retained redacted opt-in policy snapshots under `target/remote-execution-readiness-ci-promotion-opt-in-policy` plus latest-vs-previous drift for opt-in policy state, release-candidate state, strict CI opt-in allowance, would-exit code, effective-exit code, archive command, safe command, and staleness.
- Added `dx-agents parity release-gate-readiness-ci-promotion-opt-in-policy-alerts` for metadata-only opt-in policy history alerts across empty history, single snapshots, stale snapshots, opt-in policy regressions, release-candidate regressions, strict CI opt-in allowance regressions, would-exit regressions, effective-exit drift, command drift, and stable history, plus a remediation runbook.
- Added redacted browser-side dashboard compatibility usage telemetry for primary/legacy reads, writes, removes, and migrations without storing browser values.
- Added dashboard compatibility usage/decommission state to the CLI and desktop bridge status exports, including zero-usage, active-usage, and missing-telemetry decisions.
- Added DX CLI Bridge diagnostics for `embedded_terminal_native_promotion`, including native surface blockers, fallback routes, and rollback messages without enabling production routing.
- Added `dx-agents sessions tool-routing --json` for redacted session tool routing readiness, state-model, interruption, and recovery semantics.
- Added `docs/session-tool-routing.md` as the source-owned gate for session tool state exports and payload redaction rules.
- Added session tool routing to redacted desktop bridge status exports without adding another dashboard control.
- Added `dx-agents memory learning-loop --json` for redacted memory backend, memory roundtrip, skill hook, skillforge, and learning-loop readiness.
- Added memory skill learning loop output to redacted desktop bridge status exports without requiring live provider calls.
- Added DX CLI Bridge consumption of the `dx.native_promotion` JSON action, including operator export readiness, redaction state, blocker rows, and rollback messages.
- Added DX CLI Bridge native promotion archive review controls for retained redacted snapshots under `G:\Cli\target\native-promotion`.
- Added retained redacted tool safety drill history under host telemetry with bridge trend rendering, blocked recovery hints, export/open controls, and bridge status export coverage.
- Added metadata-only tool safety history drift alerts for empty, stable, improving, warning, and blocked states.
- Added `docs/tool-safety-alert-runbook.md` plus a safe DX CLI Bridge opener and release-readiness coverage for tool safety alert remediation.
- Added a metadata-only tool safety audit summary covering drill, history, alert, and runbook readiness without duplicating raw history rows in status exports.
- Added retained metadata-only tool safety audit review history with bridge trend rendering, latest remediation action, export/open controls, and bridge status export coverage.
- Added `docs/tool-safety-audit-review-runbook.md` plus a bridge opener, audit-history runbook presence, and release-readiness coverage for audit review remediation.
- Added a compact metadata-only tool safety audit review digest to audit history, bridge rendering, and redacted status exports.
- Added digest-derived metadata-only tool safety audit review alerts with bridge severity rendering and redacted status export coverage.
- Added `docs/tool-safety-audit-review-alert-runbook.md` plus a bridge opener, digest presence metadata, release-readiness coverage, and safe target routing for audit review alert remediation.
- Added metadata-only tool safety audit review escalation evidence with bridge rendering, redacted status export coverage, and focused severity tests.
- Added a dry-run-only tool safety audit recovery drill with bridge rendering, redacted status export coverage, and focused blocked, warning, cleared, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-runbook.md` plus a bridge opener, recovery drill runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery digest with bridge rendering, redacted status export coverage, and focused blocked, warning, pending, cleared, and redaction tests.
- Added digest-derived metadata-only tool safety audit recovery alerts with bridge rendering, redacted status export coverage, and focused severity, ordering, runbook-missing, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-runbook.md` plus a bridge opener, recovery alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest with bridge rendering, redacted status export coverage, and focused blocked, warning, ok, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-runbook.md` plus a bridge opener, recovery alert digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a metadata-only tool safety audit recovery alert digest release gate with bridge rendering, redacted status export coverage, release-readiness scoring, and focused ok, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-runbook.md` plus a bridge opener, release gate runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest release gate digest with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-runbook.md` plus a bridge opener, release gate digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added digest-derived metadata-only tool safety audit recovery alert digest release gate digest alerts with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-runbook.md` plus a bridge opener, release gate digest alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest release gate digest alert digest with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-runbook.md` plus a bridge opener, release gate digest alert digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added digest-derived metadata-only tool safety audit recovery alert digest release gate digest alert digest alerts with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-runbook.md` plus a bridge opener, release gate digest alert digest alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-runbook.md` plus a bridge opener, release gate digest alert digest alert digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added digest-derived metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest alerts with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-alert-runbook.md` plus a bridge opener, release gate digest alert digest alert digest alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest alert digest with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-alert-digest-runbook.md` plus a bridge opener, release gate digest alert digest alert digest alert digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added digest-derived metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest alert digest alerts with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-alert-digest-alert-runbook.md` plus a bridge opener, release gate digest alert digest alert digest alert digest alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added a compact metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest alert digest alert digest with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-alert-digest-alert-digest-runbook.md` plus a bridge opener, release gate digest alert digest alert digest alert digest alert digest runbook presence metadata, release-readiness coverage, and safe target routing.
- Added digest-derived metadata-only tool safety audit recovery alert digest release gate digest alert digest alert digest alert digest alert digest alerts with bridge rendering, redacted status export coverage, and focused ready, warning, blocked, missing-runbook, and redaction tests.
- Added `docs/tool-safety-audit-review-recovery-alert-digest-release-gate-digest-alert-digest-alert-digest-alert-digest-alert-digest-alert-runbook.md` plus a bridge opener, release gate digest alert digest alert digest alert digest alert digest alert runbook presence metadata, release-readiness coverage, and safe target routing.
- Added generic key-value credential redaction for `token=`, `api_key=`, `secret=`, and `password=` diagnostics text.
- Added safe native promotion archive opening restricted to `native-promotion-status-*.json` file names.
- Added archive retention, blocker, redaction, next-surface, production-state, and rollback summaries to the bridge without reading terminal payloads.
- Added `dx-agents models failover-drill --mode mock|dry-run --json` for redacted provider fallback-order evidence.
- Added provider failover drill status export coverage for selected provider, selected model, failed providers, fallback readiness, and recovery hints without live secret exposure.
- Added native promotion archive diff summaries for latest-vs-previous blocker, retention, production-ready, next-surface, and rollback drift.
- Added native promotion archive diff rendering and status export coverage to the DX CLI Bridge without reading terminal payloads.
- Added clean, warning, and blocked native promotion archive drift alert severity with recovery hints.
- Added bridge rendering for native promotion archive drift alerts while keeping status exports diagnostic-only.
- Added metadata-only native promotion archive trend history with short, stable, improving, and worsening states.
- Added bridge rendering for archive trend state and compact trend points while keeping status exports payload-free.
- Added `docs/native-promotion-archive-trend-runbook.md` with diagnostic-only operator guidance for short, stable, improving, and worsening archive trends.
- Added native promotion archive trend runbook metadata to bridge diff summaries, release-readiness checks, and the bridge runbook opener.
- Added `dx-agents gateway pairing-drill --mode mock|dry-run --json` for redacted gateway pairing and channel allowlist readiness evidence.
- Added gateway pairing allowlist drill coverage to desktop bridge status exports and the DX CLI Bridge compact status panel without requiring live gateway mutation.
- Added `dx-agents cron delivery-drill --mode mock|dry-run --json` for redacted cron delivery recovery evidence across runnable, skipped, paused, missed, and continuation-like jobs.
- Added cron delivery recovery drill coverage to desktop bridge status exports and the DX CLI Bridge cron panel without executing or mutating schedules.
- Added desktop bridge IPC and rendering for gateway pairing allowlist drill checks, including paircode-required, allowlisted, denied, recovery-hint, and redaction states.
- Added `dx-agents tools safety-drill --mode mock|dry-run --json` for redacted tool configuration safety evidence across allowed, approval-required, denied, missing, and critical-blocker states.
- Added tool configuration safety drill coverage to desktop bridge status exports and the DX CLI Bridge tool-safety panel without invoking tools.

### Changed

- Root package, library, binary, ACP bridge, service install names, update source, config/env prefix, and high-visibility English CLI copy now prefer DX Agents naming.
- Legacy `zeroclaw`, `ZEROCLAW_*`, archived Agent, OpenClaw, and Hermes data paths remain readable for migration.
- Web auth storage now prefers `dx_agents_token` while still reading and writing `zeroclaw_token` for compatibility with existing dashboard sessions.
- `TODO.md` now marks the first core runtime parity batch as complete and opens Batch 2 for automation continuity and operator control-plane work.
- Batch 2 now completes the automation continuity/operator control-plane checklist and opens Batch 3 for desktop live-ops hardening.
- Batch 3 now completes desktop live-ops hardening and opens Batch 4 for provider/gateway runtime confidence work.
- Batch 4 now completes provider/gateway runtime confidence and opens Batch 5 for operator remediation and run-history work.
- The DX CLI Bridge now keeps Windows Terminal as the visible fallback until the generated embedded PTY contract reports production-ready gates.
- The DX CLI Bridge now keeps embedded session lifecycle operations gated until `embedded_pty.production_ready` is true.
- The DX CLI Bridge now keeps in-app terminal rendering gated until embedded renderer features are ready.
- The DX CLI Bridge now keeps embedded input forwarding gated until renderer readiness and required input events are enabled.
- The DX CLI Bridge now keeps embedded resize forwarding gated until renderer readiness and required resize events are enabled.
- The DX CLI Bridge now keeps embedded media sessions gated until renderer, input, resize, session lifecycle, and media-session events are enabled.
- The DX CLI Bridge now keeps embedded terminal production routing gated until the generated production-readiness rollup has no blockers.
- The DX CLI Bridge now treats embedded terminal evidence exports as diagnostic-only and leaves production routing gated.
- Batch 5 now completes operator remediation and run-history work and opens Batch 6 for runtime hardening and distribution polish.
- Batch 6 now starts runtime hardening with recovery hints in the desktop bridge result surfaces.
- Batch 6 now includes compact release-readiness checks in the desktop bridge.
- Batch 6 now includes safe redacted diagnostics export review from the desktop bridge.
- Batch 6 now includes desktop bridge operator documentation.
- Batch 6 now completes runtime hardening and distribution polish, and Batch 7 opens release packaging and operator QA.
- Batch 7 now starts with release packaging readiness coverage for desktop distribution inputs and expected output paths.
- Batch 7 now includes a command-level operator QA smoke checklist and docs navigation entry.
- Batch 7 now links operator QA and desktop bridge docs from release-readiness checks.
- Batch 7 now completes release packaging and operator QA without running a release build, and Batch 8 opens embedded terminal production-gate work.
- Batch 8 now starts by preserving first-class embedded terminal input and resize readiness fields from the generated host contract instead of relying only on dashboard fallbacks.
- Batch 8 now includes redacted metadata-only input and resize fixtures exposed through the desktop IPC inventory.
- Batch 8 now includes a desktop readiness export for embedded terminal production gates and redacted fixture evidence.
- Batch 8 now includes a safe in-app PTY session pilot plan with explicit promotion rules.
- Batch 8 is complete, and Batch 9 opens a controlled session canary and runtime pilot before any embedded terminal production routing.
- Batch 9 starts with a synthetic session timeline export before any controlled local echo-process pilot work.
- Batch 9 desktop diagnostics now read generated session timeline contract data while preserving synthetic-only routing.
- Batch 9 now includes a fixed-argument local echo-process pilot that skips execution when redaction or synthetic-session evidence is unsafe.
- Batch 9 now exposes synthetic session and echo-process lifecycle diagnostics in the DX CLI Bridge terminal surface panel.
- Batch 9 now documents promotion and rollback rules for each embedded terminal canary phase.
- Batch 9 is complete, and Batch 10 opens developer-only TUI canary guardrails and runtime evidence without changing production terminal routing.
- Batch 10 starts with a read-only TUI canary gate that does not register host actions or alter production terminal routing.
- Batch 10 now includes redacted TUI canary lifecycle evidence with timeouts, output caps, and rollback triggers before any real TUI process runner.
- Batch 10 now exposes TUI canary gate, lifecycle, and rollback diagnostics in the desktop bridge without changing production terminal actions.
- Batch 10 now documents repeated-run QA and rollback conditions for the developer TUI canary.
- Batch 10 is complete, and Batch 11 opens a fixed developer-only TUI canary runner and renderer evidence batch without enabling production routing.
- The DX CLI Bridge now reads source-owned echo-pilot readiness from `embedded_terminal_echo_pilot` before showing live echo-process results.
- Batch 11 now includes the fixed developer-only TUI canary runner and bounded stdout/stderr lifecycle capture while keeping production terminal routing disabled.
- Batch 11 now exposes fixed TUI canary runner output, renderer evidence snapshots, and rollback state in the desktop bridge without enabling production routing.
- Batch 11 is complete, and Batch 12 opens source-owned TUI runner contract integration for the next DX CLI host bundle milestone.
- Batch 12 now accepts only shell-free source-owned TUI runner metadata and falls back to the local fixed runner when the host contract drifts.
- Batch 12 is complete; production embedded terminal routing remains disabled and developer-gated.
- Batch 13 completes media canary/operator evidence export while keeping mpv, tplay, viu, and Windows Terminal as production fallbacks.
- Batch 14 completes stricter TUI runner contract drift hardening while keeping production embedded terminal routing disabled.
- Batch 15 opens source-owned TUI renderer evidence contract integration after the promoted runner contract verified cleanly.
- Batch 15 completes DX Agents-side source-owned TUI renderer evidence import while preserving local fallback snapshots and disabled production routing.
- The DX CLI Bridge now prefers the generated top-level `embedded_terminal_tui_canary_renderer_evidence` contract before local renderer-evidence fallback snapshots.
- Batch 16 opens dashboard branding and DX env alias compatibility while preserving old storage and API paths.
- The web dashboard package, Vite gateway env, base/gateway globals, pairing dialog, sidebar, logs, chat header, and settings preview now prefer DX Agents naming.
- Dashboard storage now writes DX Agents keys while keeping old ZeroClaw keys readable and writable for existing sessions.
- Dashboard compatibility now covers local storage, session storage, window globals, gateway env vars, unauthorized events, and WebSocket protocol aliases.
- Batch 16 completes dashboard branding and DX env alias compatibility while keeping legacy compatibility paths readable.
- Batch 17 opens dashboard compatibility contract promotion so the alias set can move from local dashboard constants into generated source-owned metadata.
- Batch 17 completes dashboard compatibility contract promotion; the bridge now prefers host-contract metadata and keeps local constants only as an older-contract fallback.
- Batch 18 completes dashboard compatibility status export and drift guard coverage.
- The gateway SPA fallback now injects both `window.__DX_AGENTS_BASE__` and `window.__ZEROCLAW_BASE__` for path-prefixed dashboards.
- Batch 19 completes the dashboard compatibility migration plan and keeps all legacy aliases active until a versioned cleanup note is approved.
- Batch 20 completes the dashboard compatibility command surface without adding duplicate bridge UI controls.
- Batch 21 completes dashboard compatibility decommission telemetry and keeps legacy aliases blocked until a zero-legacy-usage telemetry export is available.
- The dashboard compatibility migration gate now requires `decommission_ready: true` from the redacted usage telemetry before alias cleanup can proceed.
- The DX CLI Bridge now consumes `dx.native_promotion` status output directly before falling back to generated promotion-plan metadata.
- Bridge status exports now include `dx.native_promotion_archive` output so explicit diagnostics exports write and report retained redacted native-promotion snapshots.
- Batch 22 completes session tool routing and interrupt parity with a redacted source-owned CLI report plus bridge status export coverage.
- Batch 23 completes memory skill learning loop parity with a redacted command-level report and bridge status export coverage.
- Batch 24 completes provider failover drill evidence, Batch 25 completes native promotion archive diff summaries, Batch 26 completes archive drift alerts, and Batch 27 completes trend history across retained redacted snapshots.
- Batch 28 completes the native promotion archive trend runbook and opens gateway pairing allowlist drill evidence as the next parity batch.
- Batch 29 completes gateway pairing allowlist drill evidence and opens cron delivery recovery drill evidence as the next parity batch.
- Batch 30 completes cron delivery recovery drill evidence and opens tool configuration safety drill evidence as the next parity batch.
- Batch 31 completes tool configuration safety drill evidence and opens tool safety drill history and remediation flow as the next parity batch.
- Batch 32 completes tool safety drill history and remediation flow and opens tool safety history drift alerts as the next parity batch.
- Batch 33 completes tool safety history drift alerts and opens the tool safety alert runbook as the next parity batch.
- Batch 34 completes the tool safety alert runbook and opens the tool safety export audit bundle as the next parity batch.
- Batch 35 completes the tool safety export audit bundle and opens tool safety audit review history as the next parity batch.
- Batch 36 completes tool safety audit review history and opens the audit review runbook as the next parity batch.
- Batch 37 completes the tool safety audit review runbook and opens audit review digest work as the next parity batch.
- Batch 38 completes the tool safety audit review digest and opens audit review alerts as the next parity batch.
- Batch 39 completes tool safety audit review alerts and opens the audit review alert runbook as the next parity batch.
- Batch 40 completes the tool safety audit review alert runbook and opens alert escalation evidence as the next parity batch.
- Batch 41 completes tool safety audit review alert escalation evidence and opens escalation recovery drill work as the next parity batch.
- Batch 42 completes tool safety audit review escalation recovery drill work and opens the recovery drill runbook as the next parity batch.
- Batch 43 completes tool safety audit review recovery drill runbook work and opens recovery drill digest work as the next parity batch.
- Batch 44 completes tool safety audit review recovery drill digest work and opens recovery digest alert work as the next parity batch.
- Batch 45 completes tool safety audit review recovery digest alert work and opens the recovery alert runbook as the next parity batch.
- Batch 46 completes tool safety audit review recovery alert runbook work and opens recovery alert digest work as the next parity batch.
- Batch 47 completes tool safety audit review recovery alert digest work and opens recovery alert digest runbook work as the next parity batch.
- Batch 48 completes tool safety audit review recovery alert digest runbook work and opens recovery alert digest release gate work as the next parity batch.
- Batch 49 completes tool safety audit review recovery alert digest release gate work and opens recovery alert digest release gate runbook work as the next parity batch.
- Batch 50 completes tool safety audit review recovery alert digest release gate runbook work and opens recovery alert digest release gate digest work as the next parity batch.
- Batch 51 completes tool safety audit review recovery alert digest release gate digest work and opens recovery alert digest release gate digest runbook work as the next parity batch.
- Batch 52 completes tool safety audit review recovery alert digest release gate digest runbook work and opens recovery alert digest release gate digest alert work as the next parity batch.
- Batch 53 completes tool safety audit review recovery alert digest release gate digest alert work and opens recovery alert digest release gate digest alert runbook work as the next parity batch.
- Batch 54 completes tool safety audit review recovery alert digest release gate digest alert runbook work and opens recovery alert digest release gate digest alert digest work as the next parity batch.
- Batch 55 completes tool safety audit review recovery alert digest release gate digest alert digest work and opens recovery alert digest release gate digest alert digest runbook work as the next parity batch.
- Batch 56 completes tool safety audit review recovery alert digest release gate digest alert digest runbook work and opens recovery alert digest release gate digest alert digest alert work as the next parity batch.
- Batch 57 completes tool safety audit review recovery alert digest release gate digest alert digest alert work and opens recovery alert digest release gate digest alert digest alert runbook work as the next parity batch.
- Batch 58 completes tool safety audit review recovery alert digest release gate digest alert digest alert runbook work and opens recovery alert digest release gate digest alert digest alert digest work as the next parity batch.
- Batch 59 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest work and opens recovery alert digest release gate digest alert digest alert digest runbook work as the next parity batch.
- Batch 60 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest runbook work and opens recovery alert digest release gate digest alert digest alert digest alert work as the next parity batch.
- Batch 61 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert work and opens recovery alert digest release gate digest alert digest alert digest alert runbook work as the next parity batch.
- Batch 62 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert runbook work and opens recovery alert digest release gate digest alert digest alert digest alert digest work as the next parity batch.
- Batch 63 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest work and opens recovery alert digest release gate digest alert digest alert digest alert digest runbook work as the next parity batch.
- Batch 64 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest runbook work and opens recovery alert digest release gate digest alert digest alert digest alert digest alerts as the next parity batch.
- Batch 65 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alerts and opens recovery alert digest release gate digest alert digest alert digest alert digest alert runbook work as the next parity batch.
- Batch 66 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alert runbook work and opens recovery alert digest release gate digest alert digest alert digest alert digest alert digest work as the next parity batch.
- Batch 67 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest work and opens recovery alert digest release gate digest alert digest alert digest alert digest alert digest runbook work as the next parity batch.
- Batch 68 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest runbook work and opens recovery alert digest release gate digest alert digest alert digest alert digest alert digest alerts as the next parity batch.
- Batch 69 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest alerts and opens recovery alert digest release gate digest alert digest alert digest alert digest alert digest alert runbook work as the next parity batch.
- Batch 70 completes tool safety audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest alert runbook work and opens agent runtime parity productization as the next professional feature batch.
- Batch 71 completes agent runtime parity productization and opens trust-aware skill guard audit work as the next professional feature batch.
- Batch 72 starts trust-aware skill guard auditing with redacted package verdicts and risk counters.
- Batch 72 completes trust-aware skill guard auditing by feeding metadata-only audit readiness into `dx-agents parity skills`; Batch 73 opens session search summary work.
- Batch 73 completes session search summaries and opens multi-agent routing parity reporting.
- Batch 74 completes multi-agent routing parity reporting and opens durable delivery queue health preflight.
- Batch 75 completes durable delivery queue health preflight and opens the actual durable outbound queue store as the next production feature batch.
- Batch 76 completes the durable outbound queue store and opens remote skill node readiness as the next OpenClaw-inspired runtime parity batch.
- Batch 77 completes remote skill node readiness and opens managed tool gateway readiness as the next Hermes-inspired runtime parity batch.
- Batch 78 completes managed tool gateway readiness and opens channel/app capability readiness as the next OpenClaw/Hermes long-tail integration parity batch.
- Batch 79 completes channel/app capability readiness and opens remote execution promotion guardrails as the next production parity batch.
- Batch 80 completes remote execution promotion guardrails and opens remote execution canary evidence as the next production parity batch.
- Batch 81 completes remote execution canary evidence and opens canary history and drift as the next production parity batch.
- Batch 91 completes remote execution readiness bundle history and opens readiness history alerts as the next production parity batch.
- Batch 92 completes remote execution readiness history alerts and opens a readiness CI gate as the next production parity batch.
- Batch 93 completes the remote execution readiness CI gate and opens CI gate history as the next production parity batch.
- Batch 94 completes remote execution readiness CI gate history and opens CI history alerts as the next production parity batch.
- Batch 95 completes remote execution readiness CI history alerts and opens a compact CI digest as the next production parity batch.

### Verified
- `cargo test --bin dx-agents runtime_receipt_learning`, `cargo test --bin dx-agents report_covers_core_runtime_surfaces_without_secret_leaks`, `cargo test --bin dx-agents verifier_covers_reports_without_sensitive_leaks`, `cargo test --lib tool_execution_receipt`, `cargo fmt --check`, `git diff --check`, and `cargo check --all-targets` passed for the runtime execution receipt and learning reliability parity batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_operator_readiness_ci_gate_alerts -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_release_gate_readiness_ci_alerts_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution readiness CI history alerts batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_operator_readiness_ci_gate_history -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_release_gate_readiness_ci_history_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution readiness CI gate history batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_operator_readiness_ci_gate -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_release_gate_readiness_ci_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution readiness CI gate batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_operator_readiness_alerts -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_release_gate_readiness_alerts_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution readiness history alerts batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_canary_history -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_canary_history_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution canary history and drift batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_canary -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_canary_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution canary evidence batch.
- `cargo test -p dx-agents --bin dx-agents remote_execution_promotion -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_promotion_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote execution promotion guardrails batch.
- `cargo test -p dx-agents --bin dx-agents channel_app_readiness -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_channels_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the channel/app capability readiness batch.
- `cargo test -p dx-agents --bin dx-agents managed_tool_gateway_readiness -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_managed_tools_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the managed tool gateway readiness batch.
- `cargo test -p dx-agents --bin dx-agents remote_skill_readiness -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_remote_skills_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents skill_pack_readiness -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the remote skill node readiness batch.
- `cargo test -p zeroclaw-runtime delivery_queue -- --nocapture`, `cargo test -p zeroclaw-runtime persist_job_result_delivery_stubbed_succeeds -- --nocapture`, `cargo test -p dx-agents --bin dx-agents delivery_queue_health -- --nocapture`, `cargo test -p dx-agents --bin dx-agents runtime_preflight -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_delivery_queue_json_cli_parses -- --nocapture`, `cargo test -p dx-agents --bin dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the durable outbound queue store batch.
- `cargo test -p dx-agents delivery_queue_health -- --nocapture`, `cargo test -p dx-agents parity_delivery_queue_json_cli_parses -- --nocapture`, `cargo test -p dx-agents runtime_preflight -- --nocapture`, `cargo test -p dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the durable delivery queue health preflight batch.
- `cargo test -p dx-agents agent_routing_parity -- --nocapture`, `cargo test -p dx-agents parity_agents_json_cli_parses -- --nocapture`, `cargo test -p dx-agents parity_inventory -- --nocapture`, `cargo test -p dx-agents parity_verification -- --nocapture`, `cargo fmt --check`, `git diff --check`, and `cargo check -p dx-agents --bin dx-agents` passed for the multi-agent routing parity report batch.
- `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo test -p dx-agents-desktop tool_safety_audit_recovery_alert_digest_release_gate_digest_alert_digest_alert_digest_alert_digest_alert_digest -- --nocapture`, `cargo fmt --check`, `git diff --check`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest alerts batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alert digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest alerts batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest alert batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest alert batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest alert batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest release gate batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery alert runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery digest alert batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery digest batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review recovery runbook batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review escalation recovery drill batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review escalation evidence batch.
- `cargo fmt --check`, `git diff --check`, bridge script syntax via Node, `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`, `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`, `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`, `cargo check -p dx-agents-desktop`, and `cargo check -p dx-agents --bin dx-agents` passed for the audit review alert runbook batch.
- `cargo metadata --no-deps --format-version 1`
- `cargo fmt --package dx-agents-desktop -- --check`
- `cargo check -p dx-agents-desktop`
- `cargo run -p xtask --bin web -- check`
- Live Groq provider smoke with `llama-3.1-8b-instant`, using `GROQ_API_KEY` only from the local shell environment.
- `cargo check --all-targets`
- Targeted Rust tests from the core-parity batch, including updater, doctor, channel service-reload, and provider/mock-provider checks.
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents --bin dx-agents tui_canary_cli_parses_fixed_json_mode -- --nocapture`
- `cargo test -p dx-agents gateway_pairing_drill -- --nocapture`
- `cargo test -p dx-agents-desktop gateway_pairing_drill -- --nocapture`
- `npm run typecheck` from `web`
- `cargo test -p zeroclaw-gateway select_supported -- --nocapture`
- `cargo check -p zeroclaw-gateway`
- `cargo test -p dx-agents-desktop dashboard_compatibility -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary_runner -- --nocapture`
- `cargo run --quiet -- host-verify --host-root G:\Dx\agent --compact` from `G:\Cli` passed with `embedded_terminal_tui_canary_runner_valid=true` and the DX Agents runner-contract panel check green.
- `cargo test -p dx-agents-desktop embedded_terminal_media_canary -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `cargo run -p dx-agents --bin dx-agents -- tui-canary --json`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary_renderer_evidence -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary_renderer_evidence -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary_runner -- --nocapture`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary_runner -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents cron_preview -- --nocapture`
- `cargo test -p dx-agents-desktop provider_smoke -- --nocapture`
- `cargo test -p dx-agents-desktop cron_preview_args -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo test -p dx-agents --lib cron_history -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents-desktop`
- `cargo fmt --check`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo test -p dx-agents --lib cron_history -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents-desktop` after embedded terminal input diagnostics.
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `git diff --check`
- `cargo test -p dx-agents-desktop dashboard_compatibility_status -- --nocapture`
- `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`
- `cargo test -p dx-agents-desktop bridge_self_test_inventory_contains_live_ops_commands -- --nocapture`
- `cargo test -p zeroclaw-gateway base_prefix_script -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo check -p zeroclaw-gateway`
- `cargo check -p dx-agents-desktop` after embedded terminal resize diagnostics.
- `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo fmt --check`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents --bin dx-agents`
- `git diff --check`
- `npm run typecheck` from `web`
- `cargo test -p dx-agents --bin dx-agents dashboard_compatibility::tests -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop dashboard_compatibility_status -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- dashboard-compatibility --json` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents session_tool_routing -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents sessions_cli_parses_tool_routing_json -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop session_tool_routing_args_request_json_report -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- sessions tool-routing --json` with `CARGO_BUILD_JOBS=1`
- `cargo check -p dx-agents --bin dx-agents` with `CARGO_BUILD_JOBS=1`
- `cargo check -p dx-agents-desktop` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents memory_skill_learning -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents memory_cli_parses_learning_loop_json -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop memory_skill_learning_args_request_json_report -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- memory learning-loop --json` with `CARGO_BUILD_JOBS=1`
- `cargo check -p dx-agents --bin dx-agents` with `CARGO_BUILD_JOBS=1`
- `cargo check -p dx-agents-desktop` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop host_contract_value_preserves_embedded_terminal_protocol_fields -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents dashboard_compatibility::tests -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents dashboard_compatibility_json_cli_parses -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- dashboard-compatibility --json` with `CARGO_BUILD_JOBS=1`
- `cargo check -p dx-agents-desktop`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop embedded_terminal_fixtures_cover_required_events_without_payloads -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo check -p dx-agents-desktop` after embedded terminal production-readiness diagnostics.
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo check -p dx-agents-desktop` after embedded terminal evidence export diagnostics.
- `cargo test -p dx-agents-desktop embedded_terminal_readiness_export_stays_redacted_and_gated -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `git diff --check`
- `cargo fmt --check`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop dx_cli -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents --bin dx-agents`
- `git diff --check`
- `cargo fmt --package dx-agents-desktop`
- `cargo test -p dx-agents-desktop embedded_terminal_session_timeline_is_synthetic_and_redacted -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo check -p dx-agents-desktop` after synthetic session timeline bridge diagnostics.
- `cargo check -p dx-agents-desktop`
- `git diff --check`
- `cargo test -p dx-agents --bin dx-agents echo_pilot_cli_parses_fixed_json_mode -- --nocapture`
- `cargo test -p dx-agents-desktop embedded_terminal_echo_pilot -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `git diff --check`
- `git diff --check`
- `cargo fmt --check`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents --bin dx-agents echo_pilot_cli_parses_fixed_json_mode -- --nocapture`
- `cargo test -p dx-agents-desktop embedded_terminal -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `git diff --check`
- `cargo fmt --package dx-agents-desktop`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `git diff --check`
- `cargo fmt --package dx-agents-desktop`
- `cargo test -p dx-agents-desktop embedded_terminal_tui_canary -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `git diff --check`
- `git diff --check`
- `cargo fmt --check`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop embedded_terminal -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents --bin dx-agents`
- `git diff --check`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop native_promotion_archive_status_is_schema_safe -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop native_promotion_archive -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop native_promotion_archive -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop native_promotion_archive -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents --bin dx-agents provider_failover_drill -- --nocapture`
- `cargo test -p dx-agents-desktop provider_failover_drill -- --nocapture`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo test -p dx-agents-desktop native_promotion_archive -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const match=html.match(/<script>([\s\S]*)<\/script>/); if(!match) throw new Error('script tag not found'); new Function(match[1]); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop native_promotion_archive_trend_runbook -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents --lib gateway_pairing_drill -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents gateway_pairing_drill_cli_parses_mock_json_mode -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop gateway_pairing_drill_args_request_redacted_dry_run_json -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- gateway pairing-drill --mode dry-run --json` with `CARGO_BUILD_JOBS=1`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents-desktop bridge_self_test_inventory_contains_live_ops_commands -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --lib cron_delivery_drill -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents --bin dx-agents cron_delivery_drill_cli_parses_mock_json_mode -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo test -p dx-agents-desktop cron_delivery_drill_args_request_redacted_dry_run_json -- --nocapture` with `CARGO_BUILD_JOBS=1`
- `cargo run -p dx-agents --bin dx-agents -- cron delivery-drill --mode dry-run --json` with `CARGO_BUILD_JOBS=1`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); if(!m){throw new Error('missing script')}; new Function(m[1]); console.log('bridge.html script syntax ok');"`
- `cargo test -p dx-agents --lib tool_safety_drill -- --nocapture`
- `cargo test -p dx-agents --bin dx-agents tools_safety_drill_cli_parses_mock_json_mode -- --nocapture`
- `cargo test -p dx-agents-desktop tool_safety_drill_args_request_redacted_dry_run_json -- --nocapture`
- `cargo run -p dx-agents --bin dx-agents -- tools safety-drill --mode dry-run --json`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo check -p dx-agents-desktop`
- `cargo fmt --check`
- `git diff --check`
- `cargo fmt`
- `cargo test -p dx-agents-desktop tool_safety -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]); for (const script of scripts) new Function(script); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo fmt --check`
- `git diff --check`
- `cargo fmt`
- `cargo test -p dx-agents-desktop tool_safety -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]); for (const script of scripts) new Function(script); console.log('bridge script syntax ok');"`
- `cargo fmt --check`
- `git diff --check`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`
- `cargo test -p dx-agents-desktop bridge_self_test_inventory_contains_live_ops_commands -- --nocapture`
- `node --% -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/gi)].map((m)=>m[1]); for (const script of scripts) new Function(script); console.log('bridge script syntax ok');"`
- `cargo check -p dx-agents-desktop`
- `cargo check -p dx-agents --bin dx-agents`
- `cargo test -p dx-agents-desktop tool_safety -- --nocapture`
- `cargo test -p dx-agents-desktop continuation_doc_targets -- --nocapture`
- `cargo test -p dx-agents-desktop release_readiness_report_scores_local_release_inputs -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]); for (const script of scripts) new Function(script); console.log('bridge script syntax ok');"`
- `cargo fmt --check`
- `git diff --check`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`
- `cargo check -p dx-agents-desktop`
- `cargo test -p dx-agents-desktop tool_safety_audit -- --nocapture`
- `node -e "const fs=require('fs'); const html=fs.readFileSync('apps/tauri/onboarding/bridge.html','utf8'); const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]); for (const script of scripts) new Function(script); console.log('bridge script syntax ok');"`
- `cargo fmt --check`
- `git diff --check`
- `cargo check -p dx-agents-desktop`

### Deferred

- Broad generated docs, translated docs, firmware names, and internal crate renames.

## Upstream History

The ZeroClaw upstream changelog for the fork base remains in `CHANGELOG-next.md`.
