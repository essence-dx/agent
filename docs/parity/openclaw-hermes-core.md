# DX Agents Core Parity: OpenClaw and Hermes

This matrix records the first DX Agents batch after cloning the latest upstream references into `G:\Dx\inspirations`.

For the current Batch 71 implementation backlog, see `docs/parity/openclaw-hermes-gap-review.md`.

## Source References

| Reference | Local path | How it is used |
| --- | --- | --- |
| Latest DX Agent | `G:\Dx\inspirations\zeroclaw` | Primary Rust upstream for `dx-agents`. |
| Archived custom Agent fork | `G:\Dx\inspirations\agent-archive` | Migration source for previous CLI/UI, provider, memory, gateway, cron, and skills behavior. |
| OpenClaw | `G:\Dx\inspirations\openclaw` | Feature reference for sessions, pairing, control-plane behavior, skill layout, and multi-agent routing concepts. |
| Hermes Agent | `G:\Dx\inspirations\hermes-agent` | Feature reference for slash-command flow, memory/skills learning loops, cron delivery, provider switching, and interruption semantics. |

## Implemented Or Preserved In This Batch

| Area | DX Agents status | Notes |
| --- | --- | --- |
| Package and CLI branding | Implemented | Root Cargo package is `dx-agents`, library is `dx_agents`, primary binary is `dx-agents`, ACP bridge binary is `dx-agents-acp-bridge`. High-visibility English CLI help, daemon, interactive mode, doctor, channel, service, cron, provider-auth, desktop companion app, and config-reference prompts now use DX Agents names while legacy readers remain. |
| Config and env prefix | Implemented | `DX_AGENTS_*` is the preferred prefix. Legacy `DX_AGENT_*` readers remain for migration. |
| Provider registry | Preserved and extended | Upstream Rust provider registry already includes Groq, OpenAI-compatible providers, OpenRouter, Anthropic, OpenAI, Gemini, Ollama, and many long-tail providers. |
| Groq support | Preserved | Groq has provider-specific credential resolution through `GROQ_API_KEY`; `DX_AGENTS_API_KEY` is now the generic fallback. |
| Provider failover/model profiles | Preserved and extended | Existing provider fallback/profile machinery is retained. DX env aliases can override provider/model/timeout/headers. `dx-agents models status/set/list/refresh` now exposes provider/model switching and catalog checks from the CLI. |
| Sessions and ACP | Preserved and extended | ACP bridge keeps gateway token cache behavior, now preferring `DX_AGENTS_ACP_*` env vars with legacy fallback. `dx-agents sessions` exposes list/search/running/stuck/history/state/rename/clear/delete operations over the SQLite session backend. It can abort one in-flight gateway session, abort all stuck gateway sessions in a guarded batch, and resolve stale local state for OpenClaw/Hermes-style operator recovery. |
| Session replay readiness | Implemented | `dx-agents sessions replay-readiness --mode <mock\|dry-run> --json` connects session tool routing, interruption semantics, child-session lineage, local recovery, and tool receipt evidence without exporting transcript text, prompts, queries, tool arguments/results, provider secrets, database paths, workspace paths, or payload fingerprints. |
| Tool execution receipts | Implemented | `dx-agents tools receipts --mode <mock\|dry-run> --json`, `receipt-history`, and `receipt-alerts` provide metadata-only execution receipts, retained snapshots, and drift alerts for approval state, result state, retry posture, slow/stale evidence, and payload-policy regressions without invoking tools or exporting prompts, commands, payloads, provider values, paths, or secrets. |
| Memory/search | Preserved and extended | SQLite memory, memory tools, and migration surfaces are retained from upstream. Migration now detects `memory/brain.db`, root `memory.db`, and `data/memory.db` source layouts. |
| Memory learning receipts | Implemented | `dx-agents memory learning-receipts --mode <mock\|dry-run> --json` reports accepted or safely skipped memory write/search/reindex/skill-hook receipts, injection-scan decisions, and rollback metadata without exporting memory bodies, query text, memory keys, skill file bodies, secrets, database paths, or rollback payloads. |
| Runtime receipt readiness bundle | Implemented | `dx-agents parity runtime-receipt-readiness --json` combines tool receipts, retained receipt history, receipt alerts, session replay readiness, memory learning receipts, parity inventory, and parity verifier state into one compact metadata-only operator bundle with seven source signals, readiness state, score, source schemas, recovery hints, and closed redaction flags. `dx-agents parity runtime-receipt-readiness-history --archive --json` retains payload-free snapshots, and `dx-agents parity runtime-receipt-readiness-alerts --json` blocks on receipt, replay, memory-learning, verifier, and payload-policy regressions. |
| Runtime receipt CI gate | Implemented | `dx-agents parity runtime-receipt-readiness-ci --json` combines the runtime receipt readiness bundle, retained readiness history, and readiness alerts into one strict CI-ready metadata report. It recommends exit codes, stays non-failing by default, requires explicit `--fail-on-non-clear` before exiting nonzero, keeps live tool execution disabled, avoids archive writes, and blocks payload-policy regressions without exporting prompts, command payloads, tool payloads, transcripts, memory bodies, query text, memory keys, provider values, database paths, workspace paths, or secrets. `dx-agents parity runtime-receipt-readiness-ci-history --archive --json` retains metadata-only CI gate snapshots for exit-code drift, strict-mode state, live-execution regressions, freshness, and payload-policy drift, while `dx-agents parity runtime-receipt-readiness-ci-alerts --json` turns that history into warning/blocker alerts for promotion review. `dx-agents parity runtime-receipt-readiness-ci-promotion --json` then combines readiness, retained readiness history, readiness alerts, CI gate, retained CI gate history, CI gate alerts, inventory, and verifier state into one metadata-only promotion handoff with safe archive/remediation commands and closed payload redaction. `dx-agents parity runtime-receipt-readiness-ci-promotion-history --archive --json` retains payload-free promotion handoff snapshots for repeated-run drift review, `dx-agents parity runtime-receipt-readiness-ci-promotion-alerts --json` turns that retained evidence into warning/blocker alerts, and `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet --json` combines handoff, retained evidence, alerts, inventory, verifier, runbooks, and safe next actions into one final metadata-only operator packet. `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet-history --archive --json` now retains payload-free operator packet snapshots for repeated signoff drift review, `dx-agents parity runtime-receipt-readiness-ci-promotion-operator-packet-history-alerts --json` turns that retained packet evidence into warning/blocker alerts for release signoff, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff --json` exports the final non-failing-by-default signoff packet, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff-history --archive --json` retains payload-free release signoff snapshots for strict-gate drift review, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-signoff-history-alerts --json` turns that retained signoff evidence into warning/blocker alerts, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate --json` provides the strict release gate that exits nonzero only with explicit `--fail-on-non-clear`, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate-history --archive --json` retains payload-free strict release gate snapshots for repeated operator-packet drift review, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-gate-history-alerts --json` turns that retained gate evidence into warning/blocker alerts, and `dx-agents parity runtime-receipt-readiness-ci-promotion-release-operator-packet --json` combines the strict release gate, retained gate history, gate alerts, inventory, verifier, runbooks, safe actions, required archive guidance, and explicit strict CI command into one final release handoff. `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum --json` now requires a clear release operator packet, repeated clear release gate evidence, clear release gate alerts, passing inventory, passing verifier, strict-command availability, and closed redaction before future release automation handoff, and `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum-history --archive --json` retains payload-free quorum snapshots for release handoff drift review across source states, strict-command availability, signal and section stability, freshness, and payload-policy regressions, and `dx-agents parity runtime-receipt-readiness-ci-promotion-release-evidence-quorum-history-alerts --json` turns that retained quorum evidence into warning/blocker alerts for release automation review. `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-handoff-dry-run --json` now converts quorum evidence into a non-executing metadata-only handoff packet with `effective_exit_code=0`, disabled writes, dry-run-only operator actions, and safe next commands. `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet --json`, `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet-history --archive --json`, and `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-packet-history-alerts --json` now add the metadata-only operator handoff packet, retained packet drift review, and warning/blocker alerts for release automation handoff without enabling release automation execution or exporting payloads. Inventory and verifier coverage now include the promotion history, alerts, operator packet, operator packet history, operator packet history alert, release signoff, release signoff history, release signoff alerts, strict release gate, retained release gate history, release gate alerts, release operator packet, release evidence quorum, retained quorum history, quorum alert surfaces, release automation handoff dry-run, retained dry-run history, dry-run history alerts, release automation operator handoff packet, retained operator handoff packet history, operator handoff packet history alerts, release automation operator signoff, retained operator signoff history, and operator signoff history alerts. |
| Runtime receipt CI release automation handoff verification record | Implemented | `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-handoff-verification-record --json` emits a focused metadata-only verification record for release automation operator handoff behavior, retained packet history, packet history alerts, inventory surface coverage, verifier coverage contracts, CLI parser coverage, non-execution guarantees, safe lightweight commands, and payload-free redaction without executing release automation. |
| Runtime receipt CI release automation operator signoff | Implemented | `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff --json` requires a clear operator handoff packet, retained packet history, clear packet alerts, focused verification-record coverage, inventory/verifier readiness, parity runbooks, non-execution posture, and closed payload redaction while keeping release automation execution disabled, writes disabled, and `effective_exit_code=0`. |
| Runtime receipt CI release automation operator signoff history | Implemented | `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff-history --archive --json` retains metadata-only release automation operator signoff snapshots for signoff state, verification-record state, packet/history/alert states, inventory/verifier states, non-execution posture, action/item drift, command guidance, freshness, and payload-policy regressions without exporting payloads or executing release automation. |
| Runtime receipt CI release automation operator signoff history alerts | Implemented | `dx-agents parity runtime-receipt-readiness-ci-promotion-release-automation-operator-signoff-history-alerts --json` turns retained release automation operator signoff history into metadata-only warning/blocker alerts for empty/single/stale evidence, signoff regressions, verification-record regressions, packet/history/alert regressions, inventory/verifier regressions, unsafe-execution drift, operator-action drift, signoff-item regressions, command-guidance drift, and payload-policy regressions. |
| Gateway pairing | Preserved and extended | Gateway pairing commands and ACP bridge guidance now use `dx-agents`. `dx-agents gateway status` reports live gateway health, pairing-required state, and paired-client state from the running control plane. |
| Channel allowlists | Extended | `dx-agents channel allowlist list/add/remove` manages common allowlist fields across Telegram, Discord, Slack, Matrix users/rooms, WhatsApp, iMessage, LINE, Lark, Feishu, and other configured channel surfaces. `bind-telegram` remains as a compatibility alias. |
| Cron scheduler | Preserved | Existing cron command and delivery registration remain in Rust. |
| Skills and skillforge hooks | Preserved | Existing skills/open-skills settings remain, now with `DX_AGENTS_OPEN_SKILLS_*` aliases. |
| Slash-command flow | Extended | Interactive mode now centralizes slash parsing and supports `/help`, `/status`, `/model`, `/provider`, `/clear`, `/quit`, and existing `/think:<level>` directives. Runtime provider/model switches now resolve the selected provider profile's own API key and base URL instead of reusing the previous fallback profile. |
| Migration readers | Preserved and extended | `dx-agents migrate openclaw`, `agent`, `zeroclaw`, and `hermes` now share the Rust importer. OpenClaw/Agent/DX Agent import SQLite/markdown memory layouts; Hermes imports `state.db` session messages as conversation memory plus markdown memory where present. |
| Self-update source | Implemented | `dx-agents update` now targets `millercarla211-ctrl/dx-agents` by default, accepts `DX_AGENTS_UPDATE_REPO` for release testing, and recognizes `dx-agents-*` release assets while allowing legacy `zeroclaw-*` assets during migration. |
| Service packaging names | Implemented | New service installs use `dx-agents.service`, `com.dx-agents.daemon`, `DX Agents Daemon`, `/etc/dx-agents`, `/var/log/dx-agents`, `/var/lib/dx-agents`, and `dx-agents` OpenRC service/user names. Channel allowlist reload now targets the DX Agents managed service. |
| Isolated checks | Implemented and expanding | Targeted checks cover provider config, env aliases, CLI parsing, gateway pairing, migration surfaces, focused `dx-agents agent -a <alias> --model-provider ... --model ... --message ...` live smoke, provider/model JSON discovery, and a deterministic mock OpenAI-compatible provider request/response test. |
| Receipt/replay/learning parity gates | Implemented | `dx-agents parity inventory --json` now includes tool receipts, retained receipt history, receipt alerts, session replay readiness, and memory learning receipts. `dx-agents parity verify --json` checks their schemas, required receipt/surface/alert coverage, and closed redaction flags. |
| Runtime receipt operator gate | Implemented | `dx-agents parity runtime-receipt-readiness --json`, retained bundle history, alert checks, `dx-agents parity runtime-receipt-readiness-ci --json`, retained CI gate history, CI gate alerts, and `dx-agents parity runtime-receipt-readiness-ci-promotion --json` are implemented. `dx-agents parity inventory --json` and `dx-agents parity verify --json` cover the full bundle-history-alert-CI-history-alert-promotion chain through verification-safe collectors that avoid recursive inventory/verifier calls. |
| Parity collector recursion safety | Implemented | `parity_inventory` and `parity_verification` now use collection-depth guards. Nested transitive calls return a bounded metadata-only guard report with closed redaction flags, safe diagnostic evidence, and no workspace paths or secret values. Default tests now prove inventory surface wiring and verifier check wiring through bounded source-contract checks, while the full recursive collector smoke remains available as an ignored manual diagnostic. `bounded_parity_collector_diagnostics` appears in inventory and verifier output to explain the guard posture, ignored full-smoke posture, bounded contract-test names, and closed path/secret exports. |
| Parity collector runtime budgets | Implemented and expanding | `parity_inventory` adds metadata-only timing evidence to every top-level surface using duration buckets, budget threshold metadata, and budget state. Over-budget surfaces become warning surfaces with `collection_budget_action=isolate_surface_for_followup` and `collection_budget_policy=metadata_only_warning`. Timeout-safe skipped surfaces use `collection_budget_state=skipped_surface`, `collection_budget_action=skip_surface_for_followup`, and `collection_skip_reason=timeout_safe_diagnostic`. `parity_verification` includes `inventory_runtime_budget_policy` and reports `slow_surface_count` separately from `skipped_surface_count`, requiring metadata-only isolation diagnostics before either posture is treated as handled. `dx-agents parity runtime-budget-history --archive --json` retains metadata-only runtime budget snapshots and compares slow/skipped surface posture, missing isolation diagnostics, duration bucket drift, stale evidence, and payload-policy regressions. `dx-agents parity runtime-budget-history-alerts --json` reports empty/single/stale evidence, slow/skipped regressions, duration bucket regressions, missing isolation diagnostics, payload-policy regressions, current slow/skipped posture, top alert routing, and safe archive guidance. `dx-agents parity runtime-budget-remediation --json` now turns current slow/skipped posture, missing-isolation posture, the top budget alert, safe focused commands, archive guidance, runbook guidance, and payload-free policy into one non-executing metadata-only operator packet with `effective_exit_code=0`, disabled writes, and closed redaction. `dx-agents parity runtime-budget-remediation-history --archive --json` retains remediation packet snapshots for drift review, and `dx-agents parity runtime-budget-remediation-history-alerts --json` reports stale evidence, unresolved alert drift, unsafe command drift, missing archive cadence, non-execution regressions, operator-action drift, section regressions, command-guidance drift, and payload-policy regressions. `dx-agents parity runtime-budget-ci --json` adds a non-failing-by-default CI gate over remediation, retained remediation history, and remediation alerts; it recommends exit codes, keeps `effective_exit_code=0` unless `--fail-on-non-clear` is explicitly set, disables live tool execution and archive writes, and keeps payload export closed. `dx-agents parity runtime-budget-ci-history --archive --json` retains metadata-only CI gate snapshots for repeated evidence of recommended/effective exit-code posture, strict opt-in state, non-failing default policy, live-execution policy, archive-write policy, gate-count drift, payload-policy drift, and freshness before promotion handoff. `dx-agents parity runtime-budget-ci-alerts --json` reports empty/single/stale CI evidence, exit-code regressions, strict-mode drift, non-failing default regressions, live-execution regressions, archive-write regressions, gate-count drift, command-guidance drift, payload-policy regressions, top alert routing, and stable-history posture before promotion handoff. `dx-agents parity runtime-budget-ci-promotion --json` now combines the safe-default CI gate, retained CI history, CI alerts, remediation packet, retained remediation history, remediation alerts, inventory, and verifier state into one metadata-only promotion handoff with dependency statuses, top blocker/alert routing, archive guidance, verification-safe collectors, and non-executing defaults. `dx-agents parity runtime-budget-ci-promotion-history --archive --json` now retains metadata-only promotion handoff snapshots for repeated evidence of dependency posture, top blocker/alert routing, archive command guidance, recommended/effective exit-code posture, non-execution policy, payload-policy drift, and freshness before operator-packet signoff. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now cover runtime budget history, runtime budget alerts, remediation, retained remediation history, remediation alerts, the CI gate, retained CI gate history, CI gate alerts, and the CI promotion handoff with schema, required surface/alert/gate/dependency, and redaction checks without exporting workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets. |
| Self-test slices | Implemented | `dx-agents self-test --only <check>` can run focused checks such as `sqlite`, `providers`, or `memory` without forcing the whole suite. `--quick` rejects selected runtime checks that may touch gateway or memory backends. |

## Current Runtime Budget CI Promotion Notes

`dx-agents parity runtime-budget-ci-promotion-alerts --json` now turns retained runtime budget CI promotion handoff history into metadata-only warning/blocker alerts for empty/single/stale evidence, handoff-state regressions, recommended/effective exit-code regressions, dependency regressions, top blocker/alert drift, archive-command drift, command-guidance drift, non-execution regressions, payload-policy regressions, and stable-history posture before the final operator packet. `dx-agents parity runtime-budget-ci-promotion-operator-packet --json` combines the current promotion handoff, retained handoff history, handoff alerts, parity inventory, parity verifier, runbook coverage, safe operator actions, required archive command, latest safe command, recommended exit-code posture, and closed redaction into one metadata-only operator handoff. `dx-agents parity runtime-budget-ci-promotion-operator-packet-history --archive --json` now retains metadata-only operator packet snapshots for repeated evidence of packet state, source-state posture, recommended exit-code posture, section/action/runbook stability, command guidance, top blocker/alert routing, payload-policy drift, and freshness before signoff. `dx-agents parity runtime-budget-ci-promotion-operator-packet-history-alerts --json` now turns retained operator packet snapshots into metadata-only warning/blocker alerts for empty/single/stale history, packet-state regressions, recommended exit-code regressions, source-state regressions, section/action/runbook drift, command-guidance drift, top-alert regressions, non-execution regressions, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-signoff --json` now combines the current operator packet, retained packet history, packet-history alerts, inventory/verifier posture, runbook coverage, safe operator actions, non-failing default policy, strict opt-in command, and closed runtime-budget redaction into a metadata-only release signoff packet. `dx-agents parity runtime-budget-ci-promotion-release-signoff-history --archive --json` now retains metadata-only release signoff snapshots for signoff state, recommended/effective exit-code posture, non-failing default policy, source-state posture, item/action/runbook stability, command guidance, payload-policy drift, and freshness before the strict release gate. `dx-agents parity runtime-budget-ci-promotion-release-signoff-history-alerts --json` now turns retained release signoff snapshots into warning/blocker alerts for empty/single/stale evidence, signoff regressions, exit-code regressions, default-policy regressions, source-state regressions, parity coverage regressions, item/action/runbook drift, command-guidance drift, and payload-policy regressions. `dx-agents parity runtime-budget-ci-promotion-release-gate --json` now provides a metadata-only release gate over signoff and signoff-history alerts, recommends exit codes, remains non-failing by default, and exits nonzero only when `--fail-on-non-clear` is explicitly used. `dx-agents parity runtime-budget-ci-promotion-release-gate-history --archive --json` now retains metadata-only release-gate snapshots for repeated gate-state, exit-code, strict-mode, live-execution, archive-write, command-guidance, payload-policy, and freshness review. `dx-agents parity runtime-budget-ci-promotion-release-gate-history-alerts --json` now turns retained release-gate evidence into warning/blocker alerts for empty/single/stale evidence, gate regressions, exit-code regressions, strict-mode drift, live-execution regressions, archive-write regressions, command-guidance drift, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-operator-packet --json` now combines the safe-default release gate, retained gate history, gate alerts, parity inventory, parity verifier, runbook coverage, required archive guidance, safe operator actions, explicit strict command guidance, and closed runtime-budget redaction into one non-executing metadata-only release handoff with `effective_exit_code=0`. `dx-agents parity runtime-budget-ci-promotion-release-operator-packet-history --archive --json` now retains metadata-only release operator packet snapshots for repeated evidence of packet state, recommended/effective exit-code posture, release-gate source states, inventory/verifier posture, section/action/runbook stability, command guidance, strict-command drift, top blocker/alert routing, payload-policy drift, and freshness before evidence quorum. `dx-agents parity runtime-budget-ci-promotion-release-operator-packet-history-alerts --json` now turns retained release operator packet history into metadata-only warning/blocker alerts for empty/single/stale evidence, packet-state regressions, recommended/effective exit-code regressions, release-gate source-state regressions, inventory/verifier regressions, section/action/runbook drift, command-guidance drift, top-alert drift, non-execution regressions, payload-policy regressions, and stable-history posture before evidence quorum. `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum --json` now requires a clear release operator packet, repeated clear retained release-gate evidence, clear gate alerts, inventory/verifier readiness, strict command availability, disabled writes, and closed runtime-budget redaction before future handoff automation. `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum-history --archive --json` now retains metadata-only release evidence quorum snapshots for quorum state, source states, repeated-clear evidence, strict-command availability, signal/section stability, command guidance, write-policy posture, payload-policy drift, and freshness, while `dx-agents parity runtime-budget-ci-promotion-release-evidence-quorum-history-alerts --json` turns retained quorum evidence into warning/blocker alerts before handoff automation. `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run --json` now converts quorum evidence into a non-executing metadata-only handoff packet with `effective_exit_code=0`, disabled writes, dry-run-only operator actions, and safe next commands. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now cover the retained promotion history, promotion alerts, operator packet, retained operator packet history, operator packet history alerts, release signoff, release signoff history, signoff history alerts, release gate, release-gate history, and release-gate history alerts with schema, required surface/gate/alert/section/action/item coverage, and redaction checks. These reports export only states, counts, command names, drift booleans, redaction booleans, action ids, and recovery hints; they do not export workspace paths, provider values, payloads, memory content, skill bodies, browser storage values, or secrets.

Batch 150 extends parity inventory and verifier coverage through the release automation handoff dry-run. Inventory now exposes the release operator packet, retained operator packet history, operator packet alerts, release evidence quorum, retained quorum history, quorum alerts, and handoff dry-run as first-class metadata-only surfaces. The verifier checks their schemas, required section/action/surface/signal/alert ids, and closed redaction flags without executing release automation or exporting payloads.

Batch 151 starts the runtime budget release automation operator handoff lane with `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run-history --archive --json`. The retained history stores payload-free snapshots for handoff state, quorum/history/alert source states, inventory/verifier posture, strict-command availability, non-execution and write posture, exit-code posture, operator-action stability, section stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-handoff-dry-run` without executing release automation. `dx-agents parity runtime-budget-ci-promotion-release-automation-handoff-dry-run-history-alerts --json` now turns that retained evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, handoff/source regressions, strict-command drift, unsafe-execution drift, operator-action drift, section regressions, command-guidance drift, payload-policy regressions, and stable-history posture before the operator handoff packet. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet --json` then combines the current dry-run, retained dry-run history, dry-run alerts, inventory/verifier posture, strict command guidance, archive guidance, runbook coverage, safe operator actions, non-execution policy, and closed redaction into one payload-free packet with release automation and writes disabled. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet-history --archive --json` retains payload-free operator packet snapshots under `target/runtime-budget-ci-promotion-release-automation-operator-handoff-packet`, and `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-packet-history-alerts --json` turns those snapshots into warning/blocker alerts for packet/source regressions, strict-command drift, unsafe-execution drift, action/section drift, command-guidance drift, payload-policy regressions, freshness, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-handoff-verification-record --json` now adds the focused metadata-only verification record for dry-run behavior, retained packet evidence, packet-history alerts, inventory coverage, verifier coverage contracts, CLI parser coverage, non-execution guarantees, safe lightweight commands, and payload-free redaction. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff --json` now adds the metadata-only operator signoff packet requiring clear handoff packet evidence, stable retained packet history, clear packet alerts, focused verification-record coverage, inventory/verifier readiness, runbook coverage, safe non-executing operator actions, and closed redaction. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the full Batch 151 chain through the operator signoff with schema, surface, action, alert, check, command, item, and closed-redaction coverage.

Batch 152 starts the retained operator signoff lane with `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-history --archive --json`. The retained history stores payload-free snapshots for signoff state, verification-record state, packet/history/alert source states, inventory/verifier posture, non-execution and write posture, exit-code posture, action/item stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-operator-signoff` without executing release automation. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-history-alerts --json` now turns that retained evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, signoff regressions, verification-record regressions, packet/history/alert regressions, inventory/verifier regressions, unsafe-execution drift, operator-action drift, signoff-item regressions, command-guidance drift, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate --json` now promotes the operator signoff into an explicit non-failing-by-default strict gate that consumes the signoff packet plus retained signoff-history alerts, recommends exit codes, keeps `effective_exit_code=0` by default, and exits nonzero only with `--fail-on-non-clear` while keeping release automation execution and writes disabled. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the retained signoff history, signoff-history alerts, and strict operator signoff gate with schema, surface, alert, gate, exit-code, non-execution, and closed-redaction coverage before any release automation execution path is allowed.

Batch 153 starts strict operator signoff gate retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate-history --archive --json`. The retained history stores payload-free snapshots for strict operator signoff gate state, signoff source state, retained signoff-history alert state, recommended and effective exit-code posture, explicit strict-mode posture, non-failing default posture, disabled release automation execution and archive-write posture, gate-count drift, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-operator-signoff-gate` without executing release automation or exporting prompt, command, tool, transcript, memory, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-signoff-gate-history-alerts --json` now turns that retained strict gate evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, gate-state regressions, recommended/effective exit-code regressions, strict-mode drift, unsafe-execution and archive-write drift, command-guidance drift, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet --json` now combines the strict operator signoff gate, retained gate history, gate alerts, parity inventory/verifier posture, runbooks, safe operator actions, required archive guidance, strict opt-in command guidance, non-failing default exit behavior, and closed redaction into the final metadata-only release automation operator packet. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now cover the retained strict gate history, gate-history alerts, and final operator packet with schema, required surface/alert/section/action coverage, source-contract tests, non-execution posture, and closed redaction checks.

Batch 154 starts retained final operator packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet-history --archive --json`. The retained history stores payload-free snapshots for final packet state, strict signoff gate/history/alert source states, inventory/verifier posture, recommended and effective exit-code posture, section/action/runbook stability, strict-command guidance, non-execution policy, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-operator-packet` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-operator-packet-history-alerts --json` now turns that retained final packet evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, packet-state drift, strict gate source drift, inventory/verifier drift, exit-code regressions, section/action/runbook drift, command-guidance drift, top routing drift, unsafe execution/write posture, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum --json` now requires a clear final operator packet, stable retained packet history, clear packet-history alerts, inventory/verifier readiness, runbook coverage, explicit strict operator signoff gate command availability, disabled execution/write posture, and closed redaction before any future release automation execution handoff is allowed. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the retained final packet history, packet-history alerts, and evidence quorum with schema, surface, alert, signal, section, source-contract, and closed-redaction coverage.

Batch 155 starts release automation evidence quorum retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum-history --archive --json`. The retained history stores payload-free snapshots for quorum state, final operator packet/history/alert source states, inventory/verifier posture, stable retained packet evidence, strict-command availability, non-execution and write posture, signal/section stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-evidence-quorum` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-evidence-quorum-history-alerts --json` now turns that retained quorum evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, quorum regressions, final packet/history/alert source regressions, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, signal/section drift, command-guidance drift, payload-policy regressions, and stable-history posture. `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness --json` now consumes the evidence quorum, retained quorum history, quorum-history alerts, inventory/verifier posture, runbook coverage, strict operator signoff command guidance, disabled execution/write posture, and closed redaction into a non-executing metadata-only readiness packet with `effective_exit_code=0`. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the retained quorum history, quorum alerts, and dry-run readiness packet with schema, surface, alert, section, action, source-contract, non-execution, and closed-redaction coverage.

Batch 156 starts retained dry-run readiness evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-history --archive --json`. The retained history stores payload-free snapshots for readiness state, evidence quorum/history/alert source states, inventory/verifier posture, stable retained quorum history, strict operator signoff command availability, disabled execution and write posture, action/section stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-dry-run-readiness` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-history-alerts --json` now turns that retained readiness history into metadata-only warning/blocker alerts for empty/single/stale evidence, readiness regressions, evidence quorum/history/alert regressions, retained quorum-history instability, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, action/section drift, command-guidance drift, payload-policy regressions, and stable-history posture before strict dry-run readiness gating. `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate --json` now adds the non-failing-by-default strict gate over the readiness packet plus retained readiness-history alerts, recommends exit codes, keeps `effective_exit_code=0` unless `--fail-on-non-clear` is explicit, and keeps release automation execution, archive writes, and payload export disabled. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the retained readiness history, readiness-history alerts, and strict readiness gate with schema, surface, alert, gate, exit-code, non-execution, and closed-redaction coverage before any future automation execution handoff.

Batch 157 starts strict dry-run readiness gate retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate-history --archive --json`. The retained history stores payload-free snapshots for strict dry-run readiness gate state, readiness source state, retained readiness-alert state, recommended and effective exit-code posture, explicit strict-mode posture, non-failing default posture, disabled release automation execution and archive-write posture, gate-count drift, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-dry-run-readiness-gate-history-alerts --json` now turns that retained strict readiness gate evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, gate-state regressions, readiness source-state regressions, recommended/effective exit-code regressions, strict-mode drift, unsafe execution/archive-write drift, command-guidance drift, payload-policy regressions, and stable-history posture before readiness operator packet assembly. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet --json` now combines the strict readiness gate, retained gate history, gate alerts, parity inventory/verifier posture, runbooks, safe operator actions, required archive guidance, explicit strict opt-in command guidance, non-failing default exit behavior, disabled execution/write posture, and closed redaction into one payload-free readiness operator packet. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the retained strict readiness gate history, gate-history alerts, and readiness operator packet with schema, surface, alert, section, action, source-contract, inventory collection, required-surface registration, non-execution, and closed-redaction coverage.

Batch 158 starts readiness operator packet evidence retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet-history --archive --json`. The retained history stores payload-free snapshots for readiness operator packet state, strict readiness gate/history/alert source states, inventory/verifier posture, recommended and effective exit-code posture, section/action/runbook stability, strict-command guidance, disabled release automation execution and archive-write posture, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-packet` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-packet-history-alerts --json` now turns that retained packet evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, packet regressions, strict readiness gate source regressions, inventory/verifier regressions, exit-code regressions, section/action/runbook drift, command-guidance drift, top-routing drift, unsafe execution/write posture, payload-policy regressions, and stable-history posture before readiness evidence quorum. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum --json` now requires a clear readiness operator packet, stable retained packet history, clear packet-history alerts, inventory/verifier posture, runbook coverage, strict dry-run readiness gate guidance, disabled execution/write posture, and closed redaction before future handoff automation. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify readiness packet history, packet-history alerts, and readiness evidence quorum with schema, surface, alert, signal, section, source-contract, inventory collection, required-surface registration, non-execution, and closed-redaction coverage.

Batch 159 starts readiness evidence quorum retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum-history --archive --json`. The retained history stores payload-free snapshots for readiness quorum state, readiness operator packet/history/alert source states, inventory/verifier posture, stable retained packet history, strict-command availability, disabled release automation execution and write posture, signal/section stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-evidence-quorum-history-alerts --json` now turns retained readiness quorum evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, quorum regressions, readiness operator packet/history/alert regressions, inventory/verifier regressions, strict-command drift, unsafe execution/write drift, signal/section drift, command-guidance drift, payload-policy regressions, and stable-history posture before the non-executing readiness handoff dry-run packet. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run --json` now packages readiness evidence quorum, retained quorum history, quorum alerts, parity inventory/verifier posture, runbooks, explicit future live-handoff blockers, disabled release automation execution/write posture, strict dry-run readiness gate guidance, safe operator actions, and closed redaction into a metadata-only dry-run packet before any future handoff automation can be considered. `dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the readiness quorum history, quorum-history alerts, and readiness handoff dry-run with schema, retained-history surface, alert, handoff section, action, future-blocker, inventory registration, collection, source-contract, and closed-redaction coverage.

Batch 160 starts readiness handoff retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run-history --archive --json`. The retained history stores payload-free snapshots for readiness handoff state, readiness evidence quorum/history/alert source states, inventory/verifier posture, strict-command availability, explicit future live-handoff blockers, disabled release automation execution and writes, section/action stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run` without executing release automation or exporting prompt, command, tool, transcript, memory, query, provider, database, workspace, or secret values. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-handoff-dry-run-history-alerts --json` now turns that retained readiness handoff evidence into metadata-only warning/blocker alerts for empty/single/stale evidence, handoff regressions, readiness source-state regressions, inventory/verifier regressions, strict-command drift, explicit future live-handoff blocker drift, unsafe execution/write drift, action/section drift, command-guidance drift, payload-policy regressions, and stable-history posture before the readiness automation operator handoff packet. `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet --json` now combines the current readiness handoff dry-run, retained handoff history, handoff alerts, parity inventory/verifier posture, runbooks, safe operator actions, explicit future live-handoff blockers, disabled execution/write posture, strict dry-run readiness gate command guidance, and closed redaction into one metadata-only operator packet while keeping effective exit code zero and live automation disabled.

Batch 160 now retains readiness operator handoff packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet-history --archive --json` and inspects it with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet-history-alerts --json`. These reports stay metadata-only while tracking packet state, readiness dry-run source states, inventory/verifier posture, strict dry-run readiness gate availability, non-execution/write posture, dry-run snapshot/alert counts, explicit future live-handoff blocker counts, action/runbook stability, section stability, command guidance, freshness, and redaction drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-packet` before the focused verification record and inventory/verifier integration.

Batch 160 also adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-verification-record --json` as the focused verification record for readiness operator handoff. It checks the current packet behavior, retained packet history, packet-history alerts, required inventory surfaces, declared verifier check ids, CLI parser coverage, non-execution guarantees, and payload-free redaction without running release automation or writing changes.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the full Batch 160 readiness handoff chain through the handoff dry-run history, handoff-history alerts, readiness operator handoff packet, retained handoff packet history, packet-history alerts, and focused verification record. The verifier covers schema versions, retained-history surfaces, alert ids, packet section/action/future-blocker ids, verification-record check and command ids, required inventory registration, source-contract wiring, and closed runtime-budget redaction before any future live handoff automation is allowed.

Batch 161 starts readiness operator handoff signoff with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff --json`. The signoff packet stays metadata-only while requiring a clear readiness operator handoff packet, stable retained packet history, clear packet-history alerts, focused verification-record coverage, inventory/verifier readiness, parity runbooks, safe operator actions, disabled release automation execution, disabled writes, zero effective exit code, and closed redaction before retained signoff history or the future strict signoff gate can be trusted.

Batch 161 now retains readiness operator handoff signoff evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-history --archive --json` and inspects it with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-history-alerts --json`. The retained history stores payload-free snapshots for signoff state, focused verification-record state, packet/history/alert source states, inventory/verifier state, non-execution posture, safe operator actions, signoff-item status, command guidance, freshness, and redaction drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff`.

Batch 161 also adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate --json` as the strict readiness operator handoff signoff gate. It remains non-failing by default, recommends nonzero exit codes for warning or blocked posture, exits nonzero only when `--fail-on-non-clear` is explicit, disables release automation execution and archive writes, and keeps payload export closed.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the Batch 161 readiness operator handoff signoff chain through the signoff packet, retained signoff history, signoff-history alerts, and strict signoff gate. The verifier covers schema versions, signoff item/action ids, retained-history surface ids, alert ids, strict gate ids, required inventory registration, source-contract wiring, and closed runtime-budget redaction before any strict handoff signoff can be treated as release-ready.

Batch 162 starts retained strict handoff signoff-gate evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate-history --archive --json`. The retained history stores metadata-only snapshots for the strict readiness operator handoff signoff gate state, signoff/source states, exit-code posture, explicit strict-mode posture, live-execution/archive-write posture, gate stability, command guidance, freshness, and redaction drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate` before the final handoff packet work.

Batch 162 now inspects retained strict handoff signoff-gate evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-signoff-gate-history-alerts --json`. The alert report stays metadata-only while warning or blocking on empty, single, stale, gate-state, exit-code, strict-mode, live-execution, archive-write, command-guidance, and payload-policy regressions, then leaves stable clear history ready for the final readiness operator handoff packet.

Batch 162 also adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet --json` as the final metadata-only readiness operator handoff packet. It combines the strict signoff gate, retained strict gate history, gate-history alerts, parity inventory/verifier posture, runbooks, safe operator actions, explicit strict opt-in guidance, disabled live execution, disabled archive writes, and closed redaction before inventory/verifier integration.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the Batch 162 retained strict signoff-gate history, gate-history alerts, and final readiness operator handoff packet. The verifier covers schema versions, retained-history surface ids, alert ids, final packet section and action ids, required inventory registration, source-contract wiring, collection coverage, and closed runtime-budget redaction before any final readiness handoff packet is treated as production-ready.

Batch 163 starts final readiness handoff packet retention with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet-history --archive --json`. The retained history stores metadata-only snapshots for final packet state, strict readiness operator handoff signoff-gate source states, inventory/verifier posture, exit-code posture, section/action/runbook stability, strict-command guidance, live-execution policy, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet` before packet-history alerts and evidence quorum work.

Batch 163 now inspects retained final readiness handoff packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-final-packet-history-alerts --json`. The alert report stays metadata-only while warning or blocking on empty, single, stale, packet-state, strict signoff-gate source, inventory/verifier, exit-code, section/action/runbook, command-guidance, execution/write, payload-policy, and stable-history posture before the readiness handoff evidence quorum is promoted.

Batch 163 also adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-evidence-quorum --json`. The quorum requires the final readiness handoff packet, retained final packet history, packet-history alerts, inventory/verifier posture, runbooks, explicit strict handoff signoff-gate command guidance, disabled execution/write posture, and closed redaction to be clear before any future readiness handoff automation promotion.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the Batch 163 retained final readiness handoff packet history, packet-history alerts, and readiness handoff evidence quorum. The verifier covers schema versions, retained-history surface ids, alert ids, quorum signal/section ids, required inventory registration, source-contract wiring, collection coverage, and closed runtime-budget redaction before final readiness handoff evidence can be treated as production-ready.

Batch 164 starts the readiness handoff promotion pack with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run --json`. The dry-run consumes the final readiness handoff evidence quorum, retained final packet history, packet-history alerts, inventory/verifier posture, runbooks, explicit strict handoff signoff-gate command guidance, disabled execution/write posture, and closed redaction while keeping `effective_exit_code=0`, recording future live-handoff blockers, and exposing only safe metadata-only operator actions.

Batch 164 now retains promotion dry-run evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run-history --archive --json` and reviews it with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-dry-run-history-alerts --json`. The retained history stores only metadata snapshots for promotion dry-run state, readiness handoff evidence quorum state, retained final packet history state, final packet alert state, inventory/verifier posture, strict handoff signoff-gate guidance, future live-handoff blocker stability, disabled execution/write posture, effective exit-code posture, operator-action and section stability, command guidance, freshness, and closed redaction. The alert report turns that retained evidence into warning/blocker posture for empty, single, stale, regressed source, strict-command drift, unsafe execution/write, action/section drift, command-guidance drift, payload-policy regression, and stable-history review without exporting protected payload values.

Batch 164 also adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack --json`. The promotion pack combines the current promotion dry-run, retained promotion dry-run history, promotion history alerts, parity inventory/verifier posture, runbook coverage, strict readiness operator handoff signoff-gate guidance, explicit future live-handoff blockers, closed redaction, and safe operator next actions into one metadata-only handoff packet. It keeps release automation execution disabled, writes disabled, effective exit code at zero, and every action non-executing until a future explicit live automation handoff gate exists.

Batch 164 now adds `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-verification-record --json` as the focused verification record before inventory/verifier integration. It checks promotion-pack behavior, retained promotion dry-run history, promotion dry-run history alerts, required promotion-chain inventory surfaces, declared verifier check ids, CLI parser coverage, non-execution guarantees, closed redaction, and safe lightweight command coverage without executing release automation or writing changes.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now expose and verify the complete Batch 164 readiness handoff promotion chain through the promotion dry-run, retained promotion dry-run history, promotion dry-run history alerts, promotion pack, and focused promotion verification record. The verifier covers schemas, section/action/future-blocker ids, retained-history surface ids, alert ids, focused verification-record check and command ids, required inventory registration, source-contract wiring, and closed runtime-budget redaction before retained promotion-pack evidence or strict promotion signoff prep begins.

Batch 165 will retain promotion-pack evidence and prepare strict signoff without changing the non-executing posture. The planned path is retained promotion-pack history, promotion-pack history alerts, a non-failing-by-default strict promotion signoff gate with explicit failure opt-in, inventory/verifier coverage for those surfaces, and an operator runbook update that keeps future live-handoff blockers explicit.

Batch 165 starts retained promotion-pack evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history --archive --json`. The retained history stores metadata-only snapshots for promotion-pack state, promotion dry-run source states, retained dry-run history state, promotion dry-run alert state, inventory/verifier posture, future live-handoff blocker counts, disabled execution/write posture, effective exit-code posture, safe action and section stability, command guidance, freshness, and closed redaction under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack` before history alerts and strict promotion signoff prep.

Batch 165 now inspects retained promotion-pack evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history-alerts --json`. The alert report stays metadata-only while warning or blocking on empty, single, stale, promotion-pack, dry-run source, inventory/verifier, future live-handoff blocker, execution/write, action/section, command-guidance, payload-policy, and stable-history posture before strict promotion signoff is introduced.

Batch 165 now adds the strict readiness handoff promotion signoff gate with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --json`. The gate composes the current promotion pack and retained promotion-pack history alerts, recommends warning or blocked exit codes, keeps the default effective exit code at `0`, requires explicit `--fail-on-non-clear` for strict CI failure, refuses live release automation execution, keeps archive writes as operator guidance only, requires future live-handoff blockers to stay visible, and keeps the report metadata-only with closed redaction.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now include the Batch 165 retained promotion-pack evidence and strict signoff surfaces. The inventory surfaces expose metadata-only posture for retained promotion-pack snapshots, history alerts, signoff-gate state, non-execution, archive guidance, future live-handoff blockers, strict opt-in exit mode, and redaction; the verifier checks the new schemas, retained-history surface ids, alert ids, signoff-gate ids, required inventory registration, source-contract wiring, and closed runtime-budget redaction flags before the operator runbook update closes the batch.

### Batch 165 Operator Promotion Signoff Runbook

Use this runbook only for metadata review. It does not authorize live release automation, archive writes outside the explicit history command, payload export, prompt export, tool-payload export, transcript export, memory-body export, provider-value export, database-path export, workspace-path export, or secret export.

1. Refresh the non-executing promotion pack with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack --json` and confirm `release_automation_execution_enabled=false`, `writes_changes=false`, `effective_exit_code=0`, closed redaction flags, and non-empty future live-handoff blockers.
2. Retain promotion-pack evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history --archive --json`, then inspect drift with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-pack-history-alerts --json`.
3. Run `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --json` for operator review. Treat `recommended_exit_code` as the signoff signal, but keep the default effective exit code at `0` during dry-run review.
4. Use `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --fail-on-non-clear --json` only in an explicit strict CI lane after the retained promotion-pack history, history alerts, inventory, verifier, and future live-handoff blocker posture are understood.
5. If the signoff gate is blocked or warning, follow `top_blocker.recovery_hint` or `next_action`, then repeat the safe promotion pack, archive, alert, inventory, verifier, and signoff-gate commands. Do not enable live execution or write automation from this runbook; a future live handoff gate must be implemented first.

Batch 166 starts retained strict promotion signoff evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history --archive --json`. The retained history stores only metadata snapshots for signoff gate state, promotion-pack source state, promotion-pack history alert state and counts, recommended/effective exit-code posture, strict-mode posture, live-execution/archive-write safety, future handoff blocker drift, gate stability, command guidance, freshness, and closed payload policy under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate` before history alerts and the final promotion operator packet are introduced.

Batch 166 now inspects retained strict promotion signoff evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history-alerts --json`. The alert report stays metadata-only while warning or blocking on empty, single, stale, gate-state, promotion source, exit-code, strict-mode, execution/archive-write, future live-handoff blocker, command-guidance, payload-policy, and stable-history posture before the final promotion operator packet is assembled.

Batch 166 now assembles the final metadata-only promotion operator packet with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet --json`. The packet combines the current strict promotion signoff gate, retained signoff-gate history, signoff-gate history alerts, parity inventory/verifier posture, runbook coverage, explicit strict CI opt-in command, future live-handoff blockers, disabled live execution/archive writes, recommendation-only exit posture, and closed redaction before the next inventory/verifier integration step.

Batch 167 starts retained final promotion packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history --archive --json`. The retained history stores only metadata snapshots for final promotion packet state, promotion signoff-gate source state, retained signoff evidence state, alert source state, inventory/verifier posture, recommended/effective exit-code posture, section/action/runbook stability, strict command guidance, future live-handoff blocker posture, execution/archive-write safety, freshness, and closed payload policy under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet` before release-boundary alerts and quorum are introduced.

Batch 167 now inspects retained final promotion packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history-alerts --json`. The alert report remains metadata-only while warning or blocking on empty, single, stale, packet-state, promotion signoff source, inventory/verifier, section/action/runbook, strict-command, future live-handoff blocker, execution/archive-write, command-guidance, payload-policy, and stable-history posture before the release-boundary evidence quorum is introduced.

Batch 167 now adds the release-boundary evidence quorum with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum --json`. The quorum requires the final promotion operator packet, retained final packet history, packet-history alerts, parity inventory/verifier posture, parity runbooks, explicit strict promotion signoff CI command guidance, disabled execution/archive-write posture, visible future live-handoff blockers, and closed redaction before any future live handoff gate is considered. It remains non-executing and exports only metadata states, counts, safe command names, runbook links, redaction booleans, and recovery hints.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now include the Batch 167 retained final promotion packet history, packet-history alerts, and release-boundary evidence quorum. Inventory exposes retained-history drift, alert posture, quorum state, strict-command availability, future live-handoff blocker counts, disabled execution/archive-write posture, and closed metadata-only redaction. The verifier checks schemas, retained-history surface ids, alert ids, quorum signal and section ids, required inventory registration, bounded source-contract coverage, and closed runtime-budget redaction before the release-boundary runbook can close the batch.

Batch 168 starts release-boundary soak evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history --archive --json`. The retained history stores only metadata snapshots for release-boundary quorum state, final promotion packet/history/alert source states, inventory/verifier posture, strict-command availability, future live-handoff blocker stability, disabled execution/archive-write safety, signal/section stability, command guidance, freshness, and closed payload policy under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum` before quorum-history alerts and future live-handoff prep are introduced.

Batch 168 now inspects retained release-boundary quorum evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history-alerts --json`. The alert report remains metadata-only while warning or blocking on empty, single, stale, quorum-state, final promotion packet source, retained packet history, packet-history alert, inventory/verifier, strict-command, future live-handoff blocker, signal/section, execution/write, command-guidance, payload-policy, and stable-history posture before the future live-handoff prep packet is introduced.

Batch 168 now adds the future live-handoff prep packet with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-prep-packet --json`. The packet consumes current release-boundary quorum evidence, retained quorum history, quorum-history alerts, parity inventory/verifier posture, runbook coverage, explicit strict promotion signoff CI guidance, disabled release/live execution, disabled archive writes, disabled change writes, visible future live-handoff blockers, and closed redaction. It stays metadata-only, exposes safe operator actions, keeps live handoff execution unavailable, and treats archive retention as an explicit metadata history command rather than an enabled live-write path.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now include the Batch 168 retained release-boundary quorum history, quorum-history alerts, and future live-handoff prep packet. Inventory exposes retained-history drift, alert posture, prep-packet state, strict-command readiness, future live-handoff blocker counts, disabled release/live execution, disabled archive writes, disabled change writes, and closed redaction. The verifier checks schemas, retained-history surface ids, alert ids, prep-packet section/action ids, required inventory registration, bounded source-contract coverage, and runtime-budget redaction flags before the live-handoff prep runbook closes the batch.

Batch 169 starts future live-handoff gate design with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet --json`. The packet consumes the Batch 168 prep packet, exposes explicit operator approval blockers and rollback blockers, carries strict CI signoff guidance, keeps release automation execution disabled, keeps live handoff execution disabled, keeps archive writes disabled, keeps change writes disabled, and exports only metadata states, counts, action ids, command names, runbook paths, redaction booleans, and recovery hints. It is a design-review packet only; it does not implement or enable live execution.

Batch 169 now retains future live-handoff gate design evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history --archive --json`. The retained history stores metadata-only snapshots for design-packet state, prep packet readiness, release-boundary quorum/history/alert source states, inventory/verifier posture, strict CI command availability, visible future live-handoff blockers, explicit approval blockers, rollback blockers, non-execution and archive-write posture, operator action safety, section stability, command guidance, freshness, and payload-policy drift under `target/runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet`.

`dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history-alerts --json` now turns retained gate-design evidence into metadata-only warning/blocker alerts for empty, single, stale, design-state, prep-source, inventory/verifier, strict-command, approval-blocker, rollback-readiness, execution/archive-write, operator-action, section, command-guidance, payload-policy, and stable-history posture. The alert report exports only alert ids, severities, counts, states, safe command names, drift booleans, action safety booleans, and recovery hints.

Parity inventory and `dx-agents parity verify --json` now cover the future live-handoff gate design packet, retained gate-design history, and gate-design history alerts. The verifier checks schema versions, gate-design section ids, safe operator action ids, retained-history surface ids, alert ids, required inventory registration, and closed runtime-budget redaction flags, while bounded source-contract tests keep the full inventory/verifier surface lists from drifting silently.

Batch 170 begins implementation-readiness controls with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-implementation-readiness-manifest --json`. The manifest consumes the current future live-handoff gate design packet and exports only metadata for implementation ownership, command allowlists, rollback owners, audit evidence, live-gate preconditions, disabled-by-default policy, safe operator actions, and redaction state. It keeps release automation execution disabled, live handoff execution disabled, archive writes disabled, and change writes disabled; simulator, retained-history, alert, inventory, and verifier coverage remain the next Batch 170 steps.

### Batch 169 Future Live-Handoff Gate Design Runbook

Use this runbook only for metadata review and live-gate design readiness. It does not authorize live release automation, does not authorize live handoff execution, does not authorize archive writes outside explicit retained-history commands, and does not authorize change writes, prompt export, command-payload export, tool-payload export, transcript export, memory-body export, query export, provider-value export, database-path export, workspace-path export, or secret export. Live execution remains disabled until an explicit future live-handoff implementation batch exists, lands, passes focused tests, and defines a separate opt-in gate.

1. Refresh the design packet with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet --json` and require `design_state=clear`, visible explicit approval blockers, visible rollback blockers, `release_automation_execution_enabled=false`, `live_handoff_execution_enabled=false`, `archive_writes_enabled=false`, `writes_changes=false`, and closed redaction before continuing.
2. Retain design evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history --archive --json`, then review `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-gate-design-packet-history-alerts --json`. Treat empty, single, stale, design, prep/source, inventory/verifier, strict-command, approval, rollback, execution/archive, action, section, command-guidance, and payload-policy alerts as blockers for live-gate design review.
3. Run `dx-agents parity inventory --json` and `dx-agents parity verify --json` and require the future live-handoff gate design packet, retained history, and history-alert surfaces to be present with closed runtime-budget redaction flags.
4. Use `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --fail-on-non-clear --json` only in an explicit strict CI lane after the design packet, retained design history, history alerts, inventory, verifier, approval blockers, rollback blockers, and payload-policy checks are understood.
5. If any report is warning or blocked, follow `latest_safe_command`, `top_alert.recovery_hint`, or `next_action`, then repeat the design packet, archive, alert, inventory, verifier, and strict CI review commands. Do not enable release automation, live handoff execution, archive writes, or change writes from this runbook.
6. Start a separate explicit future live-handoff implementation batch before adding any live execution path. That batch must define ownership, allowlists, rollback behavior, secret handling, durable audit evidence, strict opt-in behavior, and focused tests before a live gate can exist.

### Batch 168 Future Live-Handoff Prep Runbook

Use this runbook only for metadata review and future gate preparation. It does not authorize live release automation, live handoff execution, archive writes outside explicit retained-history commands, payload export, prompt export, command-payload export, tool-payload export, transcript export, memory-body export, query export, provider-value export, database-path export, workspace-path export, or secret export.

1. Refresh the release-boundary quorum with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum --json` and require `quorum_state=clear`, `quorum_met=true`, `release_automation_execution_enabled=false`, `writes_changes=false`, visible future live-handoff blockers, and closed redaction before continuing.
2. Retain soak evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history --archive --json`, then review `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum-history-alerts --json`. Treat empty, single, stale, regressed, unsafe execution/write, command-guidance, and payload-policy alerts as blockers for future live gate design.
3. Review `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-future-live-handoff-prep-packet --json` and require `prep_state=clear`, `live_handoff_execution_enabled=false`, `archive_writes_enabled=false`, `writes_changes=false`, visible future live-handoff blockers, safe operator actions, and closed redaction.
4. Run `dx-agents parity inventory --json` and `dx-agents parity verify --json` after the prep packet. Confirm retained release-boundary history, quorum-history alerts, and future live-handoff prep remain registered and verified before any strict CI lane consumes the evidence.
5. Use `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --fail-on-non-clear --json` only in an explicit strict CI lane after quorum, retained soak history, quorum alerts, prep packet, inventory, verifier, and runbook review are clear.
6. Do not enable live handoff execution, release automation execution, archive writes, or change writes from this runbook. A separate explicit future live gate must be designed, reviewed, tested, and intentionally enabled before DX Agents can execute live handoff work.
7. Keep all exports metadata-only: no prompts, command payloads, tool payloads, transcripts, memory bodies, query text, provider values, database paths, workspace paths, or secrets.

### Batch 167 Release-Boundary Runbook

Use this runbook only for metadata review. It does not authorize live release automation, live handoff execution, archive writes outside explicit retained-history commands, payload export, prompt export, tool-payload export, transcript export, memory-body export, provider-value export, database-path export, workspace-path export, or secret export.

1. Refresh the final promotion operator packet with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet --json` and confirm `effective_exit_code=0`, future live-handoff blockers are visible, live execution is disabled, archive writes are disabled, and redaction is closed.
2. Retain final packet evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history --archive --json`, then inspect drift with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet-history-alerts --json`.
3. Review `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-release-boundary-evidence-quorum --json` and require `quorum_state=clear`, `quorum_met=true`, `release_automation_execution_enabled=false`, `writes_changes=false`, nonzero future handoff blocker counts, and closed redaction before considering any future live handoff gate work.
4. Use `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --fail-on-non-clear --json` only in an explicit strict CI lane after retained packet history, history alerts, inventory, verifier, runbook coverage, and future live-handoff blocker posture are understood.
5. If the quorum is warning or blocked, follow `latest_safe_command` or `next_action`, then repeat the final packet, archive, alert, inventory, verifier, and quorum commands. Do not enable live automation execution or write automation from this runbook; a future live handoff gate must be implemented separately.
6. Keep all exports metadata-only: no prompts, command payloads, tool payloads, transcripts, memory bodies, query text, provider values, database paths, workspace paths, or secrets.

`dx-agents parity inventory --json` and `dx-agents parity verify --json` now include the Batch 166 retained strict promotion signoff evidence and final promotion operator packet. The inventory surfaces expose metadata-only posture for retained signoff-gate snapshots, history alerts, packet sections/actions, strict CI opt-in guidance, future live-handoff blockers, disabled live execution/archive writes, and closed redaction; the verifier checks the new schemas, retained-history surface ids, alert ids, final packet section/action ids, required inventory registration, source-contract wiring, and closed runtime-budget redaction flags before the final promotion packet runbook update closes the batch.

### Batch 166 Final Promotion Operator Packet Runbook

1. Refresh the strict promotion signoff gate with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --json` and confirm the default effective exit code remains `0`, live execution is disabled, archive writes are disabled, and future live-handoff blockers are visible.
2. Retain signoff evidence with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history --archive --json`, then inspect drift with `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate-history-alerts --json`.
3. Review `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-operator-packet --json` as the final promotion packet. Treat `recommended_exit_code` and `top_blocker` as review signals, but keep `effective_exit_code=0` unless an explicit strict CI lane is being run.
4. Use `dx-agents parity runtime-budget-ci-promotion-release-automation-readiness-operator-handoff-promotion-signoff-gate --fail-on-non-clear --json` only in the strict CI lane after retained signoff history, history alerts, inventory, verifier, runbook coverage, and future live-handoff blockers are all understood.
5. Do not enable live release automation execution or archive writes from this packet. A future live handoff gate must be implemented separately before DX Agents can convert the recommendation-only packet into executing automation.
6. Do not export prompts, command payloads, tool payloads, transcripts, memory bodies, query text, provider values, database paths, workspace paths, or secrets while reviewing or storing the final promotion packet.

## Deferred

| Area | Reason |
| --- | --- |
| Full internal crate rename from `zeroclaw-*` to `dx-agents-*` | Deferred to avoid broad dependency churn before core parity checks pass. Internal names remain an upstream-merge compatibility layer. |
| Every OpenClaw channel/app surface | First batch is core runtime parity; optional surfaces should be ported one at a time with tests. |
| Full Hermes RL/training stack | Hermes is Python-centered and research-heavy; DX Agents should learn concepts, not copy the architecture directly. |
| Voice wake, live canvas, mobile nodes | Deferred unless already present and cheap to preserve. They need dedicated UX/runtime validation. |
| Full generated book/localized installer docs rename | Deferred until code checks pass; generated docs and translated PO files are broad churn and should be refreshed in one docs-specific pass. |
| Broad full-surface inventory/verifier smoke tests | Deferred as ignored manual diagnostics because they still exercise recursive/heavy collector paths; Batch 142 now covers the default path with bounded source-contract tests and collection-depth guards. |

## Follow-Up Work

1. Move optional user-facing docs and installer scripts from legacy names to DX Agents after the Rust checks are green.
