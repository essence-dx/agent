{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for DX Agents Desktop",
  "windows": ["main", "onboarding", "dx-bridge"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "shell:allow-open",
    "store:allow-get",
    "store:allow-set",
    "store:allow-save",
    "store:allow-load"
  ]
}
